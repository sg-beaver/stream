# 개발 로그

개발 과정에서 **발견한 문제를 어떻게 테스트하고 개선했는지** 남기는 기록입니다. 단순 작업 일지가 아니며, 문제 발견 → 검증 → 개선의 전후 비교가 있는 항목만 추가합니다. 작성 규칙은 `CLAUDE.md`의 "개발 로그 규칙"을 따릅니다.

**배정 시간 합계**: 이 로그에 자주 나오는 값입니다. "몇 명이 몇 시간 일했는지"를 모두 더한 것으로,
두 명이 같은 한 시간을 함께 근무하면 2시간으로 셉니다 — 그래서 개관 시간보다 클 수 있습니다.
검증기 응답의 `coverage.assigned_hours`, 생성 응답의 `per_student[].total_hours` 합과 같은 값입니다.

핵심 기록 대상 (우선순위 순):

- Hard Constraint 검증: 반드시 지켜야 하는 근무 조건 위반이 0건인지
- 근무 형평성 검증: `fair_hours` 적용 전/후 특정 학생 편중 정도 변화
- Edge Case 검증: 인원 부족·조건 충돌 시 Solver가 의도한 결과를 내는지
- 성능 비교: 수작업 시간표 작성 시간 vs Solver 생성 시간

---

## 템플릿

새 항목은 아래 템플릿을 복사해 이 구분선 바로 아래(최신이 위)에 추가합니다.

```markdown
## YYYY-MM-DD — 제목 (한 줄 요약)

- **문제/가설**: 무엇이 잘못됐다고 판단했는가 / 무엇을 확인하려 했는가
- **테스트 조건**: 사용한 데이터·설정·실행 방법 (재현 가능하게)
- **Before**: 수정 전 실측 수치 (Solver status·solve time 포함)
- **수정 내용**: 바꾼 코드/설정과 그 이유 (커밋·PR 링크)
- **After**: 동일 조건 재실행 실측 수치
```

---

<!-- 여기부터 최신 항목이 위로 오도록 기록합니다. -->

## 2026-09-01 — AI 검토가 자연어 규칙만 보던 문제: 규정 제약(Hard)은 서버가 채점해 붙이고 소프트 페널티는 프롬프트로 넘겨 함께 보게 함 (#242)

- **문제/가설**: AI 검토(`POST /api/schedule/review`)의 판단 기준이 부서의 자연어 규칙과 학생 특이사항뿐이라, 개관 시간·가용 시간·근로 시간 상한 같은 **규정 제약 위반이 초안에 섞여 있어도 검토 패널이 아무 말도 하지 않는다**고 봤다. 특히 `POST /api/schedule/draft/edits`로 손으로 고친 초안은 솔버 제약 밖으로 나갈 수 있는데, 확정 전에 그것을 짚는 화면 경로가 이 패널뿐이다. 판정은 이미 `verify.py`(#156)가 결정적으로 할 수 있으므로 LLM에 다시 계산시키지 않는다(#114에서 주간 시간 집계를 서버가 미리 계산해 넣은 것과 같은 이유).
- **테스트 조건**: ① 로컬 dev DB(postgres)의 실제 draft 배치 **46·47번**(정보서비스팀, 2주: 2026-08-31\~09-13 / 09-14\~09-27, 배정 40건). ② 같은 배치 47번에 **개관(22:00) 밖 23:00-23:30 근무 1건을 세션에 넣고(commit 없이 rollback)** 손으로 고친 초안을 흉내 냈다 — 변경 전 코드(develop)와 변경 후 코드에서 같은 주입으로 각각 1회 호출. ③ 검출률 하네스 `backend/scripts/eval_review.py` 26케이스 × 1회를 변경 전/후 같은 조건으로. 모델은 프로덕션 기본값 `gemini-3.7-flash`.
- **Before**: 위반을 주입한 47번 배치에 대해 검토 결과 **findings 0건**, summary는 "명백한 부서 운영 규칙 위반 사항은 확인되지 않았습니다" — 개관 시간 밖 배정이 실제로 있는데 검토는 통과로 읽혔다. 검출률 하네스 **26/26(100%)**, 평균 응답 **10.5초**(최소 3.4 / 최대 35.8), 호출당 평균 **4,670토큰**(총 121,411).
- **수정 내용**: `review_batch`가 시작할 때 `verify.verify_batch`를 함께 돌려 ⓐ 결과를 프롬프트 `## 규정 제약 검증 결과` 절에 "확정 사실, 재계산·중복 기재 금지"로 넣고 ⓑ 위반을 규칙별로 묶어 **서버가 만든 finding(`source="system"`)**으로 응답에 붙인다 — LLM이 빠뜨려도 화면에서 사라지지 않게. 소프트 제약은 `solver_summary`의 `penalty_summary`·`penalty_events`를 카테고리별(SC ID·부서 중요도 배율·상위 이벤트 3건)로 `## 소프트 제약(선호) 페널티` 절에 넣되 "위반이 아니라 비용"이라 critical 금지로 지시했다. 검토 시작 조건도 넓혔다(자연어 규칙이 없어도 critical 위반·페널티가 있으면 검토, 최소 인원 미달 warning만으로는 시작하지 않음). 검증 실패는 그 절만 "확인 불가"로 비우고 검토를 계속한다.
- **After**: 같은 주입 초안에서 **critical finding 3건** — `HC-OPEN`(20220091 09-14(월) 23:00-23:30), `HC-TIME-1`(그 주 14.5시간 > 상한 14시간), `HC-TIME-4`(부서 2주 교비 190.5시간 > 상한 190시간) — 이 뜨고, summary도 "총 3건의 위반"으로 시작한다. 30분짜리 배정 하나가 주간·부서 상한까지 밀어 올린다는 것이 함께 드러났다. 오탐 확인: 손대지 않은 46·47번 원본은 `verify` 위반 **0건**, coverage **100%**(240/240 슬롯)이고 검토 findings도 **0건**이었다. 검출률 하네스는 **26/26(100%)로 동일**, 평균 응답 **12.0초**(3.6\~40.1), 호출당 평균 **5,443토큰**(+16.5%, 총 141,523) — 절 두 개가 늘어난 만큼 프롬프트가 커졌고 검출률 손실은 없다(실 배치 프롬프트 7,181자). 실호출 통합 테스트 `test_review_live.py` 26건 중 25건 통과, 1건(`note-soft-violation`)은 재실행 시 통과하는 LLM 비결정성. 유닛 테스트는 live 제외 **652건 전부 통과**.
- **남은 것**: 소프트 페널티 집계는 **생성 시점 값**이라 `draft/edits`로 고친 배정은 반영되지 않는다(규정 제약 채점은 매번 다시 읽으므로 반영된다) — 프롬프트에 그 한계를 밝혀 두었을 뿐 재계산은 하지 않는다. GEMINI_API_KEY가 없거나 AI 호출이 실패하면 규정 위반이 있어도 응답은 종전대로 `review_available=false`다 — 결정적으로 판정한 값이라 화면에 띄울 수 있지만, 응답 형태를 바꾸는 문제라 이번 범위에 넣지 않았다(`GET /api/schedule/verify`로는 볼 수 있다). 검출률 하네스는 여전히 자연어 규칙만 재고 규정 제약 절은 "위반 없음"으로 고정한다.
## 2026-09-02 — AI 검토가 담당자의 기억에 달려 있던 문제: 생성에 이어 자동으로 돌리고, 대기 모달을 접을 수 있게 (#250)

- **문제/가설**: AI 검토는 검토 화면의 버튼을 눌러야만 돌았다. 초안을 훑고 바로 '시간표 확정'을 누르면 검토는 한 번도 돌지 않은 채 확정된다 — 확정 전에 규칙 위반을 짚어줄 마지막 지점이 담당자가 버튼을 기억하느냐에 달려 있었다. 대기 모달도 최소화가 없는 전면 오버레이라 솔버가 도는 30초 동안 수합 시간표조차 볼 수 없었다.
- **테스트 조건**: 로컬 dev(백엔드 8000 · 프론트 5173, 워크트리 `stream-feat-generate-flow-auto-review`) + 시드 DB. 직원 STF001로 로그인 → 정보서비스팀(부서 2), 2026.09.14\~09.27(2주) 생성 3회. 검토는 mock이 아니라 실제 Gemini 호출이다.
- **Before**: 생성 3회 모두 `POST /api/schedule/review` 자동 호출 **0건**(버튼을 눌러야 1건). 생성 중 볼 수 있는 화면 **0개**.
- **수정 내용**: `handleGenerate`가 저장된 초안을 다시 읽은 뒤 같은 `batch_id`로 `runReview`를 이어 부른다(수동 '다시 검토'와 같은 경로). 대기 모달은 `배정 계산 → AI 검토` 2단계가 되고, '작게 보기'로 접으면 진입 화면으로 돌아가 수합·확정 시간표를 보는 동안 우하단 카드가 진행을 알린다. 검토 단계에는 '초안 먼저 보기'를 둬 모달만 닫는다. 검토가 도는 동안에는 확정·재생성을 잠근다.
- **After**: 생성 3회 전부 generate 직후 review **1건 자동 발생**(백엔드 로그). 솔버 **OPTIMAL 5.59 / 5.68 / 6.48초**, 배정 40건 · 미충원 0칸. 1회차는 생성 시작 **11초 안에** 검토까지 끝났고, 2회차는 **Gemini 503 UNAVAILABLE** → `review_available=false`(`ai_error`)로 화면에 조용히 표시되고 초안은 정상이었다. 3회차는 검토가 길어져 생성 시작 **약 28초** 만에 채워졌다(#195 기록의 평균 6.5초 · 최대 19.8초보다 김). 접은 상태에서 '수합된 근무 시간표' 조회 → '결과 보기'로 초안 복귀까지 확인. `npm run build` 통과, 콘솔 에러 0건.
- **남은 것**: 28초 관측 때문에 모달 문구를 25초부터 '초안 먼저 보기'를 권하는 쪽으로 바꿨지만, **검토 자체의 소요는 따로 재지 않고** 생성 시작 기준 경과 시간만 보여준다. 챗봇이 초안을 고친 뒤(#137)에는 자동 재검토를 하지 않는다 — 편집마다 부르면 호출이 늘어, 담당자가 '다시 검토'로 부르는 쪽을 택했다.

## 2026-09-01 — 챗봇이 근무를 뺄 때 혼자 근무·블록 경계를 못 보던 문제: 조회 응답에 인원·블록 맥락을 붙이고 후보 우선순위를 프롬프트로 고정 (#195)

- **문제/가설**: 화면에서 "주 15시간이라 교비 상한 14시간을 넘었으니 한 시간 줄여 달라"고 했더니, 챗봇이 후보 3건 중 **가장 깔끔한 것을 빼고** 나머지 둘을 1·2안으로 추천했다 — 빠진 후보는 금 18:00\~19:00(1시간짜리 독립 블록, 같은 시간에 다른 근무자 있음)이고 추천된 것은 목 오전 3시간(혼자 근무, 1시간만 줄이면 블록이 쪼개진다)과 토요일(혼자 근무)이었다. 가설은 두 겹의 결손이다. ① 프롬프트에 "무엇을 뺄지" 고르는 기준이 한 줄도 없다(있는 것은 "여러 건이면 확인받아라"·"요청 없는 수정 금지"뿐). ② `find_schedules`를 `student_name`으로 걸면 **그 학생 행만** 오므로 동시 근무 인원·최소 인원·부서 근무 블록이 모델에게 아예 안 보인다. 편집 후 알림으로 대체되지도 않는다 — 최소 인원 미달은 warning이라 `_apply_edits_via_service`의 `new_violations`(critical만)에서 의도적으로 제외돼 있다.
- **테스트 조건**: `scripts/eval_chat.py`에 시나리오 `over_hour_cap`(조수현 주 15시간: 월·화 15:00\~19:00 각 4시간·혼자, 목 09:00\~12:00 3시간·혼자, 금 18:00\~19:00 1시간·권지영 동석, **금 20:00\~21:00 1시간·혼자 = 미끼**, 토 10:00\~12:00 2시간·혼자)과 케이스 2건 추가. `gemini-3.7-flash`, `--repeat 3`, 실제 Gemini 호출. Before는 **같은 케이스 파일**로 `_staffing_annotations`를 빈 dict로 만들고 프롬프트 절·툴 설명을 제거한 상태(`--repeat 3`). 단위 테스트는 `backend/.venv/bin/python3 -m pytest`.
- **Before**: 새 케이스 **0/6**. `over-cap-cuts-standalone-hour` 0/3 — 세 번 모두 `find_schedules`만 부르고 쓰기 0건(`turn_status=null`)으로 "어느 근무를 조정할지 결정해 주셔야 합니다"라고 되물었다. `over-cap-candidates-cite-staffing` 0/3 — 후보는 나열했지만 인원 근거(혼자·동석·다른 근무자·최소 인원·권지영) 문구가 한 번도 없었다. **처음 만든 케이스는 거짓 통과했다**: 1시간짜리 후보가 하나뿐이라 길이만 보고도 맞혔고, 키워드 `단독`이 "1시간짜리 **단독** 배정"(=독립 조각, 인원과 무관)에 매칭돼 2/3이 통과했다 — 그래서 같은 모양의 1시간 후보(금 20:00\~21:00·혼자)를 미끼로 넣고 키워드에서 `단독`·`인원`을 뺐다. 기존 21케이스 회귀 기준값: **21/21**, 턴당 툴 2.52회, 턴당 토큰 평균 **17,444**(최대 34,346).
- **수정 내용**: ① `chat._staffing_annotations` 신설 — `find_schedules` 결과의 배정마다 `hours`·`headcount`(동시 근무 인원, 자기 포함)·`min_required`·`coworkers`·`understaffed_if_removed`·`work_blocks`·`block_aligned`를 서버가 붙인다. 조회 필터와 무관하게 **그 날짜 전원**의 배정으로 슬롯 점유를 세고, 정책·캘린더는 솔버·검증과 같은 창구(`apply_department_overrides` → `OpeningHoursResolver`, `resolve_slot_staffing`)로 읽는다. 슬롯마다 인원이 다르면 **가장 빈 슬롯** 기준으로 답한다. 정책 파일이 없는 부서는 인원 정보만 빠지고 조회는 그대로 나간다. 이름은 기존 경로대로 `deid.mask_data`가 별칭으로 가려 나간다. ② 시스템 프롬프트에 "근무를 빼거나 줄일 때 어느 배정을 고를지" 절 추가 — 후보 우선순위 4단(길이가 맞고 인원이 안 깨지는 삭제 → 블록 경계에 맞는 축소 → 블록을 쪼개는 축소 → 혼자 근무 자리)과 "가장 좋은 후보를 1안으로, 이유를 붙여라", "위임받았으면 1안을 바로 적용하라"를 못박았다. ③ 툴 선언 설명에 새 필드 뜻을 넣었다. ④ 단위 테스트 `tests/test_schedule_chat_staffing.py` 5건(동석자 있는 1시간 블록 / 혼자 근무 / 부분 배정 비정렬 / 가장 빈 슬롯 기준 / 정책 파일 없을 때 조용한 축퇴).
- **After**: 새 케이스 **6/6**(edit 3/3·basic 3/3). 답변이 미끼를 근거로 기각한다 — "2안: 금 20:00\~21:00 … 혼자 근무하는 시간대이므로 삭제할 경우 해당 시간에 근무자가 없어 인원 공백이 발생합니다". edit 케이스는 3회 모두 `find_schedules` → `remove_schedule` 2회로 금 18:00\~19:00만 삭제(`turn_status=applied`, 쓰기 호출 1회, 새 위반 0건). 단위 테스트 5/5, 챗봇·근무표 관련 기존 테스트 **403 passed**. 전체 하네스 **23/23**(`--repeat 1`, basic 7·edit 8·guard 8, 호출 실패 0건, 예산 소진 0턴). 같은 21케이스 회귀: **21/21** 유지, 턴당 툴 2.52 → 2.43회, 턴당 토큰 평균 17,444 → **19,243(+10%)**.
- **비용**: 인원·블록 필드는 항상 붙는다 — `find_schedules` 응답 크기가 결정적으로 **+107\~120%**(배정 7건 1,091 → 2,257자, 배정당 약 165자) 늘었다. 위 턴당 토큰 +10%는 `--repeat 1` 단일 실행 비교라 케이스별 흔들림이 섞여 있어 방향만 신뢰할 값이다(툴 호출 수가 실행마다 1씩 흔들리는 것은 #202에서도 관측).
- **남은 것**: `--repeat 3`으로 다시 재 토큰 증가분을 확정해야 한다. 하네스는 **또 포화**(23/23)이므로 다음 프롬프트 변경 전에 케이스를 먼저 늘려야 한다 — 특히 이번 절이 다루는 "블록 경계에 맞춰 줄이기"(우선순위 2·3단)는 아직 케이스가 없다. 실제 화면(dev DB)에서 같은 질문을 다시 해 보는 확인은 아직 안 했다 — 위 수치는 전부 하네스·테스트 실측이다.
## 2026-09-01 — 주 상한을 넘긴 시간표가 계속 나오던 문제: 솔버·확정·화면이 서로 다른 '주'를 세고 있었다

- **문제/가설**: 화면이 "송형준 주 17시간 — 상한 14시간 초과" 같은 위반을 계속 띄웠다. 가설은 **솔버가 제약을 어긴 게 아니라 세 층이 서로 다른 단위를 센다**는 것. 확인해 보니 솔버(`constraints/hard.py`)는 ISO 주(월\~일)를 세되 **이번 생성 그리드 안의 슬롯만** 세고, 확정 검증(`work_hours.py`)은 ISO 주를 부서·배치 안 가리고 전부 세고, 화면(`findViolations`)은 **기간 시작일부터 7일 창**을 센다. 여기서 두 가지가 갈라져 나온다 — ① 기간이 주 중간에 시작하면 화면의 7일 창이 ISO 주 두 개에 걸쳐 오탐이 난다 ② 한 ISO 주가 생성 회차마다 상한을 새로 받는다. ②는 월 상한이 `prior_monthly_hours`로 이미 고친 것과 같은 버그인데 주 상한에는 대응 필드가 없었다.
- **테스트 조건**: 로컬 dev(postgres + `backend/.venv`). 정보서비스팀(부서 2), `POST /api/schedule/generate`를 직접 호출해 응답의 `schedules`를 ISO 주별·7일 창별로 각각 합산. 회귀는 `backend/.venv/bin/python3 -m pytest`(sqlite in-memory).
- **Before**: ① `2026-09-01(화)` 시작 1주 — Solver **OPTIMAL / 1.74초**, 배정 26건. ISO 주 기준으로는 전원 상한 이내(조수현 16.5 + 13.0 / 상한 20, 윤영민 12.5 + 4.0 / 14, 박민진 7.0 + 8.5 / 14)인데 **7일 창으로는 29.5h · 16.5h · 15.5h**. 화요일 시작이라 `09-07(월)` 하루가 ISO 37주 전체로 취급돼 상한을 통째로 새로 받았다. ② `09-01~09-07`과 `09-08~09-14`를 잇달아 생성(둘 다 **OPTIMAL**, 1.71초 / 1.67초) — 경계 주 ISO 37 합계가 **박민진 19.5h · 윤영민 16.5h** (상한 14h). 솔버가 자기가 명시한 규칙을 어긴 결과다. 이 초안은 확정 단계에서 400으로 거부된다(확정은 ISO 주 전체를 센다) — **생성은 되고 확정은 안 되는** 상태.
- **수정 내용**: 세 층이 같은 것을 세게 만들었다. ① `Student.prior_weekly_hours`(ISO 연·주 → 시간)와 `_prior_weekly_hours()` 로더를 추가하고 `WeeklyHourLimitConstraint`가 `상한 − 기근무`만 배정하게 했다. 집계 범위는 확정 검증과 같다(부서·draft·manual 포함, superseded 제외, 기간 안 날짜 제외). ② `POST /api/schedule/generate`가 월요일이 아닌 시작일을 400으로 막는다. ③ 2주 단위 부서를 위해 `app/biweekly.py` 신설 — **3월 개강 첫날이 든 주가 1주차**이고 거기서 14일 간격이 블록 경계다. 14의 배수(2주·4주·학기 패턴) 생성은 블록 시작에서만 허용하고, 거부 시 앞뒤 블록 시작일을 함께 알려 준다. 화면은 두 규칙을 눌리기 전에 표시하고 버튼을 잠그며, 기본 시작일이 블록 중간이면 다음 블록 시작으로 한 번 민다. 화면의 7일 창 판정은 그대로 뒀다 — **월요일 시작이 강제되면 7일 창이 곧 ISO 주라 저절로 일치한다.**
- **After**: ① `09-01(화)` 1주 → **400** (`시작일은 월요일이어야 합니다 … 2026-09-01은 화요일입니다`). ② `09-07(월)` 2주 → **400** (`28주차 — 블록 중간 … 2026-08-31 또는 2026-09-14에서 시작해 주세요`), `09-21` 2주 → 400(30주차). ③ `09-14` 2주 → **OPTIMAL / 3.21초**, `08-31` 2주 → **OPTIMAL / 2.62초**. `09-14` 결과의 ISO 주별 합계는 9명 전원 상한 이내(최대 14.0/14), **연속 7일 최대도 14.0h** — 상한 초과 0명. ④ 제약이 차감을 실제로 쓰는지 솔버로 직접 확인(학생 1명·슬롯당 1명이라 상한만이 배정량을 정하는 구성): 이월 없음 **14h** → 6h 이월 **8h** → 9h 이월 **5h** → 14h·20h 이월 **0h**, 다른 주 이월 12h는 **14h**(영향 없음). 회귀 **690 passed → 721 passed, 0 failed** (신규 31건, 8분 10초).
- **남은 것**: 챗봇 재생성(`scheduler/chat.py`)에는 두 가드를 걸지 않았다 — 기존 초안의 기간을 그대로 다시 푸는 경로라 날짜를 새로 고르지 않고, 가드를 걸면 이 수정 전에 만들어진(월요일이 아닌) 초안은 챗봇으로 손댈 방법이 없어진다. 이월 차감은 이 경로에도 그대로 적용된다. 이미 만들어져 있는 초안·확정본은 소급해서 고쳐지지 않는다. 1주·3주 생성은 월요일 검사까지만 한다 — 애초에 격주 주기에 얹히지 않는 기간이라 블록 경계를 물을 수 없다. 학사 캘린더가 없는 해(`academic_calendar_YYYY.json` 부재)는 블록 판정을 못 해 월요일 검사로만 떨어진다. 2월 말 잔여 구간 처리는 **규칙으로만 반영**했고(학년도가 3월에 리셋된다) 잔여 주를 자동으로 잡아 주지는 않는다.

## 2026-09-01 — 근무표 요일 열이 하루씩 밀려 다른 날 배정을 편집하던 문제: 열 번호 대신 실제 요일로 날짜를 붙이고, 제약 위반을 검토 모드에서도 표시

- **문제/가설**: QA 중 배정 편집에서 화요일 09:00\~10:30 칸을 눌렀는데 그 칸에 적힌 학생이 아닌 다른 학생이 체크되어 나왔다. 가설은 **주 격자의 요일 열과 날짜가 어긋난다**는 것 — 격자는 백엔드가 날짜에서 뽑아 준 `day_of_week`(한글 라벨)로 칸을 채우는데, 칸을 눌렀을 때의 날짜는 `addDaysIso(week.start, DAY_COLS.indexOf(day))`로 **열 번호**만큼 더해 만든다. `splitWeeks`가 배정 기간 시작일부터 7일씩 자르므로 `week.start`는 월요일이 아닐 수 있고, 그러면 두 계산이 갈린다. 함께, 제약 위반 표시가 `editing` 조건 안에 있어 검토만 하는 담당자에게는 보이지 않는다는 점도 확인 대상으로 삼았다.
- **테스트 조건**: 로컬 dev(postgres + `backend/.venv`, 프론트 vite). 정보서비스팀(부서 2), 학생팀장 김현서(20220042)로 로그인 → 근무표 편성에서 **시작일 2026.09.01(화요일)** · 기간 1주(7일)로 생성. 순수 함수는 `weekDayDates`/`periodByDayOfWeek`를 esbuild로 번들해 node로 직접 호출(월 시작 주 / 화 시작 주 / 3일짜리 마지막 주).
- **Before**: Solver status **OPTIMAL**, solve time **1.79초**, 배정 26건 · 미충원 0칸 (이 항목은 솔버가 아니라 화면 문제라 이후 수치는 동일 초안 기준이다). 화요일 열은 `권지영 · 배시온` 칸인데 그 칸을 눌러 뜬 모달은 제목이 **`09.02(화)`**(09.02는 실제로 수요일) 이고 체크된 학생은 **수요일 열의 `오규원 · 권지영`** — 하루 뒤 날짜의 배정을 편집하고 있었다. 요일↔날짜 불일치 helper 검사: 화 시작 주 **7건 중 7건 불일치**. `periodByDayOfWeek`도 같은 방식이라 화 시작 주의 '월' 열에 화요일 날짜의 학기/방학 값이 들어갔다. 제약 위반 3건(박민진 15.5h·윤영민 16.5h·조수현 29.5h, 상한 초과)은 **'배정 편집'을 누르기 전에는 배너도 빗금도 뜨지 않았다**.
- **수정 내용**: `weekDayDates(start, end)`를 `utils/scheduleGrid.js`에 추가해 창 안을 훑어 **요일 → 실제 날짜**를 붙이고, `cellOfSlot`이 이 표를 쓰게 했다. `weekDaySubLabels`도 같은 방식으로 바꾸고 주간 근무 시간표에 날짜 머리글을 붙여 어긋남이 눈에 띄게 했다. `periodByDayOfWeek`는 열 번호 대신 `weekStart`의 실제 요일만큼 밀어서 센다(월요일 시작이면 종전과 동일). 창이 7일보다 짧은 마지막 주는 없는 요일 열을 편집 대상에서 뺀다. 위반 쪽은 가능 시간 조회와 `findViolations`를 `editing`과 무관하게 돌리고 배너·빗금·범례를 검토 모드에서도 표시한다.
- **After**: 동일 초안에서 화요일 09:00 칸 → 모달 제목 **`09.01(화)`**, 체크된 학생 **`권지영 · 배시온`**(칸 내용과 일치). 요일↔날짜 불일치: 화 시작 주 **0건**, 월 시작 주 **0건**, 3일짜리 마지막 주 **0건**(없는 4개 요일은 키 자체가 빠져 편집 불가). `periodByDayOfWeek`(학기 2026-09-03\~ 정책) 화 시작 주 = `{월:semester, 화:vacation, 수:vacation, 목\~일:semester}` — '월' 열이 09-07의 값을 받는다. 위반 3건이 초안을 연 즉시 배너·빗금으로 표시됨(편집 버튼 누르기 전). `npm run build` 통과, 브라우저 콘솔 에러 없음(로그인 전 `/api/academic/terms` 401 2건은 기존 동작).
- **남은 것**: `splitWeeks`는 여전히 기간 시작일 기준으로 7일씩 자른다 — 화요일 시작 주에서는 '월' 열이 그 창의 **마지막 날**이라 시간순으로 읽으면 헷갈린다(날짜 머리글로 드러나게만 해 뒀다). 주 경계를 월요일로 스냅할지는 주간 상한(월\~일 기준)과 함께 따로 판단해야 한다.

## 2026-09-01 — 근무를 얹는 경로마다 겹침·개관 검사가 빠져 있던 문제: 검사를 공용 모듈로 올리고 대타 수락·승인·재적용에 연결

- **문제/가설**: 코드베이스 훑기로 대타·확정 경로 버그 6건을 찾고 전부 임시 pytest로 재현했다. 가설은 **#159가 상한 검사에서 겪은 일이 겹침·개관 검사에서 반복되고 있다**는 것 — `_check_no_overlap`은 `routers/schedule.py` 안에만 있어 대타 라우터가 부를 수 없었고, `check_within_opening_hours`는 draft 편집·수동 등록에만 걸려 확정 경로가 비어 있었다. 근무를 얹는 경로가 여럿인데 검사가 한 라우터에 갇혀 있으면 나머지 경로는 전부 무방비다.
- **테스트 조건**: `backend/.venv/bin/python3 -m pytest`, sqlite in-memory. 시나리오는 A의 미래 월요일 14\~18시 확정 근무 1건, 같은 부서 B, 다른 부서 D. 개관 검사는 정보서비스팀 기본 정책 2026-09 기준(09/13 일요일 폐관, 09/25 추석 폐관, 월 08:00\~22:00).
- **Before**: 비회귀 스위트 **627 passed**. 재현 실측 — ① B가 같은 날 15\~17시 근무를 가진 채로 `respond` **200** → `approve` **200**, B의 그날 행이 `[(14:00,18:00),(15:00,17:00)]` 이중 배정. ② 타 부서 D도 `respond`/`approve` **200**. ③ 요청자 A가 자기 요청을 `respond` **200**. ④ 승인된 대타(15\~17시 B)가 있는 상태에서 솔버가 B를 15\~17시에 따로 배정한 계획으로 재확정하니 confirm **201**, 새 배치의 B 행이 `[(15:00,17:00),(15:00,17:00)]`이고 응답은 `confirmed_count:2, released_substitutes:[]`로 **아무것도 알리지 않았다**. ⑤ confirm이 일요일·추석 폐관일·05\~07시를 전부 **201**로 통과(같은 배정을 draft 편집·수동 등록으로 넣으면 셋 다 400). ⑥ 후보 탐색이 그날 종일 불가 예외를 낸 학생을 후보로 올리고, 다른 학기 가능시간으로 후보를 만들고, 선호도가 달라 두 행으로 나뉜 연속 구간은 못 덮는다고 보아 실제로 가능한 학생을 후보에서 뺐다.
- **수정 내용**: ① `app/overlap.py` 신설 — `find_overlap`·`check_no_overlap`을 `schedule.py`에서 옮겨 `work_hours.py`·`opening_hours.py`와 같은 층에 뒀다. ② `app/availability_windows.py` 신설 — 그 날짜의 실제 가능 구간을 솔버가 쓰는 `materialize_availability`로 전개(학기·날짜 예외 반영), `covers()`가 맞닿은 구간을 이어서 센다. ③ `_ineligibility_reason`을 만들어 **후보 목록에 뜨는 조건과 수락·승인이 통과하는 조건을 같은 함수로** 묶고, `respond`·`approve` 양쪽에서 `_ensure_eligible_substitute`로 부른다(자격 문제 409, 주간 상한 400 — 상한 400은 확정·수동 등록이 이미 쓰는 계약이라 유지). ④ confirm의 승격 직전 재검증에 `_validate_confirm_opening_hours` 추가. ⑤ `apply_approved_substitutes`가 얹기 전에 대타 학생의 겹침을 보고, 겹치면 얹는 대신 `해제됨`으로 돌려 `released_substitutes`에 `reason`과 함께 실어 알린다(#231의 길 재사용).
- **After**: 동일 조건 재실행 **644 passed, 0 failed** (627 + 신규 회귀 17건, 기존 실패 0). 위 재현 6건이 각각 — ①②③ `respond` **409**, ④ 새 배치의 B 행 `[(15:00,17:00)]` 1건 + 요청 상태 `해제됨` + `released_substitutes` 1건에 사유 표시, ⑤ 세 경우 모두 **400**(휴관일/개관 시간 문구 확인), ⑥ 예외·학기로는 후보에서 빠지고 선호도 분할 구간은 후보로 살아난다. 과잉 차단이 아님을 확인하는 대조 테스트도 함께 넣었다 — 개관 시간 안 확정 201, 겹치지 않는 대타 재적용은 종전대로 분할, 사이가 진짜 빈 가능시간은 여전히 후보 제외.
- **남은 것**: 재적용 시 대타 학생의 **주간 상한**은 아직 다시 보지 않는다(겹침만 본다). `list_department_availability_by_date`가 같은 전개를 기간 단위로 중복 구현하고 있어 `availability_windows`로 합치는 게 맞다. 화면(dev DB) 확인은 아직 안 했다 — 위 수치는 전부 테스트 실측이다.

## 2026-09-01 — 재확정이 승인된 대타를 조용히 되돌리던 문제: 승인을 배치 독립적 사실로 올리고 확정 배치를 합성 결과로 (#178)

- **문제/가설**: 확정 → 부분 대타 승인 → 겹치는 기간 재확정 후 대타 구간이 원 근무자에게 돌아간다(#178, 본문은 "코드를 읽은 결론"). 가설은 **층위가 섞였다**는 것 — 솔버 배정(재생성되는 계획)과 승인된 대타(사람이 확정한 예외)가 같은 `work_schedule` 층에 있고, `_split_schedule`이 예외를 확정 배치 안에 써 넣기 때문에 계획을 갈아끼우면 예외가 함께 쓸려나간다. 더 근본적으로 `substitute_request`에 `work_date`·`department_id`가 없어 **승인 사실이 superseded될 행 하나에 매달려 있었다**.
- **테스트 조건**: 재현 테스트는 이미 있었다 — `test_substitute_split_survives_reconfirm.py`의 strict xfail 2건(그동안의 "2 xfailed"). `--runxfail`로 실측값을 뽑고, 이슈가 미결로 둔 '요청 기록 화면' 항목까지 임시 진단으로 확인했다. 시나리오는 A의 14\~18시 중 15\~17시를 B에게 승인 후 재확정.
- **Before**: 활성 행 `[(14:00,15:00,A),(15:00,17:00,B),(17:00,18:00,A)]` → 재확정 후 **`[(14:00,18:00,A)]`**. B는 새 배치에 행이 0건. `substitute_request.schedule_id`가 가리키는 배치 status = **superseded**. 그런데 A·B의 `/me`와 담당자 목록은 셋 다 **`승인`으로 정상 표시** — 근무표엔 없는데 기록엔 승인이라는 불일치. 확정 응답은 `{"batch_id":2,"status":"confirmed","confirmed_count":1,"adjusted_dates":[]}`로 **되돌림을 한 글자도 알리지 않았다**.
- **수정 내용**: ① `substitute_request`에 `work_date`·`department_id` 추가(#229) — 승인이 `(부서, 날짜, 시각, 넘긴 사람, 받은 사람)` 좌표로 성립하고 `schedule_id`는 "지금 반영된 행"을 가리키는 가변 포인터로 강등. 기존 행은 `schema_patches` 역채움. ② `app/substitute_overrides.py` 신설(#230) — `확정 배치 = materialize(솔버 배정, 그 기간의 승인된 대타)`. confirm의 `db.add_all` 직접 삽입을 `_materialize_confirmed_rows` 호출로 바꿔 **확정 배치를 만드는 자리를 하나로 좁혔다**. 재적용을 호출부마다 기억해 부르면 행을 갈아끼우는 경로(draft 재생성·편집·챗봇)가 늘 때마다 빠뜨릴 자리가 생기기 때문. 승인과 재적용이 같은 `split_for_substitute`를 쓴다. ③ 얹을 자리가 없으면 `해제됨` 상태로 전이하고 확정 응답 `released_substitutes`로 알린다(#231).
- **After**: 재현 테스트 **3건 전부 통과**(xfail 마커 제거, 회귀 테스트로 전환). 신규 4건 추가 — 자리 없음 해제·부분 겹침 해제·되돌려진 것 없으면 빈 목록·해제는 종결(나중에 자리가 생겨도 부활 안 함). 백엔드 전체 **660 passed, 2 xfailed → 0 xfailed**.
- **화면 확인**(dev DB, 실제 클릭): 마이그레이션 4행 전부 역채움 성공(NULL 0, 원본 불일치 0). 부서2를 재확정하니 확정 완료 화면에 **"승인된 대타 1건이 해제되었습니다 … 2026.09.07 18:00\~20:00"** 표시. 최유나(요청자)·오규원(대타) 양쪽 '요청 기록'이 **`해제됨` + "근무표가 다시 확정되면서 이 시간의 근무가 없어져 대타가 해제되었습니다"**로 바뀌었고, 오규원 근무표에서 금색 '대타 근무' 칸이 사라져 **기록과 근무표가 일치**한다. 부서명이 superseded 행을 타지 않고 새 좌표에서 렌더되는 것도 함께 확인.
- **설계 결정(이슈 표와 다름)**: 요청 구간이 새 계획에 **일부만** 겹치면 겹치는 만큼 적용하지 않고 해제한다. "15\~17시를 B가 한다"는 승인인데 A가 15\~16시만 일한다면 겹치는 만큼 넘기는 것은 **B가 동의하지 않은 다른 근무를 만드는 일**이라 사람이 다시 정하게 뒀다.
- **미확인**: 알림은 화면 표시까지다 — 이 저장소에 푸시·메일 알림 수단이 없다(Notification 모델 없음).


## 2026-09-01 — 확정본과 기간이 겹치는 draft에서는 칸 하나 빼기가 저장되지 않았다: 겹침 검사에도 supersede 제외를 적용

- **문제/가설**: #214(칸 하나만 빼기)를 **화면에서 직접 눌러 확인**하던 중 저장이 400으로 실패했다 — `{"error":"이미 2026-09-07 09:00-12:00에 배정이 있어 겹칩니다."}`. 화면 동작은 정상인데(큐 3건, 클릭한 칸만 사라짐) 서버가 거부했다. 가설: 되넣는 `add`가 **다른 배치**의 행과 겹침 판정을 받는다 — `_find_overlap`은 batch를 가리지 않고 draft/confirmed/manual 전체를 본다.
- **테스트 조건**: 로컬 dev DB + 실제 브라우저. 정보서비스팀 draft(batch 38, 09-07\~09-20)와 **확정 batch 8(08-31\~09-13)이 기간 중복**. 조수현 09-07 09:00\~22:00(13h 병합 행)에서 18:00\~19:00 칸을 빼고 저장. 대조군으로 확정 기간 **밖**인 09-14의 같은 조작도 수행.
- **Before**: 09-07 → **400**, 지목된 행은 확정 batch 8의 `schedule_id=470`(조수현 09-07 09:00-12:00). 09-14 → **200**(46행 → 47행, 13h가 9h+3h로 분리). 같은 조작이 날짜에 따라 갈렸다. DB는 실패 시 부분 적용 없이 깨끗하게 롤백됐다(46행·원본 행 유지).
- **원인**: `_validate_slot_and_limits`가 `exclude_batch_ids`(이 draft를 확정하면 내려갈 confirmed 배치들, `_superseded_by_draft_ids`)를 **주간 상한 검사에는 넘기면서 겹침 검사에는 넘기지 않았다.** #212에서 상한 축에만 적용한 규칙이 겹침 축에 빠져 있었다. `add`·`move` 양쪽 호출부는 이미 이 집합을 계산해 전달하고 있었고, 함수 안에서 버려지고 있었다.
- **수정 내용**: `_check_no_overlap` 호출에 `exclude_batch_ids`를 함께 넘긴다(`app/routers/schedule.py`). 수동(manual) 배치는 확정해도 내려가지 않아 이 집합에 들어가지 않으므로 계속 겹침으로 막힌다.
- **After**(동일 조건 재현 — draft를 다시 생성해 batch 39에서 같은 셀·같은 payload): **200**. 조수현 09-07이 09:00\~18:00(9h) + 19:00\~22:00(3h) = **12h**(= 13h − 뺀 칸 1h), 확정 batch 8은 55행 그대로. 백엔드 전체 **654 passed, 2 xfailed**(실패 0).
- **동작 변경 1건**: 내려갈 확정본 자리로의 draft 편집이 이제 허용된다. 이 전제로 쓰여 있던 기존 테스트 2건(`test_move_onto_other_assignment_is_400`, 챗봇 `test_validation_failure_is_partial_failed_and_draft_unchanged`)은 겹침 대상을 **실제로 남는 배정**(같은 draft·manual)으로 바꿔 원래 의도를 유지했다. 신규 회귀 5건 추가 — 확정본 자리 add·move 허용, manual·같은 draft 겹침은 그대로 거부, #214식 부분 빼기 저장.


## 2026-08-31 — #222가 바꾼 기록 키를 따라가지 못한 테스트 4곳: 2건은 깨지고 2건은 조용히 무력화됐다

- **문제/가설**: #222(PR #224)가 역연산 기록 키를 `tool_calls[].inverse`(단수) → `inverses`(배열)로 바꿨는데, 옛 키를 문자열로 단언하던 테스트가 남아 있다. 가설은 두 가지 피해가 동시에 났다는 것 — **존재**를 단언하던 곳은 실패로 드러나지만, **부재**를 단언하던 곳(`"inverse" not in c`)은 이제 쓰기 호출에도 항상 참이라 **깨지지 않고 조용히 무력화**된다.
- **테스트 조건**: `origin/develop`(262779c, #224 머지 직후)에서 백엔드 전체를 실제 Gemini 호출 포함으로 실행. #224 본문의 "614 passed, 33 skipped"는 API 키가 없어 live 테스트가 skip된 실행이라 이 회귀가 잡히지 않았다.
- **Before**(develop 262779c): 전체 **646 passed / 2 failed** — `test_chat_live.py::test_edit_request_finds_before_moving`(`move.get("inverse")` → None), `::test_weight_complaint_uses_adjust_weight`(`.get("inverse", {}).get("op")` → None). 부재 단언 2곳(`test_schedule_chat_api.py`)은 통과했지만 **이제 무엇도 검증하지 않는다** — 쓰기 호출도 `inverse` 키를 갖지 않으므로 되돌리기 버튼이 잘못 떠도 잡지 못한다.
- **수정 내용**: 네 곳 모두 키를 직접 읽지 않고 프로덕션 판정 함수 `chat.call_inverses()`를 쓰도록 바꿨다 — 이 함수가 `inverses`(신규)와 `inverse`(구 이력 호환)를 모두 읽고, 화면의 "되돌릴 것이 있는 호출인가" 판정도 같은 함수로 한다. 키 이름이 또 바뀌어도 테스트가 따라온다. 프로덕션 코드 변경 없음.
- **After**: 전체 **647 passed / 0 failed**(2 xfailed). 부재 단언이 다시 유효하다는 근거 — 같은 실행에서 live 쓰기 테스트가 `chat.call_inverses(move)`를 참으로 통과했으므로, 쓰기 호출에 대해 이 함수는 실제로 비어 있지 않다.


## 2026-08-31 — 챗봇 다건 삭제가 툴 호출 예산에 걸려 부분 적용으로 끝나던 건(#222): 쓰기 툴을 다건화하고 호출을 원자 단위로 만들었다

- **문제/가설**: `remove_schedule`·`move_schedule`·`add_schedule`이 한 번에 1건만 처리하는데 턴당 툴 호출 예산은 5회다(`STEP_BUDGET`). 그래서 "다 빼줘"류 요청 한 턴의 상한이 `find_schedules` 1회 + 삭제 4회가 된다. 대상이 5건 이상이면 예산이 소진되며 `budget_exceeded`로 끊기는데, **이미 적용된 삭제는 롤백되지 않는다** — 담당자는 "다 빼줘"라고 했는데 앞 4건만 지워진 상태로 남고, 화면 문구("요청이 너무 커서 중간에 멈췄습니다")를 "아무것도 안 됐다"로 읽으면 그대로 넘어간다. #213을 고치며 인접 문제로 발견했다(원인·수정 방법이 달라 이슈를 나눔).
- **테스트 조건**:
  - 빈도 확인 — 로컬 seed DB(Postgres `stream_db`)에서 `(batch_id, student_id, 요일)`별 배정 수 분포를 집계했다.
  - 동작 계측 — in-memory sqlite에 3주짜리 draft, 조수현 학생의 **수요일 6건**(9/9·9/16·9/23 각각 오전 09:00\~12:00·오후 13:00\~17:00, 이슈가 지목한 "하루가 두 블록으로 나뉜 경우"). 같은 DB·같은 대상에 대해 ① 삭제를 한 건씩 부르는 경우 ② `schedule_ids`로 한 번에 부르는 경우를 `run_turn`으로 돌려 비교했다(`_llm_step` mock, LLM 호출 없음 — `scratchpad/measure_222.py`). 쓰기 툴이 다건화 후에도 단수 인자를 받으므로 ①이 곧 수정 전 모델 동작이다.
- **Before**:
  - 빈도: 학생·요일 그룹 230개 중 5건 이상은 **1개(0.4%)**, 4건은 25개(10.9%). 4건은 `find` 1 + 삭제 4 = 5로 예산에 딱 맞아 통과하고, 5건부터 깨진다. 즉 2주 근무표에서는 드물고 블록 분할·3주 이상 기간에서 터진다.
  - `chat_message.turn_status = 'budget_exceeded'` 이력: 로컬 개발 DB 기준 **0건**(전체 10건 모두 null). 배포 이력은 확인하지 못했다.
  - 동작: 툴 호출 7회(쓰기 6회 중 성공 4회), `turn_status='budget_exceeded'`, **삭제 4/6건**, draft에 2건 잔여, 기록된 역연산 4건.
- **수정 내용**:
  - 쓰기 툴 인자를 배열로 — `move_schedule`·`remove_schedule`은 `schedule_ids`, `add_schedule`은 `work_dates`. 단수 인자(`schedule_id`·`work_date`)도 계속 받는다: 다건화 후에도 모델이 학습된 형태로 단수를 보낼 때가 있는데 그때 거부하면 예산만 태우고 아무것도 못 고친다.
  - **호출 하나를 원자 단위로** — `_apply_edits_via_service`가 SAVEPOINT(`db.begin_nested()`) 안에서 건별로 `apply_draft_edit`를 돌리고, 한 건이라도 실패하면 되감아 그 호출은 아무것도 적용하지 않는다. 부분 적용 상태를 만들지 않는 것이 이 이슈의 목적이므로 역연산도 "전부 기록되거나 하나도 기록되지 않는다". 역연산 재적용이 아니라 DB 되감기라 살아남은 배정의 `schedule_id`도 그대로다. sqlite(테스트)·Postgres(운영) 양쪽에서 SAVEPOINT 동작을 먼저 확인한 뒤 택했다.
  - 기록 키를 `tool_calls[].inverse`(단수) → `inverses`(배열, 적용 순서)로. `revert_turn`은 호출 역순 + 호출 안에서도 역순으로 적용하며, 저장된 옛 이력의 단수 `inverse`도 계속 읽는다(`call_inverses`). 라우터·프론트의 "되돌릴 것이 있는 턴" 판정과 "변경 N건" 표시도 호출 수가 아니라 **편집 건수**를 세도록 고쳤다 — `adjust_weight`의 수동 편집 소실 경고(`_pending_manual_edits`)가 같은 이유로 과소 집계되고 있었다.
  - 호출당 상한 `CHAT_MAX_EDIT_ITEMS`(기본 20). 예산이 아니라 이 값이 "한 번에 얼마나"를 정한다 — 되돌리기 한 번으로 감당할 크기로 묶어 두려는 것. 중복 id는 조용히 접는다(두 번째가 404로 호출 전체를 무산시키지 않게).
  - 여러 건 이동에 `work_date`를 함께 주는 것은 거부한다 — 같은 날 같은 시각으로 보내면 서로 겹쳐 어차피 전부 실패한다.
  - 시스템 프롬프트에 "여러 건은 한 호출에 담아라", "한 호출은 전부 적용되거나 전부 아니다"를 명시. 예산 안내 문구도 "쓰기는 한 호출에 담으면 보통 조회 1 + 쓰기 1로 끝난다"로 고쳤다.
- **After**(동일 DB·동일 6건): 툴 호출 **2회**(쓰기 1회 성공), `turn_status='applied'`, **삭제 6/6건**, draft 잔여 0건, 기록된 역연산 6건 — 되돌리기 한 번으로 6건이 전부 복구된다. 원자성 확인(회귀 테스트 `test_batch_removes_are_all_or_nothing`, 수요일 5건 배치): 실제 id 3건 + 없는 id 1건을 한 호출로 보내면 `"4건 중 4번째(배정 999999)에서 실패해 이번 요청은 하나도 적용하지 않았습니다: 해당 배정을 찾을 수 없습니다."`를 돌려주고, 앞 3건은 지워졌다가 되감겨 **5건이 그대로** 남았다(역연산 기록 0건).
- **회귀**: 백엔드 전체 **614 passed, 33 skipped, 2 xfailed**(실패 0). 신규 `TestBatchWrites` 9건 포함 — 다건 삭제·원자성·되돌리기 전건 복구·단수 인자 호환·중복 id·상한 초과·다건 이동·다건 추가.
- **eval 실측**(`scripts/eval_chat.py`, `model=gemini-3.7-flash`, `--repeat 3`, 실제 Gemini 호출, 2회 실행): **63/63 (100%)** — basic 18/18, edit 21/21, guard 24/24, 호출 실패 0건. 턴당 툴 2.22회/2.21회, 쓰기 25건/24건, **예산 소진 0턴·부분 실패 0턴**(두 실행 모두). 통과 여부는 두 실행이 같고 축 수치만 1씩 흔들린다 — `temperature=0`이어도 툴 호출 수는 완전히 결정적이지 않다. 두 번째 실행 기준 "제약 위반을 만든 쓰기 6건"은 전부 그걸 의도한 케이스(`edit-breaking-availability`·`overstaffing-edit-is-reported`, `expect_new_violations: true`)에서 나온 것이다. 턴 소요 평균 8.7초(최대 22.6초), 턴당 토큰 평균 15,606. 신규 케이스 `batch-remove-in-one-call`(시나리오 `many_same_weekday` = 수요일 6건)은 **두 실행 모두 6회 전부** `find_schedules` → `remove_schedule` **단 2회**로 끝냈고(쓰기 호출 1회, `turn_status='applied'`), 판정 축은 고친 건수가 아니라 **부른 횟수** 상한(`expect_max_write_calls: 1`)으로 잰다. 회귀 우려였던 #213의 두 케이스도 그대로다 — 두 케이스 다 두 실행 모두 쓰기 0건·`turn_status=null`이었다 — `multi-target-delete-asks-first`는 매번 `find_schedules` 1회만 부르고 되물었다.
- **묶이지 않는 경우**: `bulk-edit-within-budget`("근무 두 건을 각각 한 시간씩")은 두 실행 모두 6회 전부 `move_schedule`을 2회 부른다. 대상마다 목표 시각이 달라 하나의 `schedule_ids` 호출로 표현할 수 없기 때문이며(다건 이동은 "각자 날짜에 두고 같은 시각으로"만 지원), 의도된 동작이다. 학생·시각이 제각각인 편집은 여전히 건수만큼 호출이 필요하다 — 예산 상한이 그 경우에는 그대로 남아 있다.
- **계측기 포화**: 21케이스 전부 통과라 하네스가 "이 상태로는 다음 변경의 효과를 잴 수 없다"고 다시 경고한다(#195, #213에 이어 3회째).
- **미확인**: 배포 DB의 `budget_exceeded` 이력은 확인하지 못했다.

## 2026-08-31 — 챗봇이 없는 요일 근무를 "삭제했다"고 답한 건(#213): `find_schedules`가 요일 인자를 조용히 무시하고 있었다

- **문제/가설**: 배포 서버에서 "이화정 수요일 근무 다 빼줘"에 챗봇이 "수요일 근무 2건을 모두 삭제했습니다"라고 삭제 내역(날짜·시간)까지 답했는데, 이화정은 애초에 수요일 배정이 없었다(#213). 같은 패턴을 로컬에서 3번 재시도했을 땐 3번 다 정상이라 "간헐적 LLM 환각"으로 보였다. 그런데 `find_schedules`를 보면 **요일 인자가 아예 없고 결과에도 요일이 없다** — 담당자 발화는 전부 요일 단위인데 배정은 날짜로만 오간다. 가설: 모델이 요일 필터를 걸었다고 믿고 실제로는 안 걸린 결과를 받거나, 날짜→요일을 스스로 계산하다 틀린다. 즉 모델의 자유 창작이 아니라 **툴이 요일을 다루지 못해 생기는** 오답이다.
- **테스트 조건**: in-memory sqlite에 부서 1개·학생 1명(이화정), draft 배치에 **월요일 2건만** 배정(2026-09-07·09-14 09:00\~10:30, 수요일 근무 0건 — #213 재현 조건). 같은 DB 상태에서 `develop` 판 `chat.py`와 수정판을 각각 임포트해 `_tool_find_schedules` 반환값과 툴 선언 인자 목록을 비교했다(LLM 호출 없음, `scratchpad/measure_213.py`).
- **Before**(`develop`, 커밋 4e7ce8f): 선언된 인자 5개 — `date_from`·`date_to`·`student_id`·`student_name`·`work_date`, **요일 인자 없음**. `find_schedules(student_name="이화정", weekday="수")`가 오류도 빈 결과도 아닌 **월요일 배정 2건을 그대로 반환**했다(`count: 2`) — 인자가 조용히 무시돼 필터 없는 조회로 넓어진 것이다. 반환된 각 배정에는 `work_date`만 있고 요일 표시가 없다. 즉 모델은 자기가 건 조건대로 걸러진 2건을 받아 들고 "수요일 근무 2건"이라고 설명하게 된다 — 실사용에서 나온 문구와 건수가 정확히 이 모양이다.
- **수정 내용**:
  - `_tool_find_schedules`에 **`weekday` 인자 신설**(월\~일, "수요일" 형태도 허용) — 요일→날짜 환산을 서버가 한다. 못 읽는 값은 `ValueError`로 되돌린다.
  - 반환하는 각 배정에 **`day`(요일) 추가** — 답변에 쓰는 요일은 계산이 아니라 조회 결과에서 옮겨 적게 된다. 이미 `get_student_availability`·`get_period_calendar`가 쓰던 표기(`_DAY_NAMES`)와 같다.
  - **모르는 인자를 조용히 무시하지 않는다** — `find_schedules`가 정의되지 않은 키를 받으면 `ValueError`. 필터가 무시되면 결과가 조건 없이 넓어지는데 모델은 걸러진 줄 알기 때문에, 이 침묵이 위 경로의 실제 입구였다.
  - `chat_system_prompt.md` 강화 — 날짜↔요일을 직접 계산 금지, `count: 0`이면 쓰기 툴 호출 없이 "없다"고 답할 것, "삭제했다"고 말할 수 있는 건 쓰기 툴이 `ok`를 돌려준 건뿐이고 응답의 건수·날짜·시간은 전부 툴 결과에 있던 값이어야 한다는 지침 추가.
  - `_llm_step`에 **`temperature=0.0`**(`CHAT_TEMPERATURE`로 조정 가능) — 조회·편집 과제라 같은 데이터에 같은 답이 나와야 한다. 기존에는 Gemini 기본값(1.0)이었다.
  - **다건 삭제·이동 사전 확인** — 요일·기간 단위 요청에서 `find_schedules` 대상이 2건 이상이면 그 턴에는 쓰기 툴을 부르지 말고 날짜·요일·시간 목록을 제시한 뒤 진행 여부를 묻고, 동의한 다음 턴에만 적용하라는 프롬프트 규칙을 넣었다. 근무표 기간에는 같은 요일이 여러 번 들어 있어("월요일" = 2주면 2일) 담당자가 특정 주차 하나만 생각하고 말했을 수 있는데, 되묻는 장치가 아예 없었다. 승인 절차를 코드로 넣지 않은 이유는 설계 결정 11(쓰기는 즉시 적용, 되돌리기로 취소)을 뒤집기 때문이다. 대상이 1건이거나 담당자가 날짜·시간·건수를 이미 특정한 경우는 예외로 명시했다 — 기존 eval 케이스 `bulk-edit-within-budget`("근무 두 건을 각각 한 시간씩")이 이 예외에 걸린다.
- **After**(동일 DB·동일 기간): 선언된 인자 6개(`weekday` 추가). `find_schedules(student_name="이화정", weekday="수")`가 **`count: 0`, `schedules: []`** — 없는 요일 근무가 이제 빈 결과로 정직하게 나온다. 요일 없이 조회하면 2건 모두 `"day": "월"`이 붙어 온다. `weekday="Wednesday"`·`day_of_week="수"`는 각각 "요일을 알 수 없습니다"·"모르는 인자입니다"로 거부된다. 회귀: `test_schedule_chat_api.py` 27 passed(신규 4건 포함), `test_schedule_chat_write_tools.py`·`test_schedule_chat_date_scope.py`·`test_schedule_chat_verify_tool.py`·`test_schedule_chat_weight_tool.py`·`scheduler/test_deidentify.py` 87 passed.
- **eval 실측**(`scripts/eval_chat.py`, `model=gemini-3.7-flash`, `--repeat 3`, 실제 Gemini 호출): guard **24/24**(8케이스), edit **18/18**(6케이스), 호출 실패 0건. 신규 케이스 2건 모두 3/3 — `absent-weekday-is-not-deleted`는 `find_schedules`만 부르고 "수요일에 배정된 근무가 없습니다"로 답했으며 쓰기 0건, `multi-target-delete-asks-first`는 `find_schedules` 1회 뒤 "9월 7일(월)·9월 14일(월) 두 건을 모두 삭제하시겠습니까?"로 되물었다(쓰기 0건, `turn_status: null`). 회귀 우려였던 `bulk-edit-within-budget`("근무 두 건을 각각")도 3/3으로 그대로 쓰기를 수행했다 — 건수 예외 조항이 의도대로 동작한다. 축: guard는 턴당 툴 2.17회·쓰기 0건, edit은 턴당 툴 2.22회·쓰기 21건, 양쪽 다 예산 소진 0턴·부분 실패 0턴.
- **계측기 포화**: 두 층 다 만점이라 하네스가 "이 상태로는 다음 변경의 효과를 잴 수 없다"고 경고한다 — #195에 이미 올라와 있는 문제이고, 이번에 케이스를 2건 늘렸지만 해소되지는 않았다.
- **미해결**: 배포 서버 그 메시지의 `chat_message.tool_calls` 원본은 아직 확인하지 못했다(#213 확인사항 1). 화면의 "변경 반영됨" 배지는 `turn_status`, "되돌리기" 버튼은 `tool_calls[].inverse`로 판정하는데(`ScheduleChatPanel.MessageBubble`) **둘 다 쓰기 툴 성공에만 붙는다** — 코드상 툴 호출 없이는 뜰 수 없다(확인사항 2, 회귀 테스트 `test_hallucinated_delete_leaves_no_applied_marker`로 고정). 그래서 그 화면에 배지가 실제로 떴다면 **그 턴에서 진짜 삭제가 일어난 것**이고, 이화정의 다른 요일 근무가 지워졌을 수 있다. 배포 DB에서 확인이 필요하다.
- **분리한 문제**: 쓰기 툴이 1건 단위인데 턴당 툴 예산이 5회라, 요일 단위 삭제 대상이 5건 이상이면 `find_schedules` 1회 + `remove_schedule` 4회에서 예산이 끊기고 이미 적용된 삭제는 롤백되지 않은 채 `budget_exceeded`로 끝난다. 원인·해법이 이 건과 달라 #222로 분리했다.

## 2026-08-31 — 챗봇이 "1주차와 2주차의 입력 데이터는 완전히 같습니다"라고 확인 없이 단정했다: 날짜 단위 조회 창 신설

- **문제/가설**: 담당자가 "주차마다 배정이 왜 다르냐"고 묻자 챗봇이 위 문장으로 답했다. 그런데 챗봇의 읽기 툴 `get_student_availability`는 `AvailableTime`·`ClassTime`을 **`day_of_week` 기준으로만** 읽었다 — `chat.py` 전체에 `AvailabilityException` 참조가 0건이었다. 반면 솔버(`service._load_students`)는 같은 기간을 **날짜 단위**로 본다: 기간 내 `AvailabilityException`을 읽어 `materialize_availability`로 날짜마다 전개하고, 학사 캘린더(공휴일·교내 휴강일·폐관일)를 `OpeningHoursResolver`로 날짜마다 적용한다. 가설: 챗봇은 틀린 추론을 한 게 아니라 **자기가 볼 수 있는 유일한 창(요일 표)의 모양을 입력 전체의 모양으로 단정**했다. 창을 솔버와 같은 범위로 넓히면 사라지는 종류의 오답이다.
- **테스트 조건**: in-memory sqlite에 부서 1개(`availability_mode="weekly_with_exceptions"`, 정책 파일은 기본값 `library_info_service`), 학생 1명, 요일 반복 가능시간 월~금 09:00-18:00(두 주차가 **완전히 동일**), 기간 2026-09-21~10-04(2주). 그 기간에 날짜별 예외 3건(9/22 부분 불가·9/28 종일 불가·10/2 저녁 추가 가능)과 기간 밖 1건(10/10)을 넣었다. 학사 캘린더상 1주차에는 폐관일 9/24~26, 2주차에는 공휴일 10/1·10/3이 있다. 같은 DB 상태에서 `develop` 판 `chat.py`와 수정판을 각각 임포트해 툴 반환값을 비교했다(LLM 호출 없음).
- **Before**(`develop`, 커밋 78e7cfe): `get_student_availability` 반환 키 5개(`student_id`·`student_name`·`term`·`available_times`·`class_times`), DB에 **3건 있는 기간 내 날짜별 예외를 0건** 반환. 읽기 툴 4종에 학사 캘린더를 조회할 수단 없음 — 즉 모델이 "주차별 입력이 같다"를 **확인할 방법 자체가 없는** 상태에서 그렇게 답했다.
- **수정 내용**:
  - `get_student_availability` 확장 — 기간 내 `AvailabilityException`(날짜·요일·타입·시간·종일 여부·preference)을 함께 반환하고, 각 행에 **`applied`** 플래그를 붙였다. `materialize_availability`가 `availability_mode`로 예외를 걸러내므로, 그 판정을 같이 하지 않으면 모델이 **솔버가 보지도 않은 신고를 배정 근거로** 삼는다(모르는 모드는 원본과 같이 `weekly_only`로 fail-closed). 기간이 학기 경계를 넘을 때 시작일 학기 하나만 읽던 것도 `term_segments`로 넓혔다(#156의 솔버 규칙과 동일). 부서 경계 검증은 그대로다.
  - 읽기 툴 `get_period_calendar` 신설 — 기간의 날짜별 학사 일정(공휴일·교내 휴강일·폐관일·시험 기간·학기/방학)과 그날의 부서 개관 시간, 그리고 주차별 요약(근무 가능일 수·특이일)을 반환한다. 캘린더·정책은 솔버·검증(`verify._build_context`)과 **같은 창구**로 읽어, 실제 배정에 쓰인 값과 어긋난 근거를 대지 않게 했다.
  - `chat_system_prompt.md`에 "주차·날짜 간 차이를 설명할 때" 절 추가 — 주차별 입력의 동일/상이는 위 두 툴로 **실제 조회한 뒤에만** 말하고, 조회하지 않았거나 원인이 안 보이면 추측을 사실처럼 말하지 말고 확인하지 못했다고 밝히라는 지침.
- **After**(동일 DB·동일 기간): `get_student_availability`가 기간 내 날짜별 예외 **3건 전부** 반환(기간 밖 10/10은 제외), 반환 키 10개. `get_period_calendar`가 같은 기간을 주차별로 갈라 **1주차 근무 가능일 3일**(9/24~26 폐관일) **대 2주차 6일**(10/1·10/3 공휴일 단축 개관 09:00-17:00)을 반환한다 — "입력이 같다"의 반례가 이제 툴 결과로 드러난다. 회귀: 신규 `backend/tests/test_schedule_chat_date_scope.py` 16 passed, 기존 `test_schedule_chat_api.py` 21 passed, 백엔드 전체 **584 passed / 33 skipped / 2 xfailed**(8분 26초).
- **계측기**: `scripts/eval_chat.py`에 이 실패를 겨냥한 시나리오 `week_gap`(요일 반복 가능시간은 두 주차가 **완전히 동일**하고, 차이는 1주차 폐관 3일·2주차 공휴일 2일과 권지영 9/28 종일 불가 신고에만 있다)과 케이스 3건을 넣었다. 판정 축도 하나 늘렸다 — `forbid_text_any`(답변에 있으면 실패). 있어야 할 말을 재는 `expect_text_any`로는 **"확인하지 않은 것을 단정하는" 실패를 잡을 수 없어서**다. 문구는 좁게 잡았다(`"완전히 같습니다"` 등) — `"동일합니다"`처럼 넓히면 "요일 표는 동일합니다, 다만 폐관일이…"라는 **옳은** 답까지 실패한다.
- **모델 A/B**(`gemini-3.7-flash`, 케이스 3 × 3라운드 = 9회씩, 같은 케이스 파일을 `develop` 워크트리에 복사해 실행): **Before 0/9 → After 9/9**, 호출 실패 0건. 턴당 툴 호출 3.89 → 4.11회, 턴 소요 18.4 → 15.8초, 턴당 토큰 16,335 → 17,138.
  - Before의 실패 양상이 처음 보고된 문장 그대로였다. 유도 질문("두 주차 입력 데이터는 완전히 같은 거지?")에 3회 모두 **"학생들의 근무 가능 시간과 개관 조건 등 입력 데이터가 매주 같더라도"**라고 답했다 — 개관 조건은 실제로 다르고(1주차 3일 대 2주차 6일), 모델은 그걸 조회할 수단이 없었다. "왜 줄었나"에는 **"부서 전체의 근무 배정 자체가 1주차보다 적게 편성되어 있습니다"**라는 순환 설명(결과를 원인으로 되풀이)을 냈다.
  - After는 3회 모두 폐관일 3일·공휴일 단축 개관·9/28 종일 불가 신고를 근거로 댔다.
  - **거짓 통과 하나를 잡았다.** `week-difference-uses-date-exceptions`는 처음에 구조적 축(`expect_tools`)만 뒀는데, Before에서도 **3/3 통과**했다 — 예전 모델도 `get_student_availability`를 부르긴 했다(예외 데이터가 안 왔을 뿐). 툴 선택만 재고 답의 옳고 나쁨은 안 잰 것이다. 실제 답변 6건을 받아 후보 키워드를 세어 `"불가"`·`"예외"`(Before 0/3, After 3/3)를 축으로 추가했다. `"9월 28일"`은 **양쪽 3/3**이라 판별력이 없어 버렸다 — 날짜는 `find_schedules` 결과에도 나온다. 이 케이스의 Before 0/3은 받아 둔 답변에 새 축을 다시 적용해 계산한 값이다(추가 호출 없음).
  - **`forbid_text_any`는 이번 Before를 못 잡았다.** 금지 문구가 `"완전히 같습니다"` 계열인데 모델은 `"입력 데이터가 매주 같더라도"`로 바꿔 말했다. 잡은 것은 `expect_tools`(미호출)다. 문구 목록을 넓히면 옳은 답까지 걸리므로 그대로 두되, **이 축 하나만으로 단정 실패를 잡을 수 있다고 보면 안 된다**는 것이 이번에 확인된 한계다.
- **재지 않은 것**: 솔버는 건드리지 않았으므로 solver status·solve time 변화는 없다. A/B는 라운드 3회라 표본이 작다 — 앞선 회차에서 "2회 실패"를 결정적 실패로 적었다가 5회로 재니 60%로 흔들리는 쪽이 실상이었던 전례가 있다. 다만 Before 9회가 **한 건도** 캘린더를 언급하지 못한 것은 흔들림이 아니라 조회 수단 자체가 없어서다. 계측기는 다시 포화(9/9)라, 다음 챗봇 변경을 재려면 케이스를 더 어렵게 만들어야 한다.


## 2026-08-31 — 근무 추가·이동이 개관 시간을 전혀 보지 않아 휴관일에도 배정이 들어갔다 (#216)

- **문제/가설**: 솔버는 개관 슬롯 안에서만 배정하지만, **그 뒤에 사람이 얹는 근무**(챗봇 `add_schedule`/`move_schedule`, 화면 draft 편집, 수동 등록)는 `_validate_slot_and_limits`를 거치는데 그 함수가 보는 것이 ①시각 순서 ②겹침 ③주간 상한 셋뿐이었다. 개관 여부 판정(`is_open`)은 `verify.py`에만 있고 사후 검증 전용이다. 가설: 휴관일·개관 시간 밖 배정이 아무 저항 없이 들어가고, `verify_schedule`을 따로 돌리지 않으면 위반인 채로 확정된다.
- **테스트 조건**: 정보서비스팀 기본 정책 + 2026 학사 캘린더 기준. 09/07(월) 08:00~22:00 개관 / 09/12(토) 09:00~17:00 / 09/13(일) 요일 규칙상 폐관 / 09/25(금) 추석 폐관. `POST /api/schedule/draft/edits`(add·move)와 `POST /api/schedule/manual`로 휴관일·개관 밖·부분 초과 배정을 시도. in-memory sqlite + TestClient, `backend/.venv/bin/python3`. 솔버는 관여하지 않는다(검증 함수 단위).
- **Before**: 새 테스트 10건 중 **8건 실패**. 일요일(폐관) 추가 200, 추석 폐관일 추가 200, 토요일 18:00~20:00(개관 밖) 추가 200, 토요일 16:00~18:00(뒤 1시간 초과) 추가 200, 일요일로 이동 200, 수동 등록도 일요일 201 — 전부 거부되지 않고 그대로 저장됐다.
- **수정 내용**:
  - `app/opening_hours.py` 신설 — `check_within_opening_hours()`. 판정은 솔버·검증기가 쓰는 `OpeningHoursResolver.resolve(day)`를 그대로 재사용한다. 추가 경로가 자기 기준을 두면 "추가는 되는데 검증기는 위반이라는" 상태가 되므로 기준을 하나로 묶었다. 상한 검증(`app/work_hours.py`)과 같은 이유로 라우터가 아니라 별도 모듈에 둔다 — 근무를 얹는 경로가 여럿이다.
  - 구간이 여러 개인 날(점심 휴관 등)에는 **한 구간 안에 통째로** 들어가야 한다 — 닫힌 사이를 가로지르는 근무는 허용하지 않는다.
  - 거부 문구에 이유를 담았다. 휴관일이면 "…은(는) 부서 휴관일이라 근무를 배정할 수 없습니다", 개관 밖이면 그날의 개관 시간과 요청 시간을 함께 보여준다 — 담당자가 할 일이 다르다(날짜를 옮기거나, 시간을 당기거나).
  - `_validate_slot_and_limits`(draft add·move)와 `create_manual_schedule`에 연결. 되돌리기(역연산)는 예외로 뒀다 — 직전 상태 복원이라 새 위반이 아니고, 검사하면 이 수정 전에 휴관일에 들어간 배정을 지울 수는 있는데 되돌릴 수 없게 된다(#137에서 관측된 상한 검사와 같은 함정). 그래서 인자 이름을 `skip_hour_limits` → `skip_policy_checks`로 바꿨다.
- **After**: 동일 조건에서 새 테스트 **10건 전부 통과**. 휴관일·개관 밖·부분 초과·이동·수동 등록 모두 400으로 거부되고, 개관 시간 안(토 10:00~12:00)은 종전대로 통과한다. 전체 백엔드 스위트 **585 passed, 2 xfailed** (248s).
- **기존 테스트 1건이 전제 때문에 깨져서 함께 고쳤다**: `test_move_beyond_weekly_limit_is_400`이 상한 초과를 유도하려고 07:00~22:00으로 늘리는데, 월요일 개관이 08:00부터라 개관 검사에 먼저 걸렸다(400은 그대로지만 문구가 "초과"가 아니게 됨). 검사 순서는 유지했다 — 개관 여부는 슬롯 자체의 속성이라 겹침 검사와 같은 층이고, 학생의 누적 시간에 의존하는 상한보다 먼저 보는 것이 맞다. 대신 테스트를 개관 시간 안(08:00~11:00)으로 옮기고, 월요일 개관이 14시간뿐이라 한 행으로는 14h 상한을 넘길 수 없어 같은 주 수요일에 12h를 미리 깔아 주 15h를 만들었다.
- **확정(confirm)은 이번 범위에서 제외**: `expand_weekly_pattern`이 복제 날짜를 개관 구간과 교집합해 이미 폐관일을 걸러낸다(대표 기간은 솔버 결과라 신뢰). 확정 시점에 개관 재검증을 넣으면 이 수정 이전에 만들어진 휴관일 배정이 든 draft가 확정도 안 되고 되돌리기도 막혀 갇힌다. 그런 배정은 `verify_schedule`이 HC-OPEN으로 보고한다.


## 2026-08-31 — 안 지워진 낡은 draft가 주간 상한 합계에 같이 세어져 근무 추가가 오탐 거부됐다 (#212)

- **문제/가설**: `weekly_assigned_hours()`가 `student_id` + 그 주(월~일) 날짜 범위로만 필터링해, 같은 부서에 기간이 겹치는 draft가 둘 남아 있으면 **양쪽 시간을 다 더한다**. 겹치는 draft 둘은 같은 기간에 대한 서로 다른 계획안이지 더해질 근무가 아니다. 가설: 이 이중 집계 때문에 실제로는 여유가 있는 학생의 근무 추가가 "법정 주간 상한 초과"로 거부된다. 이 함수는 챗봇 draft 편집뿐 아니라 수동 등록·대타 승인이 공유하므로 영향이 그 경로 전체에 걸린다.
- **테스트 조건**: 이슈의 재현 조건 그대로 — 부서 6(정보서비스팀-test), 학생 20261005(김찬우, 교비 → 법정 주간 상한 14h), 기간이 겹치는 draft 두 개(batch 4: 09/07~09/20, 편집 중 / batch 6: 08/31~09/13, 시드가 남긴 안 쓰는 draft). 09/07 주 배정은 batch 4에 5.5h, batch 6에 11h. 여기에 9/13 1시간 추가를 시도한다. in-memory sqlite, `backend/.venv/bin/python3` (재현 스크립트 + `tests/test_work_hours_stale_draft.py`). Solver는 이 경로에 관여하지 않는다(상한 검증 함수 단위 검증).
- **Before**: `weekly_assigned_hours` = **16.5h** (5.5 + 11). 상한 14h → 1시간 추가 시 합계 17.5h로 **400 거부**. 실제 여유는 8.5h.
- **수정 내용**:
  - `app/work_hours.py` — `weekly_assigned_hours()`에 `draft_batch_id` 인자와 부서별 draft 정리 규칙 추가. **부서 단위로 좁히지 않고** 학생 전체를 계속 합산한다(법정 상한은 학생 한 명의 총 근무시간에 걸려서, 부서로 좁히면 여러 부서에서 일하는 학생을 과소집계한다). 대신 한 부서에서 기간이 겹치는 draft는 하나만 센다 — 승자는 지금 편집·확정 중인 draft, 없으면 나중에 만들어진 쪽. 대상 draft가 `exclude_batch_ids`에 들어 있어도(확정 경로) 그 자리를 낡은 draft가 물려받지 않는다.
  - `app/routers/schedule.py` — draft 편집(`apply_draft_edit` → `_validate_slot_and_limits`)이 `draft_batch_id`를 넘기고, 확정은 기간이 겹치는 이 부서 draft를 모두 검증에서 뺀다.
  - 같은 파일 — 낡은 draft가 남는 원인도 함께 막았다. `_replace_draft_batch()`가 **기간이 정확히 같은** draft만 지워서, 기간을 옮겨 재생성하면 이전 draft가 그대로 남았다(batch 6이 그 사례). 기간이 겹치는 draft를 지우도록 바꾸고, 확정 때도 승격되지 않은 겹치는 draft를 없앤다 — 검증에서만 빼고 DB에 남겨두면 확정 이후의 수동 등록·대타 승인이 확정본 + 낡은 draft로 같은 이중 집계를 다시 겪는다.
- **After**: 동일 조건에서 `weekly_assigned_hours` = **5.5h**. 1시간 추가 시 합계 6.5h ≤ 14h → **허용**. 재생성 후 부서 6에 남는 draft는 1개(이전 2개). 신규 회귀 테스트 7건 통과(`tests/test_work_hours_stale_draft.py`), 기존 백엔드 스위트 **568 passed, 2 xfailed** (246s)로 회귀 없음.

## 2026-08-31 — 칸 하나만 빼도 연속 근무 전체가 지워졌다: 화면이 남는 구간을 되넣게 (#214)

- **문제/가설**: 근무표 편성 화면에서 칸 하나만 클릭해 사람을 빼면 그 칸만 사라져야 하는데 연속 근무 전체가 지워진다(담당 QA 보고). 원인 가설은 **편집 단위가 어긋나 있다**는 것 — 화면은 부서 블록 단위로 클릭받지만 DB `work_schedule` 행은 `merge_blocks`(`scheduler/reporting.py`)가 연속 슬롯을 **한 행으로 합쳐** 저장한다. picker가 `assigned`를 겹침 조건으로 고르므로(`AdminSchedulePage.jsx`) 18:00~19:00을 눌러도 13:00~20:00 행이 잡히고, `toggleStudent`가 그 행의 `schedule_id`를 통째로 `remove` 큐에 넣는다.
- **테스트 조건**: 개발 DB의 실제 draft(batch 23, 부서 2, 55행 — 이 중 **2시간 초과 병합 행이 51개**)에서 `schedule_id=1298`(20220912 / 2026-08-31 월 / **13:00~20:00 연속 7시간 한 행**)을 표본으로 삼았다. `AdminSchedulePage.jsx`의 `addEntry`·`toggleStudent` 본문과 `scheduleGrid.js`의 헬퍼를 **파일에서 그대로 떼어내 Node로 실행**하는 하네스를 만들어(손으로 옮긴 사본이 아님), 18:00~19:00 블록을 클릭해 체크 해제했을 때의 ① 서버로 갈 편집 큐 ② 화면에 남는 `effective` 배정을 `origin/develop`(Before)과 수정 후(After) 같은 입력으로 비교했다.
- **Before**(`origin/develop`): 편집 큐 **1건** — `{remove, schedule_id: 1298}`. 화면에 남는 배정 **0건**, 남은 근무 **0시간**. 1시간을 빼려 했는데 **7시간이 통째로** 사라진다(이슈 재현).
- **수정 내용**: `scheduleGrid.js`에 순수 함수 `subtractSpan(span, cut)` 추가 — 구간에서 클릭한 칸을 도려내고 남는 앞·뒤를 돌려준다(겹치지 않으면 원래 구간 그대로). `toggleStudent`는 빼기에서 `remove`(행 전체) + 남는 구간을 `add`로 되넣도록 바꿨고, add 항목 생성은 `addEntry`로 묶어 두 경로가 같은 모양을 쓴다. 서버 편집에는 '행 일부 삭제'가 없어 **백엔드 변경 없이 화면에서 보정**한다. `/api/schedule/draft/edits`는 한 요청을 순서대로 적용하므로 `remove`가 먼저 반영되어 되넣기가 겹침 검사에 걸리지 않는다.
- **After**(동일 입력): 편집 큐 **3건** — `{remove, 1298}` + `{add, 13:00~18:00}` + `{add, 19:00~20:00}`. 화면에 남는 배정 **2건**, 남은 근무 **6시간**(= 7시간 − 뺀 칸 1시간). `subtractSpan` 경계 9케이스(맨 앞 칸·맨 뒤 칸·행 전체·블록이 행보다 큼·한쪽만 넘침·30분 칸·안 겹침·`HH:MM:SS` 형식) 모두 기대값 일치. `npm run build` 통과(1657 modules).
- **서버 계약 확인**: `test_partial_remove_is_remove_plus_readd_of_leftovers` 추가 — 09:00~12:00 한 행에 remove + add(09:00~10:00) + add(11:00~12:00)를 한 요청으로 보내면 두 행만 남는다. `test_schedule_draft_edits_api.py` **25 passed**, 백엔드 전체 **602 passed / 2 xfailed**.
- **남은 확인**: 브라우저에서 직접 클릭해 보는 확인은 못 했다 — 이 작업은 격리된 worktree라 그 코드로 dev 서버를 띄울 수 없었다. 위 수치는 모두 **실제 출고 코드를 실행한 결과**지만 화면 조작을 거친 값은 아니다.

## 2026-08-30 — 학번·이름이 그대로 외부 LLM으로 나가고 있었다: 전송 직전 비식별화 (#200)

- **문제/가설**: AI 검토·챗봇·대타 검사·특이사항 제안 네 경로 모두 Gemini로 프롬프트를 보내는데, 그 안에 **학번·이름이 원문 그대로** 들어간다. 특이사항·대타 사유는 학생이 자유 서술한 문장이라 연락처·이메일까지 섞여 나간다. 실데이터로 운영하면 개인정보를 제3자에게 넘기는 것이 된다. 가설: 프롬프트를 완성한 뒤 한 번에 치환하면 판단에 필요한 내용은 남기면서 식별자만 뺄 수 있다.
- **테스트 조건**: 시드와 같은 모양의 부서 1개(학생 5명: 안희진·조수현·김찬우·박서연·이도현, 학번 20261001~05), 부서 규칙 2줄(한 줄은 학생 이름을 직접 지목), 특이사항 2건(하나는 다른 학생 이름 + `010-2345-6789` + `joo@sogang.ac.kr` 포함), draft 배정 3건. 같은 DB로 `review_batch` 1회와 `chat.run_turn("조수현 학생 월요일 근무 빼줘")` 1턴(툴 `find_schedules` 1회 포함)을 돌려, 실제로 전송되는 문자열에서 학번 5개·이름 5개·전화번호·이메일 총 12개 문자열의 출현 횟수를 셌다. LLM 호출은 mock(`_call_gemini`/`_llm_step` 교체)이라 모델 응답 품질은 이 측정에 포함되지 않는다.
- **Before**(`LLM_DEIDENTIFY=0`): review 프롬프트 **식별자 23건**/1,799자, chat 1턴 전송 **식별자 30건**/1,927자.
- **수정 내용**: `backend/app/scheduler/deidentify.py` 추가 — 요청 단위 별칭 맵(학번·이름 → `S01`), 연락처·이메일·주민번호는 별칭 없이 삭제, 응답은 `id`/`name`/`name_id` 세 형태로 복원. 네 모듈은 **프롬프트를 조립한 다음 통째로** 마스킹한다(섹션마다 치환하면 새 섹션이 늘 때 빠뜨린다). 챗봇만 양방향이라 저장된 이력·직원 발화·툴 결과를 나갈 때마다 가리고, 모델이 부른 툴 인자(`student_id`는 학번으로, `student_name`은 이름으로)와 최종 응답을 되돌린다 — 툴 실행과 되돌리기 기록(`tool_calls`)은 계속 실제 값을 본다. 되묻기 `target_id`는 `ClarificationAnswer`의 키라 학번만 넣는다. 대타 검사는 캐시 저장 **전에** 복원한다(별칭을 캐시에 넣으면 다음 요청에서 복원할 매핑이 없다).
- **After**(`LLM_DEIDENTIFY=1`, 기본값): review 프롬프트 **식별자 0건**/1,695자, chat 1턴 전송 **식별자 0건**/1,835자. 특이사항 문장("같은 시간은 피하고 싶어요")과 부서 규칙 문구는 그대로 남아 판단 근거는 줄지 않았다. 백엔드 전체 테스트 568 passed / 30 skipped / 2 xfailed (기존 프롬프트 문자열을 단정하던 `test_note_is_included_in_prompt` 1건은 별칭 기준으로 수정).
- **되돌아본 것**: 마스킹한 프롬프트를 눈으로 훑어 두 가지를 잡았다. (1) 학번과 이름을 나란히 적은 자리(`20261004 박서연`, `박서연(20261004)`)는 둘 다 같은 별칭이 돼 `S04 S04`로 겹쳤다 — 마스킹 뒤 한 번 접었고 review 프롬프트가 1,711자 → 1,695자로 줄었다(줄바꿈은 접지 않는다: 같은 학생의 배정 줄을 합치면 근무 건수가 사라진다). (2) **조사가 어긋날 수 있다** — 조사는 별칭을 읽은 발음에 맞춰 나온다. `S02`는 "에스공이"로 끝나 받침이 없어 `S02는`이, `S03`은 "삼"으로 끝나 받침이 있어 `S03이랑`이 나온다. 이름을 도로 넣으면 그 이름의 받침과 어긋난다(`조수현는`, `김찬우이랑`). **이건 실제 모델 출력을 관측한 게 아니라 별칭 형식에서 추론한 위험이다** — 실측 전에 막아 두는 쪽을 택했다.
- **조사 처리**: 이름 뒤에 "학생"을 붙여 받침을 고정하는 안을 검토했지만, 별칭 끝자리가 2·4·5·9(이·사·오·구)면 모델이 받침 없는 조사를 골라 `조수현 학생는`이 되어 절반만 고쳐진다. 대신 **복원한 이름의 받침을 보고 조사를 다시 고른다** — `(으로/로), (이랑/랑), (은/는), (이/가), (을/를), (과/와)` 6쌍이고 `으로/로`만 ㄹ 받침 예외를 둔다. 조사 뒤에 한글이 이어지면(`S03이라고`의 `이라`) 낱말의 일부라 손대지 않는다. 학번으로 복원하는 자리(`target_id`)는 발음을 알 수 없어 그대로 둔다. 단위 테스트 10건으로 고정.
- **하네스 정합**: `scripts/eval_review.py`가 `review_batch`를 건너뛰고 `_build_prompt` + `call_model`을 직접 불러 **마스킹을 우회하고 있었다** — 그대로 두면 서비스는 별칭 프롬프트를, 하네스는 실명 프롬프트를 재게 된다. `run_case`에 프로덕션과 같은 자리(마스킹 → 호출 → 복원)를 넣었다. API 호출 없이 확인: `tenure-relative-comparison` 케이스 프롬프트 1,097자에 남은 식별자 0건, 별칭 `S01`·`S02`, 응답은 `오규원(20211357)`로 복원. 판정(`check_result`)이 기대 키워드로 findings를 훑으므로 복원은 판정 **전에** 한다. 챗봇 하네스(`eval_chat.py`)는 `run_turn`을 그대로 부르므로 자동으로 같은 경로를 탄다.
- **검출력 A/B**(`gemini-3.7-flash`, 26케이스 × 2라운드 × 2조건 = 104회): 실명 프롬프트 **52/52 (100%)**, 별칭 프롬프트 **51/52 (98%)**. 양쪽 다 호출 실패 0건.
  - 갈린 1건은 `closing-gap`(마감 시간대 공백). **비식별화 탓이 아니라 흔들리는 케이스다** — 그 케이스만 조건당 6회씩 다시 재니 **실명 5/6, 별칭 6/6으로 뒤집혔다**(두 실행 합산 7/8 대 7/8로 동일). 기대 키워드가 `'21:00'`·`'마감'`이라 시간·개관 시간 축이고, 학번·이름 치환이 닿는 자리가 아니다.
  - **부수 효과 — 더 싸고 빨라졌다.** 별칭이 "학번 + 이름"보다 짧아 호출당 토큰 **4,813 → 4,653(-3.3%)**, 평균 응답 **5.0초 → 4.5초**(최대 11.3초 → 9.4초).
  - 결론: 26케이스 범위에서 **별칭 치환으로 검출력이 떨어진 증거는 없다.**


## 2026-08-30 — 챗봇 계측기 신설: 예상과 달리 결함은 챗봇이 아니라 테스트와 하네스에 있었다 (#195)

- **문제/가설**: 챗봇은 검토 AI와 달리 **draft를 직접 고치는데** 계측기가 없었다(live 5건이 전부였고 그마저 fixture가 깨져 있었다). 가설은 "재보면 바로 개선 대상이 나온다"였다.
- **테스트 조건**: `scripts/eval_chat.py` 신설. 15케이스를 3층(basic 읽기·판단 / edit 쓰기 / guard 하면 안 되는 것)으로 나누고 `gemini-3.7-flash`로 케이스당 2~3회 반복. **케이스마다 in-memory sqlite를 새로 만든다** — 챗봇은 쓰기 툴이 있어 개발 DB를 쓰면 실제 draft가 바뀐다. `adjust_weight` 케이스만 재solve를 가짜로 대체했다(모델의 카테고리·방향 판단만 재는 케이스라 30초 solve가 불필요).
- **Before**: 계측 없음. `STEP_BUDGET=5`는 설계문서가 "실측 후 조정"이라 적어두고 재지 않은 값이었고, 챗봇이 요청하지 않은 수정을 하는지·hard 제약을 깨는지도 수치가 없었다.
- **수정 내용**: `eval_chat.py` + `eval_chat_cases.json`. 판정은 툴 목록·선행 관계·쓰기 유무·`new_violations`·**턴 종료 후 draft 상태**(`expect_final_schedules`)·`turn_status`. `chat.LlmStep`에 `usage` 필드를 더해 턴당 토큰을 잰다(`eval_review.LAST_USAGE`와 같은 선례). 전 케이스 통과 시 **"계측기 포화" 경고를 출력**한다 — 검토 하네스에서 세 번 겪은 함정을 계측기가 직접 알리게 했다.
- **After**: 15케이스 × 2라운드 **30/30 (100%)**, 호출 실패 0건. basic 8/8 · edit 12/12 · guard 10/10.
  - **턴당 툴 호출 1.9회, 예산 소진 0턴** — `STEP_BUDGET=5`가 충분하다는 첫 실측이다.
  - 턴당 토큰 평균 **9,324**(최대 16,279) — 검토(4,822)의 약 2배. 턴 소요 평균 9.5초(최대 24.4초).
  - 쓰기 14건 중 제약 위반 4건은 전부 **의도적으로 위반을 유발하는 두 케이스**에서 나왔고, 모델이 매번 사용자에게 알렸다. 의도치 않은 위반 0건.
  - **가설은 틀렸다** — 챗봇 자체에서는 개선 대상이 나오지 않았다. 대신 결함은 다른 두 곳에서 나왔다.
- **발견 1 — 하네스 자신의 거짓 통과**: `overstaffing-edit-is-reported`가 통과했는데 실제로는 위반이 생기지 않았다. 기본 정책의 `max_per_slot`이 2라 두 명을 같은 칸에 넣어도 위반이 아니었고, 모델은 "규정 위반은 **없습니다**"라고 답했는데 기대 키워드 `"위반"`이 그 **부정문에 매칭**됐다. `max_per_slot=1` 시나리오를 새로 만들고 판정을 구조적 축(`expect_new_violations`)으로 바꿔 고쳤다. 고친 뒤 모델은 "동시 배정 인원이 최대 인원(1명)을 초과하게 됩니다"라고 정확히 보고한다.
- **발견 2 — live 테스트가 구현을 단정하고 있었다**: `test_multi_step_edit_completes_in_one_turn`이 `remove_schedule` + `add_schedule`을 요구하는데, 실측 **5회 전부** 모델은 `move_schedule` 한 번으로 같은 결과를 냈다. 결과는 매번 정확했으므로 실패는 모델이 아니라 단언 탓이다. 테스트를 **결과 기준**(턴 종료 후 draft 상태 확인)으로 바꿨다. live 챗봇 테스트 **7건 전원 통과**(직전 6/7).
- **남은 것**: 30/30이라 챗봇 계측기도 이미 포화다. 다음 챗봇 변경을 재려면 케이스를 더 어렵게 만들어야 한다 — 여러 턴에 걸친 요청, `confirmation_required`(수동 편집 + 재생성 충돌), 예산을 실제로 넘기는 대량 요청이 후보다. 회귀: 백엔드 스위트 **531 passed / 2 xfailed**.


## 2026-08-30 — 검토 프롬프트 두 결손 수정: 규칙 상충 인지 · 특이사항 되묻기 경로 (#195)

- **문제/가설**: 확장된 케이스 세트에서 두 건이 계속 실패했다. ① `rule-conflict` — 양립 불가한 규칙 쌍에서 한쪽만 위반으로 잡고, **다른 규칙을 위반하는 해결책**("1명 추가 배정 검토")을 suggestion으로 냈다. ② `note-ambiguous-clarify` — 모호한 학생 사정에 되묻지 않았다. 앞 기록에서 ②를 "지시와 저장 스키마가 어긋난 탓"으로 적었으나, 확인해 보니 `field_name`은 자유 문자열이라 스키마 문제가 아니었다. 가설: 둘 다 프롬프트가 그 경로를 안내하지 않아서다.
- **테스트 조건**: `scripts/eval_review.py`, `gemini-3.7-flash`, 26케이스. 두 케이스는 **5회씩** 따로 재고(비결정성 때문), 전체 회귀는 26 × 2라운드. 변경점은 `review_system_prompt.md`뿐이며 케이스·스키마·모델은 그대로다.
- **Before**: `rule-conflict` **3/5**, `note-ambiguous-clarify` **0/5**. (앞 기록에서 `rule-conflict`를 "2라운드 모두 실패한 결정적 실패"라고 적었는데 **표본 2회 탓이었다** — 5회로 재니 60%로 흔들리는 쪽이 실상이다.) 전체 26케이스 × 2라운드는 49/52.
- **수정 내용**: 프롬프트 세 곳.
  1. 검토 절차에 "규칙이 여러 줄이면 각각 대조하기 전에 **규칙끼리 동시 만족 가능한지** 먼저 확인하고, 상충이면 한쪽만 지적하지 말고 상충 자체를 밝힌다. suggestion에 **다른 규칙을 위반하게 되는 조치를 제안하지 않는다**"를 추가.
  2. `field_name` 설명에 "DB 컬럼명일 필요가 없고 직접 지어 적어도 된다"를 명시. 기존 설명이 `tenure_start_date`·`biweekly_max_hours` 예시뿐이라 자유 텍스트 사정 쪽으로 유도가 되지 않았다.
  3. 되물어야 하는 예에 4번 추가 — 학생 특이사항이 모호하지만 **그 학생에게 하나만 물으면 정해지는** 경우. 사정을 적은 본인에게 물으면 답이 나오므로 "되물으면 안 되는 예"의 정책 판단과 다르다는 구분을 함께 적었다.
- **After**: `rule-conflict` **5/5**, `note-ambiguous-clarify` **5/5**. 전체 26케이스 × 2라운드 **52/52 (100%)**, 호출 실패 0건. 평균 응답 5.3s / 최대 10.1s. 호출당 토큰 4,402 → **4,822**(프롬프트가 18줄 늘어 약 9.5% 증가).
  실제 출력 — 상충 케이스: `suggestion: "인원을 추가하거나 줄이는 등 배정을 바꾸더라도 다른 규칙을 위반하게 되므로, 상충하는 두 부서 운영 규칙 자체를 조정 및 재정립하는 것을 검토해 주시기 바랍니다."` 모호 케이스: `field_name: "월요일 근무 시작 가능 시각"`으로 student 되묻기가 나왔다 — #111 UI가 그대로 렌더링·저장할 수 있는 형태다.
  부작용 없음: 되묻기 오발동 금지(`no-clarify-gender-policy`) 2/2, 오탐 검사 3종 2/2씩 유지. 백엔드 스위트 **531 passed / 2 xfailed**.
- **남은 것**: **계측기가 다시 포화됐다(52/52)**. 이 상태로는 다음 프롬프트 변경의 효과를 잴 수 없다 — 8/27과 8/30에 이어 세 번째다. 다음 프롬프트 작업 전에 케이스를 또 늘려야 한다.


## 2026-08-30 — 되묻기 UI: 백엔드만 있고 화면이 없어 한 번도 돌지 않던 고리를 닫음 (#111·#195)

- **문제/가설**: AI 검토는 `clarification_requests`를 내고, 저장 API(`POST /api/schedule/review/clarifications`)와 다음 검토에 재활용하는 조회(`_get_relevant_clarification_answers`)까지 다 있는데 **화면이 그리지 않았다**(`frontend/src`에 `clarification` 문자열 없음). 담당자가 답할 경로가 없으니 `clarification_answer` 테이블은 계속 비고, 프롬프트의 "## 확인된 정보"는 운영에서 늘 "(없음)"이며 AI는 같은 질문을 매번 다시 한다. 설계문서가 UI를 "별도 논의"로 미뤄둔 지점(`review_clarification_설계문서.md:138`)이다.
- **테스트 조건**: dev 서버(백엔드 8000 · 프론트 5173) + 시드 DB. 직원 STF001로 로그인 → 부서 근무표 생성(`OPTIMAL`, 8.42초, 배정 55건) → AI 검토 실행. 실제 검토는 되묻기를 내지 않고 "시험기간 정보가 없어 확인 불가"로 summary에만 남겨, 렌더링 검증은 `window.fetch`를 가로채 되묻기 3건(student·department·rule_interpretation)을 응답에 주입해 확인했다. 저장(POST)은 가로채지 않아 실제 백엔드로 나갔다.
- **Before**: 되묻기 0건 표시. `clarification_answer` 테이블 **0행**. 재검토 시 프롬프트의 "## 확인된 정보" = "(없음)".
- **수정 내용**: `components/admin/ClarificationRequests.jsx` 신설 — 대상 유형 배지(학생 정보/부서 정책/규칙 해석), 질문·필요한 이유, 답변 입력과 저장. `api/client.js`에 `answerClarification` 추가하고 `ReviewStage`의 findings 아래에 붙였다. 되묻기가 없으면 아무것도 그리지 않는다.
- **After**: 3건 모두 정상 렌더링됐고 2건을 실제로 저장해 `clarification_answer`에 행이 생겼다(`1 student 20211357 tenure_start_date`, `2 department 2 biweekly_max_hours`). 같은 배치를 다시 검토했을 때 프롬프트가 이렇게 바뀐다 —
  ```
  ## 확인된 정보
  - 20211357의 tenure_start_date: 2024-03-04 (2024학년도 1학기부터 근무)
  - 부서의 biweekly_max_hours: 190시간입니다
  ```
  브라우저 콘솔 오류 0건, `vite build` 통과. 검증용으로 만든 2행은 삭제해 dev DB를 원복했다(남은 행 0).
- **정정**: 앞 항목에서 "되묻기 지시와 저장 스키마가 어긋난다"고 적었는데 **과했다**. `field_name`은 enum이 아니라 자유 문자열(`Column(String)`)이고 API도 비어 있지만 않으면 받는다 — 자유 텍스트 사정도 `field_name`에 이름을 붙여 되물을 수 있고, 이 화면도 그 값을 그대로 보여준다. 스키마 변경은 필요 없고, 남은 것은 프롬프트가 그 경로를 안내하지 않는다는 점뿐이다(프롬프트 개선 과제로 이관).


## 2026-08-30 — 학생 특이사항을 검토 계측에 넣자 "severity 1단계"가 계측기 탓으로 드러남 (#195)

- **문제/가설**: 앞 항목에서 findings 34건이 전부 critical이라 "severity가 사실상 1단계"라고 적었다. 그런데 그때 케이스 세트에는 **warning을 기대하는 케이스가 하나도 없었다**. #185(학생 특이사항)가 develop에 들어왔으니, "특이사항에 어긋나면 severity를 한 단계 낮춰라"는 프롬프트 규칙을 케이스로 만들어 프롬프트 문제인지 계측기 문제인지 가르려 함.
- **테스트 조건**: `scripts/eval_review.py --repeat 2`, `gemini-3.7-flash`, 딜레이 없음. 23 → 26케이스. 하네스에 `Case.student_notes`를 넣어 `_build_prompt`의 "## 학생이 낸 특이사항" 섹션까지 계측 범위에 넣고, live 테스트도 `StudentNote` 행을 실제로 시딩하게 했다. 26케이스 전부 eval 경로와 DB 경로의 프롬프트가 일치함을 임시 대조 테스트로 확인 후 삭제.
- **Before**: 23케이스 × 2라운드 44/46(96%). findings 34건 **전부 critical, warning·info 0건**.
- **수정 내용**: 특이사항 케이스 3개 추가 — ① `note-vs-rule-conflict`(학생이 일요일을 원해도 부서 규칙 위반은 critical) ② `note-soft-violation`(부서 규칙은 안 깨고 특이사항만 어긋남 → warning이어야 하고 critical이면 실패) ③ `note-ambiguous-clarify`(모호한 사정은 단정 말고 되묻기).
- **After**: 26케이스 × 2라운드 **49/52 (94%)**, 호출 실패 0건. 평균 6.7s / 중앙값 4.6s / p90 12.1s / **최대 54.6s**, 호출당 4,402토큰.
  **severity는 프롬프트 문제가 아니었다** — findings 39건 중 **warning 4건**이 나왔고 `note-soft-violation`이 2/2로 통과했다. 앞 항목의 "전부 critical"은 warning을 요구하는 입력이 세트에 없어서였다. 실제 출력: `severity: warning` / "박정민 학생이 통학 막차로 20시 이후 근무가 어렵다는 특이사항을 제출했으나 18:00-22:00 근무에 배정되었습니다." `note-vs-rule-conflict`도 2/2 — 학생이 원해도 부서 규칙 위반은 critical로 올바르게 갈랐다.
  실패 2건: `rule-conflict` **1/2**(직전 라운드 0/2 → 비결정적으로 흔들린다, 개선된 것이 아니다), `note-ambiguous-clarify` **0/2**.
  회귀: 백엔드 스위트 **531 passed / 2 xfailed**(실 LLM 2파일 제외).
- **남은 것 — 프롬프트와 스키마가 어긋난 지점**: `note-ambiguous-clarify`에서 모델은 되묻는 대신 warning finding + "정시 출근 가능 여부를 확인하거나"라는 suggestion을 냈다. 프롬프트는 한편으로 "문장이 모호하면 되묻기로 돌려라"라고 하고, 다른 한편으로 되묻기를 "특정 대상의 명확한 사실 하나"로 좁힌다. 게다가 student 되묻기는 API(`POST /schedule/review/clarifications`)가 `field_name`을 필수로 요구하는데 자유 텍스트 사정에는 붙일 필드가 없다 — **지시와 저장 스키마가 맞지 않는다**. 케이스를 통과시키려 기대치를 낮추지 않고 실패로 남겼다. 프롬프트에서 그 문장을 걷어낼지, 되묻기 스키마를 자유 텍스트까지 넓힐지는 사람이 정할 문제다.


## 2026-08-30 — 챗봇 편집이 hard 제약을 안 보고 있었다: verify 툴 + 편집 후 자동 검증 (#195)

- **문제/가설**: 챗봇의 쓰기 툴(`move`/`remove`/`add_schedule`)은 `apply_draft_edit`를 거치는데, 거기서 검사하는 건 **겹침과 주간 상한(HC-TIME-1/2)뿐**이다. 가능 시간(HC-CLASS-1)·개관 시간(HC-OPEN)·슬롯 인원(HC-STAFF-1/2)·월 상한(HC-TIME-3)은 통과한다. 한편 같은 제약을 결정적으로 채점하는 `verify.py`가 이미 있는데 `chat.py`에는 `verify`라는 문자열조차 없었다. 가설: 대화로 "저녁 8시로 옮겨줘"라고 하면 학생이 못 오는 시간에 배정돼도 아무도 막지 않고 아무도 알려주지 않는다.
- **테스트 조건**: 시드 DB의 실제 draft 배치(부서 2, 2026-08-31~09-13, 55행)로 `verify_batch` 비용 측정. 동작 검증은 가능 시간이 월~금 09:00-18:00으로 채워진 세션에서 월요일 09:00-12:00 근무를 20:00-22:00으로 옮기는 시나리오 — mock 6건(`test_schedule_chat_verify_tool.py`)과 실 Gemini 2건(`test_chat_live.py`, `gemini-3.7-flash`).
- **Before**: 그 편집이 **그대로 적용되고 결과는 `{"ok": true, ...}`뿐**이었다. 툴 목록 7개에 검증 수단 없음. 게다가 `test_chat_live.py:60`이 `ChatSession(staff_id=...)`를 쓰는데 실제 컬럼은 `created_by`라 **live 테스트 5건 전부 fixture에서 TypeError로 에러** — 챗봇의 유일한 계측 수단이 언제부턴가 한 번도 돌지 않고 있었다.
- **수정 내용**:
  - `verify_schedule` 읽기 툴 추가 — `verify_batch`를 그대로 태우고, 결과를 critical 우선·최대 10건으로 줄여 학번 대신 이름을 붙여 돌려준다.
  - 쓰기 툴에 **편집 전후 위반 차집합**을 붙였다. 모델이 검증을 부를지에 기대지 않고, 편집이 새로 만든 critical 위반을 항상 결과에 얹는다. warning(인원 부족)은 제외 — 근무를 옮기면 거의 항상 자리가 바뀌어 매 편집마다 딸려 오고 진짜 위반이 묻힌다.
  - 시스템 프롬프트에 "`new_violations`가 있으면 요청대로 고쳤더라도 먼저 알리고 되돌릴지 물어라", "'규정 지키나'류 질문은 짐작하지 말고 `verify_schedule`을 써라"를 추가.
  - `test_chat_live.py`의 `staff_id` → `created_by` 수정.
- **After**: `verify_batch` **0.023~0.037s**(3회, 위 55행 배치) — 편집마다 두 번(전·후) 돌려도 부담 없다. 같은 편집이 이제 `new_violations: [{rule: "HC-CLASS-1", student: "조수현(20223333)", when: "2026-09-07 20:00-22:00"}]`을 함께 돌려주고, 실 Gemini가 그것을 답변으로 옮겼다 — *"다만 이번 변경으로 규정 위반이 새로 발생했습니다. … 조수현 학생이 제출한 근무 가능 시간 밖입니다. 이대로 유지하시겠습니까, 아니면 되돌리거나 다른 시간대를 찾아볼까요?"* "규정 지키나" 질문에도 `verify_schedule`을 호출했다.
  회귀: 백엔드 스위트 **494 passed / 2 xfailed**(신규 mock 6건 포함, 실 LLM 2파일 제외). live 챗봇 테스트는 **5건 전원 에러 → 7건 중 6건 통과**.
- **남은 것**: 유일한 live 실패 `test_multi_step_edit_completes_in_one_turn`은 이번 변경과 무관하다 — "월요일 빼고 화요일에 넣어줘"에 모델이 `remove`+`add` 대신 `move` 한 번으로 처리했는데, 테스트가 결과가 아니라 **툴 경로**를 단정한다. fixture가 깨져 있던 동안 드러나지 않던 문제이고, 비결정적이라 한 번의 관측으로 단정 말고 반복 측정 뒤 "결과 기준"으로 고칠지 판단해야 한다(#195의 챗봇 계측기 항목).


## 2026-08-30 — AI 검토 계측기 포화 해소: 18케이스 만점 → 확장 23케이스에서 규칙 상충 실패 노출 (#195)

- **문제/가설**: #177 이후 검토 AI를 다시 재보려 했는데 18케이스가 전부 통과했다. 계측기가 만점이면 프롬프트를 고쳐도 좋아졌는지 나빠졌는지 알 수 없다 — 2026-08-27에 9→14케이스로 늘린 이유("계측기 포화")가 그대로 재발했다는 가설.
- **테스트 조건**: `scripts/eval_review.py --repeat 2`, `gemini-3.7-flash`(프로덕션 기본값), 딜레이 없음. Before는 기존 18케이스, After는 확장 23케이스이며 **같은 실행 안에서 기존/신규를 나눠 집계**해 조건 차이를 없앴다. 원본은 `backend/output/eval_2026-08-30_*.json`(gitignore, 로컬 보관).
- **Before**: 18케이스 × 2라운드 = **36/36 (100%)**, 호출 실패 0건. 평균 6.5s / 중앙값 5.3s / p90 11.1s / 최대 19.8s, 호출당 4,394토큰. findings 22건이 **전부 critical**(warning·info 0건)이고 케이스별 findings·되묻기 개수가 1·2라운드 완전히 동일 — 비결정성조차 잡히지 않았다.
- **수정 내용**:
  - 케이스 18 → 23. 추가한 축은 규칙 간 상충 / 같은 규칙 위반 3건을 한 finding에 모으기 / 대규모 배치에 서로 다른 규칙 3건 동시 위반 / 이미 답변된 되묻기의 재질문 금지 / 배정 데이터만으로 판단 불가한 규칙 문구. 대규모 케이스는 `scale-clean`에 위반 3건을 심고 파이썬으로 **정확히 3건만 성립하는지 재검산**한 뒤 넣었다.
  - 판정 강화(`check_result`): 기대 `severity` 축, `all_of`(한 finding의 evidence에 여러 근거가 다 있어야 함), `max_findings`(finding 개수 상한) 추가. `evidence`·`suggestion` 검사를 `hits[0]` 하나가 아니라 **매칭된 finding 전부**로 확대. 기존 키워드 목록 형식은 그대로 받는다.
  - `Case`에 `clarification_answers` 추가하고 `run_case`가 `_build_prompt`에 넘기게 했다 — #111이 넣은 "## 확인된 정보"·"## 확인된 규칙 해석" 섹션이 그전까지 계측 밖이었다.
  - `test_review_live.py`의 DB 시딩 보강: 학생·합격 지원서·근속·가용시간·되묻기 답변을 실제 테이블에 넣는다. 그전엔 학생 행조차 만들지 않아 `tenure-relative-comparison`이 eval에서는 통과하고 live에서만 실패했다(#177 기록의 17/18). 임시 대조 테스트로 **23케이스 전부 eval 경로와 DB 경로의 프롬프트가 완전히 일치**함을 확인하고 그 테스트는 지웠다.
- **After**: 23케이스 × 2라운드 = **44/46 (96%)**, 호출 실패 0건. 평균 6.5s / 중앙값 5.4s / p90 13.0s / 최대 16.8s, 호출당 4,201토큰. 같은 실행 안에서 **기존 18케이스 36/36 · 신규 5케이스 8/10** — 포화는 케이스 세트 탓이었다.
  유일한 실패는 `rule-conflict`로 2라운드 모두 0/2(비결정 흔들림이 아니라 결정적 실패)다. "모든 시간대 2명 이상"과 "동시 1명 이하"가 동시에 성립할 수 없는데, 모델은 앞 규칙 위반만 critical로 잡고 **뒤 규칙을 위반하는 해결책("1명 추가 배정 검토")을 suggestion으로 냈다.** 규칙 간 상충을 보지 못하는 것이 현재 프롬프트의 확인된 한계다.
  findings 34건은 여전히 **전부 critical**(warning·info 0건) — severity가 사실상 1단계라는 #195 2번이 더 큰 케이스 세트에서도 재확인됐다.
  회귀: 백엔드 스위트 **488 passed / 2 xfailed** (실 LLM 호출 2파일 제외).
- **남은 것**: 학생 특이사항(#185) 케이스는 `origin/develop`에 `StudentNote`·`review.student_notes`가 아직 없어 이번 확장에서 뺐다 — #185 병합 후 `student_notes` 축과 "특이사항 위반은 warning으로 낮춘다" 검증 케이스를 함께 넣어야 한다.


## 2026-08-30 — AI 검토 계측기가 다시 포화: 18케이스 36회 전부 통과 (#195)

- **문제/가설**: #177 전환 이후 검토 AI를 다시 재보고 개선점을 찾으려 했다. 가설은 "케이스가 18개로 늘었으니 약점이 한둘 보일 것"이었는데, 실제로는 반대 결과 — 계측기가 만점이라 개선 여부를 판별할 수 없는 상태였다.
- **테스트 조건**: `scripts/eval_review.py --repeat 2`, 18케이스(`eval_review_cases.json`) 고정, `gemini-3.7-flash`(프로덕션 기본값), 프롬프트·스키마·케이스 무변경, 딜레이 없음. 원본은 `backend/output/eval_2026-08-30_gemini-3.7-flash_repeat2.json`(gitignore, 로컬 보관).
- **Before**(8/29 #177, 같은 케이스 세트 1라운드씩 2회): 18/18 · 18/18, 평균 응답 4.0s / 4.25s, 호출당 토큰 4,158 / 4,089. findings는 두 라운드 각 12건 **전부 critical**.
- **수정 내용**: 코드 변경 없음. 계측 결과와 코드 점검 결과를 이슈 #195로 등록했다 — ① 계측기 포화(변별력 상실) ② severity가 사실상 1단계 ③ 되묻기가 프론트에 렌더링되지 않아 `clarification_answer`가 영구히 비어 있음(`AdminSchedulePage.jsx:725`가 findings만 그리고, `frontend/src`에 `clarification` 문자열이 없다) ④ 검토 결과 미저장 ⑤ `eval_review.py:411`이 `_build_prompt`에 `student_notes`·`clarification_answers`를 안 넘겨 #185·#111이 추가한 프롬프트 섹션이 계측 밖 ⑥ 챗봇(`chat.py`)은 eval 하네스 자체가 없음.
- **After**: 36/36 (100%), AI 호출 실패 0건. 응답 평균 6.5s / 중앙값 5.3s / p90 11.1s / **최대 19.8s**(10초 초과 5회), 호출당 평균 4,394토큰(입력 3,520 / 출력 237). findings 22건 **전부 critical — warning·info 0건**(8/29 24건도 전부 critical). 케이스별 findings·되묻기 개수가 1·2라운드 완전히 동일해 비결정성조차 잡히지 않았다. 검출률은 8/29와 같고 응답시간만 늘었다 — **점수가 낮은 게 아니라 만점이라 못 쓰는 계측기**라는 것이 이번 측정의 결론이다.


## 2026-08-29 — 자연어 특이사항 → 슬롯 선호도 제안 (#185 B-AI)

- **문제/가설**: 학생이 적은 문장은 AI 검토가 읽지만(위 항목), **선호도로 옮기는 경로는 사람 손밖에 없다**. "금요일 저녁은 피하고 싶어요"를 학생이 다시 격자에서 슬롯 8개로 찍어야 한다. 문장을 슬롯 후보로 바꿔 보여주고 학생이 확인만 하면 되게 하되, **자동 반영은 하지 않는다**는 전제로 설계했다.
- **테스트 조건**: `gemini-3.7-flash` · 시드 DB 부서 2 오규원(20211357)의 실제 가능 시간(2026-2, 13구간 — 금 18:00~22:00 · 화 08:00~10:30 포함). 문장 4개짜리 노트 1건과 엣지 3케이스(가능 시간 밖 요청 · 모호한 문장 · 배치 방식 요청)를 실제 API로 호출하고 트랜잭션 rollback으로 원복했다.
- **Before**: 경로 자체가 없다(제안 0건, 학생이 손으로 찍는 수밖에). 첫 구현은 **호출조차 실패** — `response_schema`에 `Literal[1, 3]`(정수 enum)을 주면 Gemini SDK가 `Input should be a valid string`으로 요청을 거부한다.
- **수정 내용**:
  - `scheduler/note_suggest.py` + `note_suggest_system_prompt.md` + `POST /api/availability/me/note/suggest`. Gemini 클라이언트·모델·재시도는 `review.py` 것을 재사용하고 프롬프트·스키마만 분리했다(`substitute_check.py`와 같은 방식).
  - AI 응답 선호도를 `"avoid"`/`"prefer"` 라벨로 받고 서버에서 1/3으로 옮긴다 — 정수 enum 미지원 회피이자, 모델에게도 숫자보다 라벨이 덜 헷갈린다.
  - **서버 검증**: AI가 돌려준 슬롯을 학생이 실제로 낸 슬롯으로 교집합해 잘라내고, 같은 슬롯에 상충하는 제안이 오면 먼저 온 것만 남긴다. 프롬프트 지시만으로는 보장되지 않고, 목록 밖 슬롯이 화면에 뜨면 학생이 확인을 눌러도 저장에서 422로 튕긴다.
  - 저장은 하지 않는다. 반영은 학생이 확인한 뒤 `PUT /api/availability/me`(slot_preferences)로 직접.
- **After**: 4문장 노트 → **제안 2건 + 미구조화 2건**으로 기대와 정확히 일치.
  - `금요일 저녁은 통학 막차 때문에…` → 금 18:00~21:30 **8슬롯 / preference 1** (그 학생 금요일 저녁 가능 시간 전체와 일치)
  - `화요일 오전은 수업이 없어서…` → 화 08:00~10:00 **5슬롯 / preference 3** (가능 시간 08:00~10:30의 오전 슬롯 전부)
  - `맡겨주시면 성실히 하겠습니다` · `월요일은 3교시가 늦게 끝나서 15분쯤…` → 미구조화 (후자의 사유: "15분 단위는 30분 슬롯으로 표현할 수 없음")
  - 엣지 3케이스 모두 **제안 0건**, 지어낸 슬롯 0개. 일요일 요청은 "슬롯 목록에 일요일이 없어 제안할 수 없다"로, 배치 방식 요청은 "연속 근무 등 배치 방식"으로 정확히 분류했다.
- **관찰**: Gemini를 monkeypatch한 단위 테스트 11건이 **전부 통과하는 상태에서 실제 호출이 스키마 오류로 실패**했다. 응답 스키마를 SDK에 넘기는 구간은 mock이 덮어 버리는 자리라, AI 모듈은 실호출을 한 번은 거쳐야 한다.
- 백엔드 **514 passed** (live LLM 테스트 2파일 제외). 회귀 11건 추가(`test_note_suggest_api.py` — 가능 시간 밖 슬롯 절삭·상충 제안·조용한 실패 포함).


## 2026-08-29 — 학생 자연어 특이사항을 AI 검토·챗봇에 연결 (#185 B트랙)

- **문제/가설**: 가능 시간 격자는 "언제 되고 언제 안 되는지"만 담는다. "월요일은 3교시가 늦게 끝나 15분쯤 늦어요" 같은 사정은 갈 곳이 없어 학생이 그 시간을 통째로 빼거나(가용 시간 손해) 아무 말 없이 넘어가는 수밖에 없었다. 자연어 규칙은 **부서 단위**(`department_policy.custom_rules`)만 있고 학생 개인용 필드는 DB·API·화면 어디에도 없었다.
- **테스트 조건**: 시드 DB · 부서 6(정보서비스팀-test) draft 배치 4(2026-08-31~09-13, 배정 83건). 이 부서는 `custom_rules`가 **없어** AI 검토가 통째로 건너뛰던 곳이다. `_call_gemini`를 캡처 함수로 바꿔 실제 호출 없이 프롬프트만 받아 보고, 특이사항 2건(박정민·이화정)을 넣은 뒤 같은 배치를 다시 검토했다(트랜잭션 rollback으로 DB 원복).
- **Before**: `review_batch(db, 4)` → `review_available=False`, `reason=no_rules`. **프롬프트 0자** — 부서 규칙이 없으면 학생이 무엇을 적어도 읽히지 않는다.
- **수정 내용**:
  - `student_note` 테이블(학생·학기당 한 건) + `GET/PUT /api/availability/me/note`(학생 본인, 공백 저장 = 삭제) + `GET /api/availability/department/{id}/notes`(직원·학생팀장).
  - AI 검토·검토 챗봇 프롬프트에 `## 학생이 낸 특이사항` 섹션 추가. 부서 `custom_rules` 바로 아래 자리다. 시스템 프롬프트에 "규칙이 아니라 사정 — 어긋나면 severity 한 단계 낮추고, 부서 규칙과 부딪히면 부서 규칙 우선, 모호하면 되묻기"를 명시했다.
  - 검토 게이트를 넓혔다: 부서 규칙이 없어도 **학생 특이사항이 있으면 검토한다**. 둘 다 없을 때만 `no_rules`.
  - 솔버에는 넣지 않는다 — 부서 `custom_rules`와 같은 원칙이다.
- **After**: 같은 배치가 `review_available=True`로 돌고 프롬프트 **7,601자**에 두 건이 그대로 실렸다(`- 20261001 박정민: 월요일은 3교시가…`, `- 20261002 이화정: 저녁 근무는 통학 막차…`).
- **중간에 잡은 것**: 처음엔 배치 시작일 학기 하나(`resolve_term_for_department`)로 특이사항을 읽었는데, 배치 4는 8/31(여름)~9/13(가을)로 학기 경계를 넘어 **가을학기에 낸 2건이 통째로 빠졌다**(After 측정이 그대로 `no_rules`로 나와 발견). `services.term_segments`로 걸치는 학기를 모두 읽도록 바꿔 해결했고, 챗봇 컨텍스트도 같은 규칙으로 맞췄다. 회귀 1건 추가.
- 백엔드 **503 passed** (live LLM 테스트 2파일 제외). 회귀 14건 추가 — API 10건(`test_student_note_api.py`), 검토 주입 4건(`test_review.py::TestStudentNotes`).


## 2026-08-29 — "가능하지만 피하고 싶은 시간"을 배정에 반영 (SC-AVOID-1 근거 연결, #185)

- **문제/가설**: 학생 가능 시간이 체크/해제 이진값이라 "가능은 한데 되도록 피하고 싶다"를 낼 방법이 없다. 솔버에는 회피 요청 제약(SC-AVOID-1, 가중치 12)이 이미 있는데 DB 로더가 `avoid_ranges=[]`로 하드코딩돼 있어(근거 테이블 없음) **구현돼 있고도 한 번도 걸리지 않는 제약**이라고 봤다. `available_time.preference`(1/2/3) 컬럼은 있지만 저장 경로가 모두 2로 고정이라 값이 들어올 길도 없었다.
- **테스트 조건**: 시드 DB · 부서 2(로욜라도서관 정보서비스팀, 선발 11명) · 2026-09-07~09-20(2주) · `time_limit 30s`. 저녁 가능 시간이 가장 많은 정창범(20221818)의 18~22시 구간 6건을 `preference=1`로 표시(트랜잭션 rollback으로 DB 원복)하고, 같은 데이터에서 `_AVOID_THRESHOLD`만 0(전개 없음=수정 전)과 1(수정 후)로 바꿔 나란히 돌렸다.
- **Before**: `OPTIMAL` / solve 3.67s / objective 2694. 정창범 저녁 배정 **19.5h**(전체 25.5h) — 회피 표시를 해도 배정이 그대로다. `penalty_summary`에 `avoid_range` 항목 자체가 없다(제약이 걸리지 않음).
- **수정 내용**:
  - `PUT /api/availability/me`에 `slot_preferences`(슬롯 → 1/2/3) 추가. `services.slots_to_preference_intervals`가 병합 시 **선호도가 다르면 맞닿아도 합치지 않는다** — 뭉치면 강도가 사라진다. 보내지 않은 슬롯은 2(가능). `slots`에 없는 슬롯의 선호도는 422.
  - `GET /api/availability/me`가 `slot_preferences`를 함께 돌려준다(기본값 2인 슬롯은 생략).
  - `scheduler/service._load_students_from_db`가 날짜 전개 결과에서 `preference <= _AVOID_THRESHOLD(1)`인 구간을 `AvoidRange`로 만들어 넘긴다. 제약·가중치는 손대지 않았다 — 근거만 연결.
- **After**: `OPTIMAL` / solve 3.90s / objective **2694로 동일**. 정창범 저녁 배정 **19.5h → 0h**(전체 25.5h → 6.0h). 부서 전체 배정 시간은 **256.0h로 그대로**이고 저녁만 116.0h → 110.0h — 그의 저녁 19.5h가 오규원(+11.0h)·권지영(+11.0h)·안승준(+5.0h) 등에게 넘어갔다. 다른 페널티(`fair_hours` 912 · `preference_match` 882 · `meal_break` 440 · `preferred_staffing` 240 · `contiguity` 220)는 Before와 **한 항목도 달라지지 않았다** — 회피를 지키느라 다른 조건을 깎지 않았다는 뜻.
- **관찰**: 회피 가중치(12)가 형평(SC-FAIR-1, 6)보다 커서, 피하고 싶은 시간이 그 학생 가용 시간의 대부분이면 **회피를 지키느라 본인 근무 시간이 크게 줄어든다**(25.5h → 6.0h). 총 배정 시간이 수요로 고정돼 있어 `fair_hours` 총합은 변하지 않지만(한 사람의 미달이 다른 사람의 충족으로 상쇄) 개인 단위로는 손해가 될 수 있다. 화면에서 "피하고 싶음"을 많이 찍으면 근무 시간이 줄 수 있다고 알려주는 편이 낫다.
- 백엔드 479 passed (live LLM 테스트 2파일 제외). 회귀 9건 추가 — API 6건(`test_availability_me.py::TestSlotPreferences`), 로더 3건(`test_student_loader.py::TestAvoidRanges`).
## 2026-08-29 — 학번 재번호 뒤 `--only` 시드가 다른 사람에게 합격 지원서를 붙임 (배포 사고)

- **문제/가설**: `--only`를 행 단위 채우기로 바꾸면서(#192) 존재 판정을 **학번 하나**로 했다. #189가 교육대학원 학번을 `20262021~20262030` → `20263001~20263010`으로 재번호했는데 배포 DB는 옛 번호를 그대로 들고 있어, 아텍 시드가 `20262021`을 "이미 있는 아텍 노시현"으로 오인할 것으로 봤다.
- **테스트 조건**: 배포 서버(3.34.82.68)에서 `--only aat-dept` 실행 후 `GET /api/students/department/{7,8}` 실측. 이어서 로컬 시드 DB에 같은 상태를 재현 — 아텍 학생 `20262021`·`20262022`를 지우고 그 학번으로 `구본영(교육대학원)`·`김려원(교육대학원)`을 넣은 뒤 `--only aat-dept` 실행.
- **Before**: 배포 서버 출력이 `채움 — 지원서 2`(학생 0). 부서 7 명단 **22명**인데 그중 `20262021 구본영(교육대학원)`·`20262022 김려원(교육대학원)`. 같은 두 사람이 부서 8 명단(10명)에도 그대로 있어 **한 사람이 두 부서에 걸쳤다.** 학생·가능시간·수업시간은 안 만들어지고 지원서 2행만 생겼다. 로컬 재현에서도 동일하게 지원서만 붙었다.
- **수정 내용**: `seed_test_department()` 맨 앞에 신원 대조를 넣었다 — 스펙의 학번이 DB에 이미 있으면 `name`·`department_name`을 CSV와 비교하고, 다르면 그 학번은 다른 사람의 것이므로 **어떤 변경도 하기 전에** 충돌 목록을 담아 반환한다. `main()`은 `db.rollback()` 후 무엇이 어긋났는지와 해소 절차를 출력하고 종료 코드 1로 끝낸다. 부서 생성보다 앞에 두어, 충돌 시 부서 행조차 만들지 않는다.
- **After**: 같은 재현 DB에서 `중단했습니다. 아무것도 바꾸지 않았습니다.` + `20262021: DB에는 구본영(교육대학원), 시드는 노시현(아트&테크놀로지학과)` 2건, 종료 코드 **1**. 실행 후 `posting_id=8` 지원서 **20건 그대로**(늘지 않음). 충돌을 없앤 뒤 다시 실행하면 `채움 — 학생 2 · 지원서 2 · 가능시간 8 · 수업시간 10`로 정상 동작. 백엔드 테스트 **488 passed**(신규 2건 포함). 배포 서버는 잘못 생긴 지원서 2행을 삭제해 부서 7이 20명·학과 불일치 0으로 복구했다.

## 2026-08-29 — `--only` 시드가 부서 행만 보고 통째로 스킵해 배포 DB가 뒤처짐

- **문제/가설**: `seed_test_department()`의 멱등 가드가 `department` 행 존재 여부 하나였다. 시드 CSV에 학생을 더해 develop에 머지해도 서버에는 부서 행이 이미 있어 `--only`가 아무것도 하지 않는다고 봤다. 같은 이유로 새로 추가한 컬럼도 값이 안 채워질 것으로 의심했다.
- **테스트 조건**: 배포 서버(3.34.82.68) API 실측 + 로컬 시드 DB에 배포 상태를 재현. 부서 7(아텍) 학생 2명·지원서·가능시간·수업시간을 지우고, `department.headcount_to`를 22 → 20, `course_ta_enabled`를 true → false로 되돌린 뒤 담당자가 고친 것처럼 `department_policy.min_per_slot=3`·`custom_rules`를 심었다. 그 상태에서 `--only aat-dept` 실행.
- **Before**: 배포 서버 — `STF011` 로그인 응답 `course_ta_enabled: false`(전 부서 false), `GET /api/students/department/7` **20명**(`20262001~20262020`). 코드는 배포됐는데(응답에 필드 존재) DB 값이 없어 **수업 조교 편성 메뉴가 어느 부서에서도 안 보였다.** 로컬 재현에서도 `--only aat-dept`가 `부서 7이 이미 있습니다 — 아무것도 바꾸지 않았습니다.`만 찍고 종료.
- **수정 내용**: 가드를 행 단위 채우기로 교체. 부서·정책·직원·학생·공고·지원서는 키로 존재 확인 후 없는 것만 삽입하고, `department` 행의 시드 소유 스칼라 값(이름·상한·정원·`course_ta_enabled`)은 CSV 기준으로 맞춘다. 반대로 `department_policy`(담당자가 화면에서 고치는 값)와 이미 있는 행의 내용은 덮지 않는다. 가능시간·수업시간·날짜 예외는 자연 키가 없어 **그 학생이 한 행도 없을 때만** 넣는다 — 이미 낸 학생에게 덧붙이면 중복이 된다. 반환값을 bool → 채운 항목 dict로 바꿔 무엇이 바뀌었는지 출력한다.
- **After**: 같은 재현 DB에서 `--only aat-dept` → `채움 — 학생 2 · 지원서 2 · 가능시간 8 · 수업시간 10`, `부서 값 갱신 — headcount_to, course_ta_enabled`. 결과는 `headcount_to=22`·`course_ta_enabled=t`·학생 **22명**(`20262001~20262022`)이고, 담당자가 심어 둔 `min_per_slot=3`·`custom_rules`는 **그대로 남았다**. 한 번 더 실행하면 `이미 최신 시드와 같습니다. 바꾼 것이 없습니다.`이고 학생 22·가능시간 119·지원서 22로 행 수 변화 없음. 부서 6·8도 같은 메시지. 백엔드 테스트 **486 passed**(신규 `test_seed_only_idempotent.py` 6건 포함).

## 2026-08-29 — 아텍 정원을 과목 TA 수요로 역산해 22명으로 (#172, #173)

- **문제/가설**: 아텍 정원 20명은 근거 없이 잡힌 값이었다. 아텍 근로는 축이 둘인데(과 사무실 대기 근무 주 7시간 + 과목 TA 1~2과목), 정원을 정하는 쪽은 **TA 축**이다 — 대기 근무는 개관 90h(2주)만 채우면 되지만 TA는 과목 수만큼 사람이 필요하다. 담당 범위를 아텍 18과목 + 지식융합미디어대학 23과목 = **41과목**으로 잡으면, 한 사람 최대 2과목(`MAX_COURSES_PER_TA`)이라 공급이 `인원 × 2`슬롯이고 20명 = 40슬롯 < 41과목이라 **전 과목에 1명씩도 안 붙는다**고 봤다.
- **테스트 조건**: `courses_2026_2.csv`(2026-2 실제 개설 데이터)와 아텍 학생 수업 시간표로 배정 가능성을 CP-SAT로 직접 풀었다. 제약은 실제 API(`_blocking_reason`)와 같게 — 1인 최대 2과목, 본인 수강 시간과 겹침 금지, 맡은 과목끼리 시간 겹침 금지. 별개로 근무표 생성은 검증용 DB 재시드 후 부서 7, 2026-09-07~09-20(2주).
- **Before(20명)**: TA 배정 OPTIMAL — **41과목 중 40과목**만 배정, 1과목 미배정. 40슬롯을 한 칸도 남기지 않고 쓴 결과라 시간 충돌로 낭비된 슬롯은 없었다(= 순수 인원 부족). 50명↑ 2명 규칙(수요 52슬롯)은 40슬롯만 충족, 12 미달. 근무표 생성은 OPTIMAL 1.86~2.20s · 미충원 0 · 배정 90.0h · 근무건 24.
- **수정 내용**: 부서 7 정원과 공고 8 `headcount`를 20 → **22**로 올리고, 학생 2명(`20262021` 노시현 · `20262022` 황가람)을 시드에 추가했다. 수업 시간표는 실제 아텍 교시 격자에서 뽑고, 가능 시간은 1.5시간 블록 경계에 맞춰 수업과 겹치지 않게 넣었다. 학번 대역도 함께 정리했다 — 아텍이 20명을 넘으면서 교육대학원 대역(`20262021`~)과 부딪히므로, 부서 6이 `20261xxx`·부서 7이 `20262xxx`인 규칙에 맞춰 **교육대학원을 `20263001`~`20263010`으로 옮기고** 아텍을 `20262001`~`20262022` 연속 대역으로 만들었다. `--only`는 부서 존재 여부로만 멱등성을 판단하므로(학번이 아니라) 이미 시드된 DB가 중복·고아 계정을 만들지는 않는다 — 다만 그 DB는 옛 학번을 그대로 들고 있으므로 DEPLOY.md의 수동 갱신 절차를 타야 한다.
- **After(22명)**: TA 배정 OPTIMAL — **41/41과목 전부 배정**, 공급 44슬롯 중 3슬롯 여유. 50명↑ 2명 규칙은 여전히 8슬롯 미달이라(26명 필요) 이번 범위 밖이다. 근무표 생성은 **OPTIMAL 1.98s · 미충원 0 · 배정 90.0h · 근무건 26**으로 대기 근무 쪽 결과는 그대로다(개관 수요가 그대로라 총량 불변). 2주간 배정 0시간인 학생은 8명 → **5명**으로 줄었는데, 이는 인원이 늘며 `fair_hours` 목표 총합이 커진 부수효과일 뿐 배분 축이 고쳐진 것은 아니다(앞 항목의 무효 구간은 그대로). 백엔드 **470 passed · 2 xfailed**.
- 부서 2주 총합 상한 `gyobi_biweekly_dept_total_max_hours: 280`은 그대로 뒀다 — 실제 배정 90.0h가 한참 아래라 걸리지 않고, 다른 부서(190·200)도 인원×상한으로 유도된 값이 아니라 운영 예산값이다.

## 2026-08-29 — 아텍 근무 블록 1.5시간화 · 공평 배분 제약의 무효 구간 발견 (#172)

- **문제/가설**: 아텍 대기 근무 블록이 3시간(09-12·12-15·15-18)인데, 학과 수업은 75분 교시(09:00~10:15, 10:30~11:45, …)라 블록 하나가 교시 두 개를 걸친다. 공고가 "공강시간이 충분하여 조교 근무시간 조정에 어려움이 없는 사람"을 요건으로 걸 만큼 공강에 끼워 넣는 근무인데, 3시간 블록은 한 교시만 비는 학생을 통째로 배제한다고 봤다. 또 공고의 **주 7시간은 과 사무실 대기 근무 몫**이고 과목 TA 출결체크(1~2과목)는 별도 축(#173)이라, 블록을 잘게 쪼개도 상한 해석이 달라지지 않는다.
- **테스트 조건**: 검증용 DB(`stream_seed_check`) 전체 재시드 후 부서 7, 2026-09-07~09-20(2주) 생성. 블록만 3시간 3개 → 1.5시간 6개로 바꾸고 나머지는 동일. 각 3회 실행(결과 동일).
- **Before**: 개관 90.0h·180슬롯 / OPTIMAL 1.78s · 미충원 0 · 배정 90.0h · 근무건 24. 학생별 2주 배정 `{0h: 5명, 3h: 6명, 6h: 6명, 12h: 3명}` — **20명 중 5명이 2주간 한 번도 배정되지 않음**. 페널티 `fair_hours 2280 · preference_match 108 · contiguity 96`, objective **2484**.
- **수정 내용**: `aat_department_office.json`의 `work_slots`를 09:00·10:30·12:00·13:30·15:00·16:30·18:00 경계의 1.5시간 6블록으로 바꿨다. 블록 하나가 75분 교시 하나를 감싸므로, 공강이 한 교시뿐인 학생도 블록을 통째로 설 수 있다. 개관(09:00~18:00)을 정확히 타일링해 `validate_work_slots_tiling`을 통과한다.
- **After**: OPTIMAL 1.86~2.20s · 미충원 0 · 배정 90.0h · 근무건 24. 학생별 배정 `{0h: 8명, 6h: 9명, 12h: 3명}`. 페널티·objective는 **2484로 Before와 완전히 동일**했다.

  동일 objective가 가리킨 것이 더 중요하다 — **`FairHoursConstraint`가 이 조건에서 배분에 아무 영향을 주지 못한다.** 목표치가 `min(주간 상한, 그 주 가용 슬롯)`인데 아텍 학생 20명 전원이 가용 슬롯 ≥ 주간 상한(7h=14슬롯)이라 목표가 모두 14슬롯으로 같고, 주간 상한이 배정을 목표 이하로 묶으므로 `Σ미달 = Σ목표 − Σ배정 = 560 − 180 = 380슬롯`이 **배분과 무관한 상수**가 된다(× 가중치 6 = 2280). 결과적으로 "5명 0시간 / 3명 12시간"과 "전원 4.5시간"의 비용이 같고, 실제 배분은 `contiguity`(블록 시작당 4점)가 정한다 — 조각 근무를 줄이려 소수에게 몰아주는 쪽이 이긴다. 블록을 쪼개면 하루 최대 블록 수가 늘어 이 쏠림이 오히려 뚜렷해졌다(0시간 5명 → 8명). **부서 7의 회귀가 아니라 원래 최적화되고 있지 않던 축이 드러난 것**이며, 총수요가 인원 총capacity보다 작은 부서는 모두 같은 구간에 있다(부서 6·8도 해당). 배분 축 자체의 수정은 정보서비스팀까지 영향이 가므로 별도 항목으로 다룬다.
- 백엔드 **454 passed**(live LLM 2파일 제외).

## 2026-08-29 — 검증 부서 2곳을 실제 공고 값으로 교정 (#172)

- **문제/가설**: 부서 7·8의 개관 시간·수업 시간표를 실제 모집 공고와 대조한 적이 없었다. 교육대학원 행정팀 공고(2023-08-07)는 근무시간이 **학기중 월~목 10~21시 · 금 10~17시**인데 정책 파일은 월~목 22시 · 금 09시로 들어가 있었고, 학생 가능 시간도 22:00까지 · 금 09:00부터라 **개관 밖 구간을 낸 상태**였다. 개관 밖 가능 시간은 배정에 안 쓰이면서 화면에는 낸 것처럼 보여, 나중에 "냈는데 안 잡힌다"로 되돌아온다고 봤다. 또 교육대학원 학생 수업 시간이 주간(10:30~11:45 등)으로 들어가 있는데, 2026-2 개설교과목을 보면 64강좌 중 **59강좌가 월~목 18:30~21:40 야간**이라 저녁 근무 가능자 판정이 실제와 반대로 서 있었다.
- **테스트 조건**: 검증용 DB(`stream_seed_check`)에 `--reset` 전체 시드 후 부서 7·8에 대해 2026-09-07~09-20(2주) 생성. 수정 전후 같은 DB·같은 기간으로 두 번 측정했다.
- **Before**: 부서 7 개관 90.0h·180슬롯 / OPTIMAL 1.78s · 미충원 0 · 배정 90.0h. 부서 8 개관 **112.0h·224슬롯** / OPTIMAL 2.94s · 미충원 0 · 배정 **194.0h**(2주 부서 상한 200h까지 여유 6h).
- **수정 내용**: (1) `grad_edu_admin.json` 개관을 공고대로 월~목 21:00 · 금 10:00으로 내리고, 야간 선호 밴드(2명)도 18:00~**21:00**으로 맞췄다. (2) 학생 가능 시간에서 개관 밖 13행을 잘랐다(22:00→21:00 12행, 금 09:00~13:00→10:00~14:00 1행). (3) 교육대학원 학생 수업 시간 24행을 실제 야간 교시(18:30~20:00 / 20:10~21:40)로 교체하되, 각자 가능 시간과 겹치지 않는 요일에 배정했다. (4) 공고 8·9의 업무·자격 문구를 실제 공고 내용으로 채우고, 아텍 공고 분류를 `교내 부서` → `학과별 사무실`로 고쳤다. (5) 부서가 스스로 정한 주간 상한을 `departments.csv`에 명시했다(부서 8 = 14). 아텍 주 7시간은 이미 들어가 있었고 정책 파일의 `gyobi_weekly_max_hours: 14`는 **법정** 상한이라 그대로 뒀다 — 실제 적용값이 7h임을 생성 로그로 확인했다.
- **After**: 부서 7은 개관·상한 변경이 없어 그대로(개관 90.0h·180슬롯 / OPTIMAL **1.55s** · 미충원 0 · 배정 90.0h). 부서 8은 개관 **102.0h·204슬롯**(−10.0h = 월~목 1h×8일 + 금 1h×2일)으로 줄고, **OPTIMAL 3.9~5.2s(3회 실행) · 미충원 0 · 배정 176.0h**(200h까지 여유 24h). 페널티 내역에 `preferred_slot_miss`가 없어 **야간 2명 선호는 전부 충족**됐다. 백엔드 **454 passed**(live LLM 2파일 제외, 직전과 동일).

## 2026-08-29 — 학생 관리 '근무 가능 시간표'가 방학 주차로 열려 가을학기 수합이 비어 보임 (#184)

- **문제/가설**: 담당자 학생 관리 화면의 가능 시간표가 늘 **오늘이 속한 주**로 열린다. 가능 시간은 학기(term) 단위로 저장되므로, 학기 경계 직전(오늘 2026-08-29, `2026-summer`는 08-31까지)에 열면 다음 학기 수합이 통째로 안 보이고 "학생이 안 냈다"로 오독된다. 패널에 학기를 고를 수단이 없어 담당자가 스스로 몇 주를 넘겨야 하는지 알아내야 하는 점도 함께 의심했다.
- **테스트 조건**: 로컬 시드 DB(`seed_mock_data.py`), 정보서비스팀-test(부서 6, 학생 10명), 담당자 `STF010`. 화면은 `/admin/students` 첫 진입 상태(첫 학생 자동 선택), 데이터는 `GET /api/availability/department/6/dates`로 직접 대조.
- **Before**: 진입 시 주차 = `2026.08.24 ~ 08.30`(여름학기). 그 주 응답 **9행 / 6명** — 이화정(20261002)·유강훈(20261003)·김호진(20261004)·문소윤(20261007)은 `2026-summer` 가능 시간이 0행이라 격자가 완전히 비었고, 자동 선택되는 첫 학생 강보영(20261008)도 1행뿐이라 화면이 사실상 빈 표로 보였다. 같은 부서의 가을 첫 주 `2026.08.31 ~ 09.06` 응답은 **70행 / 9명**.
- **수정 내용**: `frontend/src/pages/admin/AdminStudentsPage.jsx` — ①기본 주차를 `mondayOfIso(오늘)` → **다음 주 월요일**로 변경(근무표 생성 화면 `AdminSchedulePage.nextMondayIso()`와 같은 기준: 이번 주는 이미 확정 근무가 도는 주라 담당자가 확인할 대상은 다음 주부터다). ②패널 헤더에 학기 선택기 추가(`GET /api/academic/terms`) — 고르면 그 학기 시작일이 든 주로 이동하고, 주차를 넘기면 선택기도 따라 바뀐다(그 주 7일 중 가장 많이 걸친 학기로 판정 — 개강 주는 한 주가 두 학기에 걸친다). ③수업 시간표(붉은 칸)도 보고 있는 주의 학기로 조회해 방학 수업이 학기 주차에 겹쳐 그려지지 않게 했다. ④가능 시간이 0인 주의 안내에 학기명·주차를 넣었다. `components/admin/AdminPanel.jsx` — 헤더에 `flexWrap`/제목 `nowrap`을 넣어, 우측 컨트롤이 넓을 때 제목이 글자 단위로 쪼개지던 것을 컨트롤이 아랫줄로 내려가도록 고쳤다.
- **After**: 같은 계정·같은 진입 경로에서 주차 = `2026.08.31 ~ 09.06`, 학기 선택기 `2026 가을학기`로 열린다. 그 주 응답 **70행 / 9명**이 그대로 격자에 그려지고(강보영 기준 빈 표 → 채워진 표), ◀로 한 주 되돌리면 선택기가 `2026 여름학기 (진행 중)`로 바뀌며 기존의 9행/6명 화면이 나온다. 학기 선택기로 `2026 여름학기` → `2026.06.22 ~ 06.28`, `2026 가을학기` → `2026.08.31 ~ 09.06`로 이동하는 것까지 브라우저에서 확인. `vite build` 통과, 콘솔 에러 없음.

## 2026-08-29 — 학생팀장에게 부서 설정 변경 개방 (조회 전용 해제)

- **문제/가설**: #156에서 학생팀장에게 부서 정책을 **조회만** 열었는데, 개관 시간·근무 슬롯·배정 인원·중요도는 곧 편성 결과다. 편성만 맡기고 기준값을 직원 몫으로 두면 팀장이 근무표를 짤 때마다 직원 응답을 기다리게 된다고 봤다. 챗봇 배율 저장(`weights/persist`)도 같은 `soft_weight_scales`를 쓰는데 한쪽만 막혀 있어, 화면에서는 되고 챗봇에서는 안 되는 경계가 생기는지도 확인 대상이었다.
- **테스트 조건**: `tests/test_schedule_editor_permission.py`(부서 2곳·직원 1명·같은 부서 학생팀장/일반 학생·타부서 학생팀장 시나리오)에서 `PATCH /api/schedule/policy/{id}` 응답 코드를 측정. 화면은 로컬 dev(:5173 + :8000, 시드 DB)에서 학생팀장 20261005(김찬우, 부서 6)로 로그인해 부서 설정에서 2주 상한을 저장해 확인했다.
- **Before**: 학생팀장 PATCH → **403** `{"error": "직원만 공고를 등록할 수 있습니다."}`(`auth.require_staff`의 공용 메시지). 화면은 `AdminSettingsPage`가 readOnly로 그려 입력·저장 버튼이 아예 없었다.
- **수정 내용**:
  - `PATCH /api/schedule/policy/{id}`: `auth.require_staff` → `services.require_schedule_editor`, 부서 확인도 `require_own_department` → `require_own_department_or_lead`. 편성 경로와 같은 선으로 옮겼다.
  - `POST /api/schedule/chat/sessions/{id}/weights/persist`: 같은 이유로 `require_schedule_editor`로 열고, 세션 소유 확인에 더해 저장 시점 부서 확인(`require_own_department_or_lead`)을 추가했다 — 세션 생성 후 소속이 바뀐 계정이 남의 부서 정책을 쓰지 못하게.
  - 프론트: `DepartmentPolicyEditor`의 `readOnly` 분기(가드 4·`disabled` 6·커서/툴팁/버튼 감춤)를 전부 제거하고, `AdminSettingsPage`의 조회 전용 배너·안내 문구, `AdminSchedulePage`의 "부서 담당 직원에게 요청해 주세요"·'부서 설정 보기' 문구도 함께 걷어냈다.
  - 막히는 경계는 그대로다: 일반 근로 학생 403(`편성할 권한`), 타부서 팀장 403(`본인 소속 부서`).
- **After**: 학생팀장 PATCH → **200**, 응답·DB 모두 반영(부서 6의 `biweekly_max_hours` 190 → 188 저장 확인 후 190으로 복원). 화면에서도 "설정이 저장되었습니다" 배너와 `PATCH /api/schedule/policy/6 → 200 OK`를 확인했다. 권한 테스트 **24 passed**(신규 3건: 본인 부서 변경 200·타부서 403·일반 학생 403), 정책·챗봇 관련 5개 파일 합쳐 **86 passed**. 전체 백엔드 스위트는 451 passed / 1 failed + 5 errors이며, 실패·에러는 모두 이 변경과 무관한 기존 항목(live LLM 테스트, `ChatSession.staff_id` 옛 필드명을 쓰는 `test_chat_live.py` 픽스처)이다.

## 2026-08-29 — 부서 수업 시간표를 날짜 단위로 전개 (개강 주 학기 혼선 해소)

- **문제/가설**: `GET /api/class-time/department/{id}` 응답이 주간 패턴(`day_of_week`) 형태라 한 번에 학기 하나밖에 담지 못한다. 가능 시간(`/dates`)은 학기 구간으로 쪼개 날짜별로 내려주므로, 개강 주처럼 한 주가 학기 경계를 넘으면 **가능 시간은 두 학기, 수업 시간은 한 학기**가 되어 같은 격자 위에서 어긋난다고 봤다.
- **테스트 조건**: 시드 DB · 부서 2(로욜라도서관 정보서비스팀, 선발 11명) · 담당자 STF001 · 기간 2026-08-31~2026-09-06(`term_segments` → `[(2026-summer, 08-31, 08-31), (2026-2, 09-01, 09-06)]`). 이 부서의 수업 시간은 `2026-2` 34건/11명 · `2026-summer` 4건/2명(조수현 수·금, 정창범 화·목)이라 두 학기가 뚜렷이 다르다. 신규 엔드포인트 응답을 "날짜마다 그 날짜의 학기 행 중 요일이 맞는 것"으로 계산한 기대 집합과 대조하고, 화면은 dev 서버(:5173)에서 확인했다.
- **Before**: 여름학기 하나로 그 주를 덮으면 **4건** — 날짜별 `09-01:1 · 09-02:1 · 09-03:1 · 09-04:1`(조수현·정창범뿐). 나머지 9명은 수업이 아예 안 보인다. 화면에서도 오규원의 08.31~09.06 주차 수업 칸이 **0칸**(학생 관리)·**0블록**(근무표 편성 상세)이었다.
- **수정 내용**:
  - `schemas.ClassTimeDateItem` + `GET /api/class-time/department/{id}/dates` 추가. 가능 시간 `/dates`와 같은 구조 — `term_segments`로 기간을 자르고, 구간에 등장하는 학기마다 ClassTime을 한 번씩 조회한 뒤 구간별로 전개해 합친다. 62일 상한·권한(`require_schedule_editor` + `require_own_department_or_lead`)도 동일하다. 수업엔 날짜 예외(휴강) 테이블이 없어 예외 없이 전개하지만, 요일↔날짜 규칙을 한 곳에 두려고 같은 `materialize_availability`를 `weekly_only`로 재사용했다. 기존 주간 패턴 엔드포인트는 그대로 둔다 — 근무표 편성의 '매주 반복 패턴' 보기가 학기 단위 값을 쓴다.
  - 프론트: `AdminStudentsPage`(주차 보기 전용)는 `fetchDepartmentClassTimeDates`로 전환하고 학기 계산(`termKeyForDate`)을 걷어냈다. `AdminSchedulePage`는 '특정 주'에서만 날짜 조회를 얹고 '매주 반복 패턴'은 기존 학기 조회를 유지한다. 날짜 조회가 실패하면 패턴 값으로 폴백한다(빈 배열은 '이 주엔 수업 없음'이라는 정상 응답이라 실패와 구분).
- **After**: 같은 기간에서 **28건** — 날짜별 `09-01:8 · 09-02:6 · 09-03:9 · 09-04:5`, 11명 전원. 08-31(방학 구간, 월요일)은 그 학기에 월요일 수업이 없어 0건으로 정확히 비었다. 기대 집합과 **완전 일치**(빠짐 0 · 군더더기 0). 화면에서 오규원 08.31~09.06 주차 수업 칸이 학생 관리 **0 → 15칸**(30분 단위, 화 10:30–12:00·13:30–15:00 / 목 10:30–12:00·15:00–18:00과 일치), 근무표 편성 상세 **0 → 5블록**. 같은 계정의 '매주 반복 패턴'(여름학기) 보기는 그대로 0칸 — 학기 단위 화면은 건드리지 않았다.
  - 회귀 5건 추가(경계를 넘는 주는 날짜마다 다른 학기 · 한 학기 안 기간은 요일 전개 · 수업 없는 기간은 빈 목록 · 기간 검증 400 · 타 부서 403). 백엔드 434 passed (live LLM 테스트 2파일 제외). `docs/API_SPEC.md` 갱신.

## 2026-08-29 — 개강 주 가능 시간이 방학 패턴으로 전개되던 문제 (term_segments 누락)

- **문제/가설**: `GET /api/availability/department/{id}/dates`가 `resolve_term(None, from_date)` 한 번으로 조회 기간 전체의 AvailableTime을 읽는다. 기간이 학기 경계를 넘으면(개강 주 2026-08-31~09-06 — 8/31은 `2026-summer`, 9/1부터 `2026-2`) 9/1~9/5에 방학 패턴이 붙어, 가을학기 가능 시간을 낸 학생이 그 주에 근무 불가로 보일 것이라고 봤다. 근무표 생성(`scheduler/service._load_students_from_db`)은 같은 상황에서 이미 `services.term_segments`로 기간을 쪼개므로, 화면과 생성 결과가 서로 다른 가능 시간을 보고 있는 셈이다.
- **테스트 조건**: 시드 DB · 부서 2(로욜라도서관 정보서비스팀, 선발 11명) · 담당자 STF001 · 기간 2026-08-31~2026-09-06. `term_segments`는 이 주를 `[(2026-summer, 08-31, 08-31), (2026-2, 09-01, 09-06)]` 2구간으로 자른다. 수정 전/후 전개를 `backend/.venv/bin/python3` 스크립트로 같은 DB·같은 기간에 대해 나란히 계산하고, 수정 후 값은 실행 중인 서버(:8000)에 실제로 요청해 대조했다.
  - 부서 6(정보서비스팀-test)은 이 주의 9/1~9/5가 전부 **날짜 예외**로 덮여 있어(운영 시트 1주차) 주간 패턴이 드러나지 않는다 — 버그가 가려지는 케이스라 부서 2로 측정했다.
- **Before**: 11명 전원의 값이 틀렸다. 주 합계 **485.0시간**, 날짜별 구간 수 `08-31:15 · 09-01:14 · 09-02:15 · 09-03:18 · 09-04:14 · 09-05:8 · 09-06:0`. 예) 안승준 43.0h, 박민진 43.0h, 조수현 57.0h.
- **수정 내용**: `routers/schedule.py`의 `list_department_availability_by_date`가 `term_segments(from_date, to_date)`로 기간을 학기 구간으로 자르고, 구간에 등장하는 학기마다 AvailableTime을 한 번씩 조회한 뒤(`weekly_by_term`) 구간별로 `materialize_availability`를 돌려 결과를 합치도록 바꿨다. 구간은 날짜가 겹치지 않아 `dict.update`로 합쳐도 안전하다. 날짜 예외는 학기와 무관하므로 기존대로 기간 전체를 한 번만 조회한다. `_load_students_from_db`와 같은 규칙·같은 헬퍼다.
- **After**: 같은 기간·같은 부서에서 주 합계 **511.0시간**(+26.0h), 날짜별 구간 수 `08-31:15 · 09-01:19 · 09-02:16 · 09-03:21 · 09-04:21 · 09-05:9 · 09-06:0`. 방학 구간인 8/31은 15건 그대로이고 9/1 이후만 바뀌었다. 예) 안승준 43.0 → **59.0h**, 박민진 43.0 → 52.0h, 조수현 57.0 → 60.0h. 실행 중인 서버 응답(101구간 · 511.0h)이 계산과 일치했고, 화면(STF001 → 학생 관리 → 안승준 → 08.31~09.06)도 요일별 합계 `7·11·11·11·11·8·0` = **총 59시간**으로 같은 값을 보여준다.
  - 회귀 3건 추가(`TestTermBoundary`: 경계를 넘는 주는 날짜마다 다른 학기 패턴 · 한 학기 안 기간은 구간 1개라 기존과 동일 · 날짜 예외는 경계와 무관하게 적용). 수정 전 코드에서 첫 번째 테스트가 실패하는 것을 확인했다. 백엔드 427 passed (live LLM 테스트 2파일 제외).
  - 남은 것: `GET /api/class-time/department/{id}`는 응답이 주간 패턴(day_of_week) 형태라 학기 하나만 담을 수 있다. 한 주가 두 학기에 걸치면 화면이 그중 한 학기 시간표만 겹쳐 보게 된다.

## 2026-08-29 — 수합·수업 시간표가 '오늘 학기'에 고정돼 비어 보이던 문제 (#156 후속)

- **문제/가설**: 학생팀장(김찬우, 부서 6) 계정으로 '근무표 편성 → 수합된 근무 시간표'를 열면 수업 시간이 거의 없고 '가능 시간 미확보 4명'이 뜬다. 시드 데이터가 빠진 게 아니라, 학기를 넘기지 않은 조회가 서버의 `resolve_term(None)` = **오늘 기준 학기**로 떨어지는 것이 원인이라고 봤다. 오늘(2026-08-29)은 여름학기(6/23~8/31)에 속하는데 운영 시트 수합본은 가을학기(2026-2)에 들어 있다.
- **테스트 조건**: 시드 DB · 부서 6(정보서비스팀-test, 선발 10명) · 팀장 계정 20261005. 엔드포인트를 그대로 재현(`backend/.venv/bin/python3`로 `get_department_student_ids` + `term_filter`)하고, dev 서버(:5173)에서 같은 화면을 학기·주차를 바꿔 가며 확인했다.
- **Before**:
  - `class-time/department/6` term 미지정 → `2026-summer` 해석 → **2건 / 2명**. `term='2026-2'`이면 **63건 / 10명**. (DB에는 두 학기 모두 들어 있다: 2026-2 63건, 2026-summer 2건)
  - 수합 화면 진입 기본값(시작일 08.31 → rosterTerm `2026-summer`): 가능시간 확보 **6명 · 미확보 4명 · 총 53h**. 학기를 `2026-2`로 바꾸면 **10명 · 0명 · 314.5h**.
  - '특정 주'로 09.07~09.13을 보면 격자와 총 가능시간은 **314.5h**인데 옆의 통계·배지는 그대로 **미확보 4명** — 격자는 주차(가을), 통계는 패턴(여름) 기준이라 한 화면에서 두 학기가 섞여 나온다.
  - `AdminStudentsPage`는 부서 수업 시간을 **term 없이** 불러 항상 오늘 학기 것을 쓴다. 09.07 주차를 봐도 이화정의 수업 칸이 **0칸**(가을 수업 7구간이 있는데도).
- **수정 내용**:
  - `AdminSchedulePage.AvailabilitySection` — '특정 주' 보기에서 `submitted`를 그 주 데이터 기준으로 다시 판정하고, 통계 카드도 `roster`가 아니라 화면에 그려지는 `viewRoster`로 센다. 로딩 중(`weekRows === null`)에는 패턴 값을 유지해 전원 미확보로 깜빡이지 않게 했다.
  - `AdminStudentsPage` — 보고 있는 주차의 학기(`termKeyForDate`)를 계산해 `fetchDepartmentClassTime(deptId, weekTerm)`으로 넘긴다. 가능 시간(`/availability/.../dates`)과 같은 학기를 겹쳐 보게 된다. `SchedulePage`가 이미 쓰던 방식과 동일하다.
- **After**:
  - 수합 '특정 주' 09.07~09.13: 가능시간 확보 **10명 · 미확보 0명 · 314.5h**, 학생 배지 10명 전원 '직접 입력'. 격자·통계·배지가 같은 값을 말한다. 08.24~08.30(여름) 주차는 그대로 6명/4명 — 그 주 기준으로 맞는 값이다.
  - 학생 관리 09.07 주차: 이화정 수업 칸 **0 → 33칸**(30분 단위, 가을 수업 7구간과 일치).
  - 남은 것: `/availability/department/{id}/dates`는 `resolve_term(None, from_date)`로 **시작일 학기 하나**를 기간 전체에 쓴다. 08.31~09.06처럼 한 주가 두 학기에 걸치면 9/1~9/5에 여름학기 패턴이 붙는다. `services.term_segments`가 이 경우를 위해 이미 있는데 이 엔드포인트만 쓰지 않는다.

## 2026-08-29 — AI 검토·챗봇 기본 모델을 gemini-3.7-flash로 전환 (#177)

- **문제/가설**: #114에서 3.5-flash와 3.7-flash가 검출력 동률(28/28)이고 3.7이 응답시간·토큰에서 앞선다는 결과가 나왔는데, 프로덕션 기본값은 3.5로 남아 있었다. 케이스가 14 → 18개로 늘어난 지금도 3.7이 최소한 동률인지 확인하고 전환하려 함.
- **테스트 조건**: `scripts/eval_review.py --model {gemini-3.5-flash,gemini-3.7-flash}`, 18케이스(`eval_review_cases.json`) 고정, 모델당 2라운드(각 `--repeat 1` 별도 실행), 딜레이 없음, 같은 날 연속 실행. 프롬프트·스키마·케이스 모두 동일하고 변경점은 모델명뿐.
- **Before**: gemini-3.5-flash — 1라운드 **17/18**, 2라운드 18/18(합계 35/36). 평균 응답 5.7s / 4.9s, 호출당 토큰 4,684 / 4,450. 1라운드에서 `no-clarify-gender-policy`(되묻기 금지 — 성별 선호 정책성 질문) 실패: 되묻지 말아야 할 케이스에서 "검토가 불가능합니다"로 되물었다. 2라운드엔 통과 → 라운드 간 흔들림.
- **수정 내용**: `app/scheduler/review.py`의 `REVIEW_MODEL`, `app/scheduler/chat.py`의 `CHAT_MODEL` 기본값을 `gemini-3.5-flash` → `gemini-3.7-flash`로 변경. provider는 REQ-SCHED-016대로 Gemini 고정이며 세대만 올렸다. `.env.example`에는 두 변수를 **주석 처리해** 넣었다 — 빈 값(`REVIEW_MODEL=`)으로 두면 `os.getenv(key, default)`가 빈 문자열을 유효한 값으로 받아 SDK가 `ValueError: model is required.`로 죽는 것을 검증 중에 확인했고, 코드도 `os.getenv(key) or default` 형태로 바꿔 빈 값에 견디게 했다. 또 배포 `.env`에는 모델 변수가 없어 서버도 코드 기본값을 따른다(`docs/DEPLOY.md`).
- **After**: gemini-3.7-flash — 1라운드 **18/18**, 2라운드 18/18(합계 **36/36**). 평균 응답 4.0s / 4.2s, 호출당 토큰 4,158 / 4,089. 두 라운드 모두 실패 케이스 0건, AI 호출 실패 0건.

  | | gemini-3.5-flash | gemini-3.7-flash |
  |---|---|---|
  | 검출률(2라운드 합계) | 35/36 | **36/36** |
  | 평균 응답시간 | 5.3s | **4.1s (-23%)** |
  | 호출당 토큰 | 4,567 | **4,124 (-10%)** |

  **429 없음** — 8/24에 3.7-flash 측정을 막았던 `GenerateRequestsPerDayPerProjectPerModel-FreeTier`(20/일) 제한은 유료 전환 후 재현되지 않았다. 72회 호출(2모델 × 18케이스 × 2라운드) 동안 호출 실패 0건으로, #177이 유일한 실질 리스크로 꼽았던 항목이 해소됐다.

  **회귀**: 백엔드 스위트 433 passed(실 LLM 호출 2개 파일 제외). 실제 호출 테스트 `test_review_live.py`는 3.7·3.5 양쪽 다 **17/18**로 같았고 실패 케이스도 `tenure-relative-comparison` 하나로 동일했다 — 모델 전환과 무관한 기존 비결정 실패다. 소요는 66s(3.7) vs 89s(3.5).

  **한계**: 측정은 전부 *검토* 케이스 세트다. 챗봇(`chat.py`)은 같은 세대로 맞추기만 했고 챗봇 과제로 재지 않았다 — 이상 징후가 보이면 `CHAT_MODEL`로 되돌릴 수 있다. 원본 결과는 `backend/output/eval_2026-08-29_gemini-3.{5,7}-flash*.json`(gitignore 대상, 로컬 보관).

## 2026-08-29 — 부서 기본 학기 (#172)

- **문제/가설**: 학기를 지정하지 않은 조회는 `resolve_term`이 **오늘 날짜**로 학기를 정한다. 검증용 test 부서 셋은 가을학기(`2026-2`) 데이터만 있는데 오늘(8/29)은 `2026-summer`라, 조교가 화면을 열면 학생 관리·수합 조회·수업 조교가 모두 빈 채로 보인다고 봤다. 방학에 다음 학기를 준비하는 게 정상 흐름이라 날짜만으로 학기를 정하는 게 맞지 않는다는 가설이다.
- **테스트 조건**: 검증용 DB(`stream_seed_check`)에 전체 시드를 다시 넣고, test 부서 3곳(6·7·8)에 대해 2026-09-07~09-20(2주) 생성을 돌렸다. 화면은 부서 7 · `STF011`로 수업 조교를 열어 확인했다(앞 항목).
- **Before**: 부서 7 수업 조교 화면이 `2026-summer`로 열려 **과목 0개**. 부서 6·7·8의 부서 수합 조회도 같은 이유로 빈 목록이었다.
- **수정 내용**: `department_policy.default_term` 추가. 학기 결정 순서를 **요청 지정 > 부서 기본 학기 > 오늘 기준**으로 바꾸고, 부서 스코프 조회(부서 수합·부서 수업 시간·수업 조교)와 **학생 본인 화면**(가능 시간·수업 시간 조회/저장)에 적용했다 — 담당자가 수합하는 격자와 학생이 보는 격자가 다른 학기면 "냈는데 안 보인다"가 된다. 저장은 학사 캘린더에 있는 학기만 받는다(없는 학기를 넣으면 그 부서 화면이 통째로 빈다). 시드는 test 부서 6·7·8을 `2026-2`로 넣고, 운영 부서는 NULL(기존 동작) 그대로다.
- **After**: 세 부서 모두 오늘 날짜와 무관하게 가을학기 기준으로 열리고, 같은 기간 생성이 **전부 미충원 0**으로 돌았다.

  | 부서 | 학생(교비) | 가능시간 | 개관(2주) | 생성 | 교비 2주 합계 / 상한 |
  |---|---|---|---|---|---|
  | 6 정보서비스팀-test | 10명(7) | 104건 314h | 156.0h · 312슬롯 | OPTIMAL 2.58s · 미충원 0 | **177.0h / 190h** |
  | 7 아텍-test | 20명(20) | 111건 504h | 90.0h · 180슬롯 | OPTIMAL 1.77s · 미충원 0 | **90.0h / 280h** |
  | 8 교육대학원 행정팀-test | 10명(10) | 30건 120h | 112.0h · 224슬롯 | OPTIMAL 2.55s · 미충원 0 | **194.0h / 200h** |

  회귀 11건 추가(우선순위 3단 · 학생이 소속 부서 학기를 따름 · 부서 없는 학생은 오늘 기준 · 엔드포인트 2건 · 정책 API 조회/저장/해제/잘못된 학기 400). 백엔드 **438 passed**(직전 427), 프런트 `npm run build` 통과.


## 2026-08-29 — 수업 조교 부서: 과목 단위 배정 축 (#173)

- **문제/가설**: 기존 모델은 "개관 시간 격자에 사람을 채우는" 형태라 수업 조교를 담지 못한다. 근무 시간이 과목 시간표에서 나오고, **같은 시간에 여러 과목이 열리므로** 슬롯별 인원(#171)으로는 "과목마다 TA 1명"을 표현할 수 없다고 봤다. 2026-2 아텍 개설 과목으로 실제로 그런지부터 셌다.
- **테스트 조건**: SAINT '개설교과목정보' 내려받기 파일(2026-2, 지식융합미디어대학 43과목)을 `scripts/import_courses.py`로 파싱해 시드에 넣고, 검증용 DB(`stream_seed_check`)에 전체 시드를 다시 주입했다. API는 `tests/test_course_ta_api.py`(부서 7 · 학생 3명 최소 환경)로 확인했다.
- **Before**: 과목이라는 개념 자체가 없었다. 같은 시간에 몇 과목이 겹치는지 세어 보니 2026-2 기준 최대 동시 **8과목**(금 10:30, 단과대 전체) — 아텍 과목만 세도 **4과목**(AAT3007·AAT3009·AAT4006·AAT4011)이 같은 시각에 열린다. 시간 격자 하나로는 구분되지 않는 조합이다.
- **수정 내용**:
  - 모델 3개 추가 — `course`(학기·과목번호·분반·개설 학과·교수·수강생수), `course_meeting`(요일별 수업 시간 = TA 근무 시간), `course_ta`(과목↔학생 배정).
  - 임포트 스크립트 — SAINT 파일은 확장자만 .xls이고 실제로는 HTML 표라, 표준 라이브러리로 파싱해 시드 CSV로 뽑는다. 수업 시간이 없는 과목은 근무 시간을 만들 수 없어 건너뛰고 목록으로 알린다.
  - API 4개(`/api/course-ta/...`) — 조회·후보·배정·해제. 배정은 담당자가 직접 하되 **막아야 할 것은 서버가 막는다**: 본인 수강 시간 겹침, 이미 맡은 과목과의 겹침, 과목 수 상한(2), 주간 근로시간 상한. 후보 조회와 배정이 같은 판정 함수(`_blocking_reason`)를 쓰게 해 화면과 서버가 다른 답을 내지 않게 했다.
  - 주간 근로시간은 **법정 상한**으로만 본다 — 아텍의 부서 상한 주 7시간은 과 사무실 대기 근무 몫이라, TA 시간에 그대로 걸면 실제 운영보다 좁아진다.
  - 화면 — 관리자 '수업 조교'. 학과를 고르면 개설 과목이 주간 시간표로 정리되고, 겹치는 과목은 열을 나눠 배치한다(금 10:30에 8과목이 한 칸에 겹친다). 과목을 누르면 배정된 TA와 후보 학생이 사유와 함께 보인다.
- **After**: 과목 **43개** · 수업 시간 **68줄**(아텍 18 · 지식융합미디어대학 23 · 글로벌한국학부 2)이 들어갔고, 시드가 본인 수강 시간과 겹치지 않는 TA 배정 4건을 함께 넣는다. API 회귀 **13건**(조회·학과 필터 / 배정 성공 / 본인 수업 겹침 400 · 맡은 과목 겹침 400 · 과목 수 상한 400 · 타 부서 학생 400 · 중복 400 · 타 부서 스코프 403 / 해제·404 / 후보 판정과 배정 결과 일치). 백엔드 **440 passed**(#172 시점 427), 프런트 `npm run build` 통과.
  - 실제 화면(부서 7 · STF011 로그인)에서 확인하며 세 가지를 더 고쳤다. ①오늘(8/29)이 방학이라 학기가 `2026-summer`로 잡혀 **과목 0개**로 보였다 → 학기를 지정하지 않았고 그 학기에 과목이 없으면 과목이 있는 최근 학기로 바꿔 쓰고(`term`으로 알림), 학기 선택기를 붙였다. ②학과 기본값이 '과목이 가장 많은 학과'(지식융합미디어대학 23과목)라 아텍 조교에게 엉뚱한 학과가 먼저 떴다 → 부서명에 들어 있는 학과(아트&테크놀로지학과 18과목)를 먼저 연다. ③세로 범위를 09:00~22:00 고정으로 그려 저녁 수업이 없는 학기엔 화면의 3할이 빈 칸이었다 → 실제 수업 시간에서 범위를 뽑는다(이 학기는 09:00~18:00).
  - 화면 실측: 아텍 18과목 · TA 배정 4과목 · 주간 TA 근무 합계 10.3시간. Visual Design(월·수 12:00~13:15)에 김서진 배정 → 5과목 · 12.8시간(+2.5)으로 갱신, 해제 후 원상 복구. 후보 20명에 대해 화면의 비활성 처리와 서버 판정이 **불일치 0건**이었다(막힌 학생 12명 — 본인 수강 겹침 11 · 이미 배정 1).
  - 아직 남은 것: TA 배정을 확정 근무표(`work_schedule`)로 펼치는 것과, 대기 근무 솔버가 TA 시간을 이미 근무 중으로 보게 하는 것. 지금은 두 근무가 서로를 모른다.

## 2026-08-29 — 실사용 테스트 부서 2곳 시드 (#172)

- **문제/가설**: 검증 부서가 정보서비스팀-test(6) 하나뿐이라, 운영 시간·근무 형태가 다른 부서에서 수합·생성이 도는지 확인할 길이 없었다. 특히 (1) 개관 시간이 요일마다 다른 부서(교육대학원 행정팀: 월~목 22시, 금 17시), (2) 공휴일에 아예 쉬고 시험 주말 연장도 없는 부서(학과 사무실·행정팀)가 지금 정책 스키마로 표현되는지가 확인 대상이었다. 정책 파일은 `semester_public_holiday`·`exam_weekend`를 **필수 구간**으로 받으므로, 주말이 폐관인 부서도 시험 주말엔 열리는 값이 강제된다고 봤다.
- **테스트 조건**: 사용자 DB를 건드리지 않도록 별도 DB(`stream_seed_check`)를 만들어 `seed_mock_data.py --reset` 전체 시드를 넣고, 부서 7·8에 대해 2026-09-07~09-20(가을학기 2주차부터 2주) `generate_schedule`을 돌렸다(`backend/.venv/bin/python3`). 개관 슬롯 수는 같은 기간의 `OpeningHoursResolver.resolve`로 셌다.
- **Before**: 부서 7·8이 존재하지 않았다. 정책 스키마로는 `exam_weekend`를 비울 수 없어, 토·일 폐관 부서도 시험 주말(10/24 토)엔 `resolve()`가 연장 구간을 돌려준다 — 학과 사무실이 시험 기간 토요일에 문을 여는 근무표가 나온다.
- **수정 내용**:
  - 정책 파일 두 개 추가 — `aat_department_office.json`(학기 평일 09:00~18:00, 3시간 블록 3개, 인원 1/1, 방학 전 요일 폐관), `grad_edu_admin.json`(학기 월~목 10:00~22:00·금 09:00~17:00, 방학 월~금 10:00~17:00, 자유 30분 그리드, 인원 1/2).
  - `semester_public_holiday`·`exam_weekend`에 null을 허용했다 — null이면 각각 "공휴일·교내 휴강일 폐관", "시험 주말 연장 없음(평소 요일 규칙 그대로)". 도서관처럼 공휴일에도 여는 부서만 구간을 갖는다.
  - 시드: 부서 7(직원 STF011, 교비 20명 `20262001`~`20262020`)·부서 8(직원 STF012, 교비 10명 `20262021`~`20262030`), 공고 8·9. 아텍 학생의 수업 시간표는 **2026-2 실제 개설교과목**(SAINT 내려받기)에서 학생당 4과목씩 뽑아 넣었고, 가능 시간은 그 수업과 겹치지 않는 근무 블록만 담았다.
  - `--only`가 부서 하나씩 붙이는 공용 경로가 되도록 `seed_test_department`를 부서 스펙 기반으로 일반화했다 (`--only test-dept | aat-dept | grad-edu-dept`).
- **After**: 전체 시드 정상(부서 8 · 직원 8 · 학생 52 · 공고 9). 두 부서 모두 **미충원 0**으로 생성됐다.
  - 부서 7: 가능시간 111건(504h) · 수업 128건 · 개관 2주 180슬롯(90.0h) → `OPTIMAL`, solve **1.56s**, 배정 24건, 배정 슬롯-인원 **180/180(100%)**, 배정받은 학생 15/20명, 2주 최다 12.0h(주 6h ≤ 부서 상한 7h).
  - 부서 8: 가능시간 30건(120h) · 수업 24건 · 개관 2주 224슬롯(112.0h) → `OPTIMAL`, solve **2.67s**, 배정 74건, 배정 슬롯-인원 **388/224**(야간 선호 2명 반영), 학생 10/10명 배정, 2주 최다 24.0h(주 12h ≤ 교비 14h).
  - 회귀 10건 추가(명단·학번 대역·정책 파일 연결 / 개관 커버리지: 아텍 블록 15개 전부·교육대학원 슬롯 전부 가능자 ≥1 / 수업 ↔ 가능 시간 비겹침 / 공휴일·시험 주말 폐관). 백엔드 **427 passed**(수정 전 417, live LLM 테스트 2파일 제외).


## 2026-08-29 — 근무 슬롯별 배정 인원 (#171)

- **문제/가설**: 배정 인원(`min_per_slot`/`max_per_slot`)이 부서에 하나뿐이라 모든 슬롯에 같은 값이 걸린다. 수업 시간대마다 필요한 조교 수가 다른 부서(학과 출석체크 등)는 표현할 방법이 없다. 근무 슬롯(#89)이 이미 "이 부서의 근무 단위"를 정의하므로, 블록에 인원을 붙이면 기존 제약 구조를 그대로 두고 슬롯별 인원을 표현할 수 있다고 봤다.
- **테스트 조건**: `tests/scheduler/test_work_slot_blocks.py`의 최소 정책(코드 구성, 30분 슬롯) — 학기·방학 평일 09:00–13:00 개관, 블록 3개(09:00–10:30 / 10:30–12:00 / 12:00–13:00), 전 시간대 가능한 학생 4명(A~D, 교비), 5일. 부서 기본 인원 최소 1명·최대 1명 고정. 같은 정책·같은 학생으로 **블록 인원 미설정(= 기존 동작)** 과 **09:00–10:30 블록만 2~2명** 두 번 풀었다(`backend/.venv/bin/python3`).
- **Before**: `OPTIMAL`, solve 1.002s, objective 452, 미충원 0. 월요일 슬롯별 인원 `09:00~12:30 전 구간 1명`, 5일 총 배정 슬롯-인원 **40**.
- **수정 내용**:
  - 도메인: 근무 블록을 `(시작, 종료)` 튜플에서 `WorkSlotBlock`(+`min_per_slot`/`max_per_slot`, None이면 부서 기본값)으로 바꾸고, 슬롯→인원 판정을 `resolve_slot_staffing()` 한 곳에 뒀다. 솔버(`StaffingBoundsConstraint`)·사후 검증(`verify._check_staffing`)·미충원 보고(`SlotShortage.required`)가 같은 함수를 쓴다.
  - 저장·API: `department_policy.work_slots` JSONB가 기존 `["08:00","09:00"]`과 인원이 붙은 `{"start","end","min_per_slot","max_per_slot"}`를 함께 읽는다(마이그레이션 없음, 인원을 정한 블록만 dict로 저장). PATCH는 블록 인원이 부서 기본값과 합쳐 성립하지 않으면 400 — 부서 최대 인원을 저장된 블록의 최소 인원 아래로 낮추는 요청도 같이 막는다.
  - 화면: 부서 설정 > 근무 슬롯에서 블록을 클릭해 그 블록만 인원을 정한다. 설정한 블록은 카드에 인원이 표시되고, 블록을 나누거나 합치면 그 설정은 사라진다(부서 기본값으로 복귀).
  - 미충원 구간 병합(`merge_shortage_blocks`)과 검증 위반 메시지는 기준 인원이 같은 슬롯끼리만 합친다 — 2명 기준 구간과 1명 기준 구간이 한 줄로 붙으면 어느 쪽 기준인지 알 수 없다.
- **After**: 같은 조건에서 `OPTIMAL`, solve 0.499s, objective 382, 미충원 0. 월요일 슬롯별 인원 `09:00·09:30·10:00 = 2명`, `10:30~12:30 = 1명`(부서 기본값), 5일 총 배정 슬롯-인원 40 → **55**. 블록 경계에서만 인원이 갈리고 나머지는 그대로다.
  - 화면 실측(부서 6 · STF010 로그인, 검증용 DB): 부서 설정 > 근무 슬롯에서 월 09:00–10:30 블록을 눌러 2~2명으로 저장 → 카드에 '2명' 표시, `GET /api/schedule/policy/6`의 그 블록만 `min/max_per_slot=2`이고 이웃 블록은 `null`(부서 기본 1~2명) 그대로였다. 같은 DB로 2026-09-07 주를 생성해 보니 블록 2~2명일 때 월 09:00~10:30이 `[2,2,2]`인데 이웃도 `[2,2]`·`[2,2,2]`라(선호 인원 2명 때문) 구분되지 않아, 블록 **최대 1명**으로 조여 다시 돌렸다 → 그 블록만 `[1,1,1]`, 이웃은 `[2,2]`·`[2,2,2]` 그대로. `OPTIMAL` 1.94s · 미충원 0. 확인 후 저장값은 시드 상태로 되돌렸다.
  - 회귀: 블록 인원 4건(블록 최소가 부서 기본값을 덮음 · 블록 최대가 부서 최대보다 낮게 조임 · 미충원 `required`가 블록 기준 2명 · 블록 미정의 요일은 부서 기본값) + API 5건(저장·조회, 블록 내 최소>최대 422, 부서 최대와 충돌 400, 부서 인원 하향 400, 개관만 재저장해도 인원 유지). 백엔드 전체 **426 passed / 23 skipped**(수정 전 417 passed / 23 skipped — live LLM 테스트를 건너뛴 환경). API 키를 넣고 돌리면 443 passed / 1 failed / 5 errors인데, 실패·에러는 모두 이 변경과 무관한 기존 항목(live LLM 응답 판정, `test_chat_live.py`의 옛 픽스처)이다. 프론트엔드 `npm run build` 통과.


## 2026-08-29 — 국가 근로 월 상한이 기간을 나눠 생성하면 뚫리던 문제 (HC-TIME-3 이월 차감)

- **문제/가설**: 월 46시간 상한(HC-TIME-3)은 hard로 등록돼 있지만 **스케줄링 기간 안의 날짜만** 집계한다(SPEC에 미구현으로 표시돼 있던 항목). 생성은 보통 2주씩 끊으므로, 같은 달을 두 번 생성하면 각 회차는 상한 안이어도 월 합계가 넘을 것이라고 봤다.
- **테스트 조건**: 부서 2(정보서비스팀). 2026-08-31~09-13을 확정해 둔 상태에서, 같은 9월의 다음 2주(09-14~09-27)를 새로 생성하고 국가 학생의 9월 합계를 셌다.
- **Before**: 두 번째 생성이 `OPTIMAL`로 통과하고 9월 합계가 **김현서 58.0h · 조수현 63.0h** — 상한 46h를 각각 12h·17h 넘겼다. 어느 회차도 자기 기간 안에서는 상한을 어기지 않아 아무 데서도 걸리지 않는다.
- **수정 내용**: `Student.prior_monthly_hours`(연·월 → 기간 밖 기근무 시간)를 추가하고, `service._prior_monthly_hours`가 draft·confirmed·manual 배치에서 **기간 밖 날짜만** 모아 채운다. 기간 안은 이번 결과로 대체되므로 세지 않는다(같은 기간의 기존 draft를 이중 집계하지 않는 효과도 있다). 상한이 학생 개인에게 걸리는 값이라 **부서를 가리지 않고** 합산한다. `MonthlyGukgaLimitConstraint`는 `상한 − 기근무`만큼만 이번 기간에 배정하도록 바꿨다(이미 넘겼으면 0).
- **After**: 같은 재생성이 `OPTIMAL` · 미충원 **0슬롯**이면서 9월 합계가 **김현서 46.0h · 조수현 46.0h** — 정확히 상한에 붙고 넘지 않는다. 커버리지 손실 없이 제약만 지켜졌다. 회귀 7건 추가(기간 밖만 집계 · draft/manual 포함 · superseded 제외 · 타 부서 합산 · 달별 분리 · 로더가 실제로 넘기는지). 백엔드 424 passed.
  - 남은 것: 주간 상한(HC-TIME-1/2)과 부서 2주 총합(HC-TIME-4)은 여전히 기간 안에서만 본다. 주 경계에 맞춰 생성하면 주간은 자연히 맞지만, 1주짜리 배치를 두 번 확정하면 부서 2주 총합은 아무도 검사하지 않는다.


## 2026-08-29 — 대타 데모가 성립하도록 부서 여력 확보 · 데모 생성 단위를 2주로 (#164)

- **문제/가설**: #159에서 대타 승인에 주간 상한 검사를 넣자 시드의 대타 요청이 **4건 → 0건**이 됐다. 솔버가 `SC-FAIR-1`로 부서 인원 전원을 상한까지 채워 대타 여력이 0이 되기 때문이다. 근본 원인은 부서 인력이 개관 수요보다 적은 것이라고 보고, 인원을 늘리면 여력이 생기는지 확인했다.
- **테스트 조건**: 부서 2(로욜라도서관 정보서비스팀), 다음 주 시작. 시드가 솔버로 확정 근무표를 만들고 실제 후보 탐색(`_find_candidates`)으로 대타 4건을 얹은 뒤, `verify_batch`로 채점하고 승인 API를 직접 눌러 확인했다. 부서 운영 상한은 비활성 상태(법정 상한만 적용).
- **Before**: 근로 학생 9명 · 1주 생성. 인력이 낼 수 있는 배정 시간 합계 138시간 < 개관 수요(최대 150시간)라 솔버가 100% 소진 → **전원 여력 +0.0h**, 대타 요청 **0건**.
- **수정 내용**: (1) 근로 학생 2명 추가(9 → 11명, 가능 시간은 저녁·오전으로 나눠 서로 다른 시간대를 덮게). (2) 시나리오별로 여력 요구를 나눴다 — 시간이 실제로 옮겨가는 ②수락·③승인만 대타 학생의 여력을 확인하고 예약분을 반영하며, ①대기·④반려는 후보만 있으면 된다. 짧은 근무부터 고른다(여력은 몇 시간 단위인데 근무는 3~8시간이라 긴 것부터 집으면 설 사람이 없다). (3) 데모 생성 단위를 **1주 → 2주**로 바꿨다.
- **After**: 대타 요청 **4건** 복구. `OPTIMAL` solve 5.84s, 근무 55건, 개관 153.0h 중 **306/306 슬롯 100%** 최소 인원 충족, 배정 시간 합계 269.5시간, 솔버 배정 제약 위반 **0건**. 데모 흐름을 실제로 눌러 확인 — ①대기 후보 조회 정상, ②수락 건 승인 **200**이고 확정본 critical **0 → 0건**(승인해도 위반이 안 생긴다), ③승인은 이미 반영된 상태, ④반려는 사유 표시. 백엔드 417 passed.
  - 생성 단위를 2주로 바꾼 이유: 1주만 만들면 **HC-TIME-4**(부서 2주 교비 총합 190h)의 14일 창에 7일만 들어가 제약이 사실상 걸리지 않는다. 실측으로도 1주 생성의 교비는 115.0h인데 같은 패턴이 2주 반복되면 230h로 상한을 넘는다. 2주로 생성하니 교비가 **정확히 190.0h**(상한 190h)로 붙고 미충원은 0슬롯이었다 — 제약이 binding이면서도 커버리지 손실이 없다.
  - 남은 한계: HC-TIME-4는 **생성 기간 안에서만** 걸린다. 1주짜리 배치를 두 번 확정하면 부서 2주 총합을 검사하는 곳이 없다(확정도 검증기도 배치 단위). SPEC이 2주 단위 생성을 권장하는 이유가 이것이고, 시드도 이제 그 단위를 따른다.


## 2026-08-29 — 담당자 수합 표가 방학 주에도 학기 개관 시간으로 그려진다 (#160)

- **문제/가설**: 방학인 2026-08-31(월)의 08:00~09:00·20:00~22:00 칸이 빗금 '근무 없음'이 아니라 흰색 '가능자 없음'으로 보인다. 담당자 화면(`AdminSchedulePage.jsx`)만 `opening_hours.semester`를 하드코딩해 읽고, 근무 슬롯도 `blocksByDayLabel(policy)`처럼 기간 인자 없이 부르는 것이 원인이라고 봤다. 같은 판정을 학생 화면은 `periodByDayOfWeek`로 이미 하고 있다.
- **테스트 조건**: 시드 DB(부서 2, 선발 9명) · 담당자 계정 STF001 · `근로 시간표 → 수합된 근무 시간표 → 특정 주` 2026.08.31~09.06. 이 주는 8/31만 방학, 9/1부터 학기라 한 주가 두 기간에 걸친다(부서 정책: 월 학기 08:00~22:00 11블록 / 방학 09:00~20:00 7블록). 수정 전(dev :5173)과 수정 후(dev :5174)를 같은 주·같은 계정으로 열고, 표 DOM에서 요일별 '근무 없음' 칸 수와 병합 블록 수를 세어 비교했다.
- **Before**: 월(08.31) — 근무 없음 **0칸**, 병합 블록 **11개**(= 학기 기준). 화~금(09.01~09.04) 0칸/11개, 토 12칸/3개, 일 28칸/0개. 즉 방학 월요일이 학기 요일과 완전히 같은 모양으로 그려졌다.
- **수정 내용**: 기간 인식 개관 시간 조회를 `utils/workSlots.js`의 `openRangeLookup(policy, periodByDay)`로 공용화하고(`closedSlotKeys`도 이 함수를 쓰게 해 학생 화면과 규칙이 갈리지 않게 함), 담당자 화면이 `periodByDayOfWeek`(특정 주)·`uniformPeriodByDay`(매주 반복 패턴, 보고 있는 학기 기준)로 계산한 `periodByDay`를 수합 표·검토 단계 표·확정 근무표 표의 `blocksByDayLabel`과 `openRangeLookup`에 넘기도록 했다.
- **After**: 동일 주·동일 계정에서 월(08.31) — 근무 없음 **6칸**(08:00·08:30·20:00·20:30·21:00·21:30), 병합 블록 **7개**(= 방학 기준). 화~금 0칸/11개, 토 12칸/3개, 일 28칸/0개로 **학기 요일은 그대로**. `npm run build` 통과(1652 modules, 2.15s).


## 2026-08-29 — 학생팀장에게 학생 화면이 하나도 열리지 않았다 (#156)

- **문제/가설**: 학생팀장으로 로그인하면 근무표 편성 화면만 떴다. `AdminShell`이 팀장 메뉴를 `adminMenu.filter(['schedule'])`로 만들어 관리자 메뉴에서 한 칸만 남긴 구조였는데, 팀장도 근로 학생이라 공고·관심 공고·공통 지원서·내 지원 현황·본인 근무 시간표·대타 요청이 모두 필요하다. 사이드바에 그 항목들이 아예 없었다.
- **테스트 조건**: 로컬 dev 서버(backend 8000 + frontend 5173), 시드 계정 3종으로 로그인해 사이드바 항목 수와 화면 이동을 확인했다 — 학생팀장 `20261005`(김찬우), 일반 근로학생 `20220042`(김현서), 직원 `STF001`(박정보).
- **Before**: 팀장 사이드바 **1개**(근무표 편성), 로그인 착지 `/admin/schedule`. 학생 화면 6개는 주소를 직접 입력해야만 열렸다 — 화면 안에 진입 경로가 없었다.
- **수정 내용**: `teamLeadMenu = streamMenu + 근무표 편성(leadSchedule)`을 두고 `Shell`·`AdminShell`이 팀장에게 같은 사이드바를 그리도록 했다(학생 라우트 표는 `Shell`에서 export해 공유). 로그인 착지도 일반 학생과 같은 `/home`. 편성 화면은 학생 메뉴의 `schedule`(본인 시간표 조회)와 겹치지 않게 별도 id로 뒀고, 관리자 메뉴 워딩도 '근로 시간표' → '근무표 편성'으로 통일했다. 권한 범위는 그대로 — `TEAM_LEAD_MENUS = ['schedule']` 가드가 남아 팀장이 주소로 다른 관리자 화면에 들어가면 편성 화면으로 되돌아간다 (PR #163).
- **After**: 팀장 사이드바 **1개 → 7개**, 편성 ↔ 학생 화면 왕복 이동 확인. 일반 학생은 **6개로 종전과 동일**(회귀 없음), 직원은 6개 그대로에 라벨·아이콘만 바뀌었다. `npm run build` 통과. 백엔드 변경 없음.


## 2026-08-29 — 대타 승인이 주간 상한을 넘기던 경로 차단 (#159)

- **문제/가설**: `PATCH /substitute-requests/{id}/approve`가 대타 학생의 주간 근로 시간 상한을 검사하지 않았다. 승인은 근무 담당자를 실제로 바꾸므로 그 학생의 주간 시간이 늘어나는데, 확정·수동 등록·draft 편집이 모두 쓰는 `_check_weekly_hour_limits`를 이 경로만 부르지 않았다. 후보 탐색(`_find_candidates`)도 가능 시간과 근무 겹침만 봐서, 화면에는 설 수 없는 후보가 그대로 떴다.
- **테스트 조건**: 시드 주(2026-08-31~09-06, 부서 2, 9명)의 확정 근무표에서 `수락` 상태 요청을 승인하고, `GET /api/schedule/verify`로 배치를 채점해 전후를 비교했다. 회귀 테스트는 sqlite 픽스처(4시간 근무 1건, 교비 상한 14시간)로 경계값을 고정했다.
- **Before**: 승인 요청이 **200으로 통과**하고 확정 근무표의 critical 위반이 1 → 2건으로 늘었다 (`HC-TIME-1`, 대타 학생 주간 15.5시간 / 상한 14시간). 우연이 아니라 구조적이다 — 솔버가 `SC-FAIR-1`로 **전원을 상한에 정확히 맞춰** 채우므로(교비 14.0h·국가 20.0h) 그 주 대타 여력이 0이고, 어떤 후보를 골라도 승인하면 초과한다.
- **수정 내용**: 주간 상한 규칙을 `routers/schedule.py`에서 `app/work_hours.py`로 옮겨 공용으로 만들고(동작 변경 없음 — 옮긴 직후 404 passed 확인), 대타 경로 두 곳에 적용했다. (1) `_find_candidates`가 남은 여력이 요청 구간보다 적은 학생을 후보에서 제외한다. (2) `approve`가 승인 직전에 `check_weekly_hour_limits`를 호출해 넘으면 400. 후보에서 걸러도 승인에서 다시 보는 이유는, 목록을 본 시점과 승인 시점 사이에 그 학생 근무가 늘 수 있고 수락은 학생이 직접 하기 때문이다.
- **After**: 같은 승인 요청이 **400**으로 막히고 근무 담당자·요청 상태가 그대로 남는 것을 확인했다. 회귀 테스트 7건 추가 — 상한 도달 학생이 후보에서 빠지는지, 여유가 있으면 종전대로 승인되는지(과잉 차단 아님), 요청자 시간이 아니라 **대타 학생** 시간 기준인지, 부분 대타(#123)는 넘기는 구간만큼만 세는지(경계: 12h+2h=14h 통과 / 13h+2h=15h 차단). 백엔드 411 passed.
  - **부작용**: 시드의 대타 데모가 요청 0건이 됐다. 그 주는 여력이 0이라 후보가 없는 게 맞는 동작이라, 시드가 학생별 여력을 출력해 이유를 드러내도록 했다. 데모가 성립하려면 부서에 여력이 있어야 하고, 이는 데모 데이터 결정이라 별도로 다룬다.
  - **함께 드러난 것**: 여력 출력에서 국가 학생 2명이 **-5.0h**로 나왔다. 솔버는 정책 JSON의 법정 상한(국가 20h)만 쓰고 `department.weekly_hour_limit`(부서 2는 15h)는 제약으로 쓰지 않는데, `confirm`은 그 상한을 검사한다. 실제로 `POST /schedule/generate` 200 → `POST /schedule/confirm` **400 "부서 운영 상한 주 15시간을 초과합니다"** 로 재현된다 — **생성은 되는데 확정이 안 되는 상태**다. 예전 시드는 확정 배치를 DB에 직접 넣어 이 경로를 타지 않아 가려져 있었다. 별도 이슈로 다룬다.


## 2026-08-29 — 생성은 되는데 확정이 안 되던 근무표 (#161)

- **문제/가설**: #159 작업 중 학생별 대타 여력을 출력했더니 국가 근로 학생 2명이 **-5.0h**로 나왔다. 이미 상한을 넘겨 배정돼 있다는 뜻이라 추적했다. 가설: 솔버는 정책 파일의 법정 상한(교비 14h·국가 학기 20h)만 제약으로 쓰는데, 확정(`_check_weekly_hour_limits`)은 거기에 더해 `department.weekly_hour_limit`(정보서비스팀은 **15h**)도 검사한다 — 두 기준이 어긋나면 솔버 산출물을 확정할 수 없다.
- **테스트 조건**: 부서 2(로욜라도서관 정보서비스팀, 근로 학생 9명), 2026-09-07~09-13 7일. `POST /api/schedule/generate` 응답의 `schedules`를 그대로 `POST /api/schedule/confirm`에 넣어 왕복시켰다.
- **Before**: `generate` **200** `OPTIMAL` 배정 30건 → `confirm` **400 "해당 학생은 부서 운영 상한 주 15시간을 초과합니다."** 국가 학생 2명이 각 20.0h(법정 상한)로 배정돼 부서 상한 15h를 넘었다. 배정 시간 합계 138.0시간. **부서 2에서 생성 → 확정 플로우가 완결되지 않는 상태.** 예전 시드가 확정 배치를 DB에 직접 넣어 이 경로를 타지 않아 가려져 있었다.
- **수정 내용**: `service.apply_department_overrides`에 `_apply_department_weekly_limit`을 추가해, 부서 운영 상한이 있으면 정책의 주간 상한을 `min(법정, 부서)`로 좁힌다. 부서 상한은 `department_policy`가 아니라 `department` 테이블에 있어 정책 행이 없어도 적용되도록 이른 시점에 건다. 완화는 하지 않는다 — 부서 값이 법정보다 높으면 법정이 그대로다.
- **After**: 같은 왕복이 `generate` **200** `OPTIMAL` solve 2.99s 배정 28건 → `confirm` **201** (`confirmed_count: 28`). 국가 학생이 각 15.0h로 줄어 배정 시간 합계 128.0시간. **개관 156슬롯(78.0h) 중 156슬롯 100% 최소 인원 충족, 미충원 0** — 상한을 좁혔지만 커버리지 손실은 없었고, 줄어든 10시간은 2명 배치 구간에서 빠졌다(`preferred_staffing` 페널티 120). 회귀 6건 추가 — 좁히기·완화 안 함·부서 값 없음·**솔버 상한과 확정 상한이 같은지**(불변식)·솔버 상한까지 채운 주가 확정 검사를 통과하는지. 백엔드 410 passed.


## 2026-08-29 — 시드 확정 근무표를 솔버 산출물로 교체 · 대타 승인이 주간 상한을 넘긴다 (#156)

- **문제/가설**: 이 이슈의 출발점이 "확정 근무표가 꽉 차지 않는다"였고, 원인은 시드가 확정 배치를 **손으로 7행** 적어 넣은 것이었다. `solver_summary`가 없어 제약을 지켰는지 확인할 방법이 없고, 기간도 월~금이라 주간 뷰(월~일)와 어긋나 토요일 개관이 배치에 없는 것처럼 보였다. 솔버로 생성해 확정하면 커버리지가 얼마나 오르는지, 그 위에 대타 데모를 얹었을 때 확정본이 여전히 제약을 지키는지 확인했다.
- **테스트 조건**: 시드 실행 시점에 `generate_schedule(department_id=2, start_date=다음 주 월요일, num_days=7)`을 호출해 결과를 그대로 `confirmed` 배치로 저장. 대타 데모 4건(대기·수락·승인·반려)은 배정 결과에서 **실제 후보 탐색 로직**(`_find_candidates`)을 태워 "대타 설 사람이 있는" 근무만 골라 얹었다. 채점은 `scheduler/verify.py`로 **솔버 배정 직후**와 **대타 반영 후** 두 번. 실행일 2026-08-29 기준 대상 주는 2026-08-31~09-06.
- **Before**: 수기 시드 — 근무 7행, 배정 시간 합계 21.0시간, 기간 08-31~09-04(금). 개관 슬롯 134개(67.0h) 중 최소 인원 충족 **42개(31.3%)**. `solver_summary` NULL이라 검증기가 `PROVENANCE` 경고 + `SC-UNDER-1` 9건을 냈다(critical 0 — 규정 위반은 아니고 개관 시간의 2/3이 빈 상태).
- **수정 내용**: 시드가 솔버를 호출해 확정 배치를 만들도록 교체하고, 기간을 **일요일까지**로 넓혔다. 대타 데모 근무는 하드코딩을 걷어내고 배정 결과에서 날짜를 흩어 고르며, 대타자는 그 주 배정이 가장 적은 후보를 쓴다. 시드가 끝나면 확정본을 스스로 채점해 커버리지와 위반 건수를 출력한다.
- **After**: `OPTIMAL`, solve 5.87s. 근무 30행, 배정 시간 합계 138.0시간, 기간 08-31~09-06(일). 개관 슬롯 150개(75.0h) 중 최소 인원 충족 **150개(100.0%)** — 31.3% → 100%. **솔버 배정 자체의 제약 위반 0건.** 대타 요청 4건 정상 생성(대기 요청의 후보 5명 조회 확인). 백엔드 테스트 404 passed.
  - **다만 대타 승인 1건을 반영하면 위반이 0 → 1건이 된다** (`HC-TIME-1`, 대타자 주간 19.5h / 상한 14h). 이 주는 솔버가 SC-FAIR-1로 **9명 전원을 상한에 정확히 맞춰** 채워서(교비 14.0h·국가 20.0h, 여유 0h) 어떤 후보를 골라도 승인하면 초과한다. 원인은 시드가 아니라 **`PATCH /substitute-requests/{id}/approve`가 대타자의 주간 상한을 검사하지 않는 것** — 승인 한 번으로 확정 근무표가 규정 위반 상태가 될 수 있다. 같은 검증(`_check_weekly_hour_limits`)을 확정·수동 등록·draft 편집은 이미 하고 있고 대타 승인만 빠져 있다. 별도 이슈 #159로 옮겼다.
  - CP-SAT는 동률 해를 여러 개 가질 수 있어(SPEC 3.7) 시드를 다시 돌리면 배정과 대타 대상 학생이 달라질 수 있다. 위 수치는 관측된 한 실행 기준이다.


## 2026-08-29 — 실측 수합 데이터를 시드로 옮기다 날짜 예외 인코딩 결함 2건 발견 (#156)

- **문제/가설**: 운영 스프레드시트(정보서비스팀 10명 × 2주)를 그대로 담은 테스트 부서(`정보서비스팀-test`, department 6)를 시드로 만들었다. 시트는 **날짜 단위**인데 DB는 **학기별 주간 패턴 + 날짜 예외** 구조라 변환이 필요하다. "옮겨 넣은 값을 로더로 다시 펼치면 원본 시트와 같은가"를 확인했다 — 다르면 화면엔 냈는데 배정이 안 되거나, 내지 않은 희망이 생긴다.
- **테스트 조건**: 전사본(`scripts/seed_data/test_dept_availability.csv`, 학생 10명 × 12일)을 주간 패턴(2주차 + 08/31)과 1주차 날짜 예외로 나눠 담고, `materialize_availability`로 2026-08-31~09-13을 다시 펼쳐 원본과 구간·선호도까지 대조. 전사 자체의 정확도는 시트가 계산해 둔 '근무 가능 시간' 행(120일치)을 체크섬으로 썼다.
- **Before**: 전사 체크섬은 **120일 전부 일치**(전사 오류 0건). 그런데 왕복 대조에서 **6칸 불일치** — 이서연 09/01~09/04, 김다은 09/04, 안찬호 09/02. 원인 두 가지:
  1. `loader/availability.py`의 `_add`가 **맞닿기만 한 구간까지 병합**하고 새 선호도를 씌운다. "가능 10:30-13:30 + 희망 13:30-15:00"이 하나로 합쳐져 앞 3시간까지 희망이 됐다 — 학생이 내지 않은 희망이 만들어져 SC-PREF-1이 엉뚱한 시간대를 우선 배정한다.
  2. 같은 파일이 예외를 **정렬 없이** 적용한다. docstring은 "종일 UNAVAILABLE → 부분 UNAVAILABLE → AVAILABLE" 순서를 명시하는데 코드는 조회 순서를 그대로 따랐고, 호출부는 `order_by` 없이 조회한다. "그날 패턴을 지우고 그날 구간만 다시 넣는" 조합의 결과가 행 순서에 좌우된다.
- **수정 내용**: (1) `_add`가 실제로 **겹칠 때만** 병합하도록 좁혔다 — 맞닿기만 한 구간은 선호도가 같을 때만 합친다. (2) 예외를 문서화된 순서로 정렬해 적용(`_exception_order`). 두 동작 모두 회귀 테스트로 고정했고, 시드 왕복 대조도 테스트로 남겼다(`tests/test_seed_test_department.py`).
- **After**: 왕복 불일치 **6칸 → 0칸**. 새 부서로 근무표를 생성하니 `OPTIMAL`, solve 2.14s, objective 4,793. 개관 슬롯 306개(153.0h) 중 **303개(99.0%)** 최소 인원 충족, 배정 시간 합계 277.5시간(10명, 각 18.5~39.5h). 미충원 3슬롯은 09/01(화) 15:00~16:30 하나뿐이고 **후보 0명** — 그 시간에 10명 전원이 수업이라 시트 자체의 성질이지 시드 결함이 아니다(전사본에서 직접 확인). 백엔드 테스트 382 passed(실 LLM 호출 2개 파일 제외).


## 2026-08-29 — 확정 배치 제약 검증기: OPTIMAL이 곧 "제약 준수"는 아니었다 (#156)

- **문제/가설**: 앞 항목에서 솔버가 `OPTIMAL`을 내고도 잘못된 입력 위에서 푼 사례가 나왔다. 그렇다면 **상태값으로는 배정의 옳음을 판정할 수 없다**. 그런데 확정본을 채점할 경로가 없다 — AI 검토(`review.py`)는 `draft`만 보고, 솔버는 `work_schedule` 행을 아예 읽지 않는다. 가설: 확정본을 정책·캘린더·가용시간으로 다시 채점하면 지금 DB에 있는 배치에서도 위반이 나올 것이다.
- **테스트 조건**: 실 DB의 배치 2개 — `batch#1`(confirmed, 2026-08-31~09-04, 시드 수기 7행)과 `batch#6`(draft, 2026-08-31~09-13, 학기 경계 버그 수정 **전**에 생성된 솔버 산출물, `OPTIMAL` 2.35s). 새 `scheduler/verify.py`로 두 배치를 채점. 검사 항목은 HC-OPEN / HC-CLASS-1·6 / HC-STAFF-1·2 / HC-TIME-1~4 + 슬롯 중복·기간 밖 배정. HC-BLOCK-1은 부분 대타(#123)가 허용된 예외라 제외.
- **Before**: 확정본에 대해 자동으로 확인할 수 있는 것이 **0건**. `batch#1`은 `solver_summary`가 NULL이라 어떤 제약을 통과했는지 기록조차 없었고, `batch#6`은 `OPTIMAL`이라는 상태값만 있었다.
- **수정 내용**: `app/scheduler/verify.py` 추가 — 배치를 솔버와 같은 로더(`_load_students`, `OpeningHoursResolver`, 부서 정책)로 다시 채점하는 결정적 검증기(LLM 없음). `GET /api/schedule/verify?batch_id=`(직원 전용, 본인 부서만)로 노출. 유닛 14건 + API 4건 추가.
- **After**: 실측 —
  - `batch#1`: critical 0건, warning 10건(`PROVENANCE` 1 + `SC-UNDER-1` 9). 커버리지 42/134 슬롯(**31.3%**, 개관 67.0h 대비 배정 시간 합계 21.0시간). 즉 "규정 위반은 아니지만 개관 시간의 2/3이 빈" 배치임이 수치로 드러났다.
  - `batch#6`: **critical 13건 전부 `HC-CLASS-1`**(가용 시간 밖 배정) + warning 18건. 커버리지 252/306(82.4%).
  - 이 13건은 오탐이 아니다 — 예로 `20220042`의 2026-09-01(화) 15:00~16:30 배정을 DB에서 대조하니 가을학기(`2026-2`) 화요일 가용 시간은 08:00~15:00·16:30~18:00으로 그 구간이 비어 있었다. 여름학기(`2026-summer`) 값이 12:00~17:00이라 옛 로더에서만 배정 가능했던 슬롯이다. **검증기가 앞 항목의 학기 경계 버그를 독립적으로 다시 잡아냈다.**
  - 백엔드 테스트 375 passed(실 LLM 호출 테스트 2개 파일 제외).


## 2026-08-29 — 학기 경계를 넘는 생성 기간에서 개관 앞뒤 시간이 통째로 미배정 (#156)

- **문제/가설**: 08.31~09.06 주간 근무표가 화면에서 비어 보인다는 제보. 확인해보니 원인이 둘이었다. ① 화면의 "확정" 배치(`schedule_batch#1`)는 솔버 산출물이 아니라 `seed_mock_data.py`의 대타 데모 시드(수기 7행, `solver_summary` NULL)였다. ② 같은 시작일로 만든 솔버 draft(`batch#6`)는 `OPTIMAL`인데도 부족 슬롯 54건을 냈고, 그 54건이 **전부 `candidates: []`**였다. 가설: `_load_students_from_db`가 `resolve_term(None, period_start)`로 **생성 기간 시작일의 학기 하나**만 읽어 기간 전체에 적용하는데, 08-31은 `2026-summer` 마지막날이라 여름 가용 시간(09:00~20:00)이 09-01부터의 가을학기 날짜(개관 08:00~22:00)에도 붙는다.
- **테스트 조건**: 실 DB(정보서비스팀 department_id=2, 합격 학생 9명), `generate_schedule(start_date=2026-08-31, num_days=14)`, 정책 `library_info_service.json`(학기 평일 개관 08:00~22:00, `min_per_slot=1`). 커버리지는 `OpeningHoursResolver`로 뽑은 개관 슬롯 306개(153.0h) 대비로 셌다. 로더 단독 확인은 `_load_students_from_db` 직접 호출 후 2026-09-01의 슬롯별 `can_work` 가능자 수로 측정.
- **Before**: `OPTIMAL`, solve 2.35s, objective 56,664 (`understaffing` 54,000 = 54슬롯 × 1000). 최소 인원 충족 252/306 (**82.4%**), 부족 54슬롯 전부 후보 0명. 부족 시간대는 학기 평일 9일 × {08:00, 08:30, 20:00, 20:30, 21:00, 21:30}으로 정확히 일치. 로더 단독: 2026-09-01 개관 28슬롯 중 가능자 0명인 슬롯 6개.
- **수정 내용**: `services.py`에 `term_segments(period_start, period_end)`를 추가해 생성 기간을 학기 경계로 자르고, `scheduler/service.py::_load_students_from_db`가 구간마다 그 학기의 `available_time`을 읽어 `materialize_availability`를 돌린 뒤 날짜별로 합치도록 바꿨다. 학기 판정은 기존 `resolve_term`과 같은 규칙(`AcademicCalendar.term_for`)이라 기간이 한 학기 안에 들어오면 구간이 1개로 나와 동작이 그대로다. 회귀 테스트 2건 추가(`tests/scheduler/test_student_loader.py`) — 수정 전 `test_availability_follows_the_term_of_each_date`가 실패하는 것을 확인했다.
- **After**: 동일 조건 재실행 — `OPTIMAL`, solve 9.47s, objective **2,149** (`understaffing` 0). 최소 인원 충족 **306/306 (100.0%)**, 부족 슬롯 **0건**. 배정 시간 합계 269.5시간(Before 238.0시간). 잔여 페널티는 `preference_match` 1,215 / `meal_break` 420 / `contiguity` 236 / `preferred_staffing` 200 / `fair_hours` 78. 로더 단독: 2026-09-01 가능자 0명 슬롯 0개. 백엔드 테스트 377 passed / 1 failed(`test_review_live.py` — 실제 Gemini 호출 비결정 테스트, 이 변경과 무관).
  - solve time이 2.35s → 9.47s로 늘어난 건 Before가 08:00~09:00·20:00~22:00에 **변수 자체가 없어** 탐색 공간이 좁았기 때문이다. 제약 완화가 아니라 원래 풀어야 했던 문제로 돌아온 것.


## 2026-08-28 — 학기 solve 30초 타임아웃: 격차 한계 2% + 결정적 병렬로 7초·재현 보장 (#143·#132)

- **문제/가설**: 시드 현실화(#141) 후 학기 2주 생성이 30초 제한 도달(`FEASIBLE` 30.02s). 30초 시점 objective 2182 vs bound 2146(격차 1.65%)로 좋은 해는 일찍 찾으니, 병목은 해 탐색이 아니라 **최적성 증명**이고 시간 제한 상향은 무의미할 것으로 가정. 또 seed·워커 미고정이라 동일 입력에도 결과가 달라(#132), 기존 `test_free_grid_without_work_slots_is_identical`이 5회 중 1회꼴 간헐 실패.
- **테스트 조건**: `stream_bench143` DB에 시드 주입, 정보서비스팀(9명)·2026-09-07 시작·14일. 모델 크기: 배정 변수 1,770 / 전체 변수 4,332 / 제약 4,075 / 페널티 항 4,130(contiguity 1,770 + preference_match 1,628이 지배). ortools 9.15.
- **Before**: 학기 `FEASIBLE` 30.02s objective 2182. **시간 제한 600초로 올려도 `FEASIBLE` 600.10s objective 2166 / bound 2147** — 10분에 0.7% 개선뿐, bound 정체로 증명 실질 불가(가설 확인). 단일 워커+seed는 완전 재현되나 품질 붕괴(120s에 objective 2524, 격차 24%) — #132가 요구한 `num_search_workers=1`은 채택 불가.
- **수정 내용**: `engine/solver.py`에 `_make_solver()` 도입 — `relative_gap_limit=0.02` + `interleave_search` + `num_search_workers=8`(코어 수 아닌 상수 — 머신 무관 재현) + `random_seed=42`, env로 조정 가능. `ScheduleResult.best_objective_bound` 기록(격차 종료 OPTIMAL의 품질 검증 근거), generate 응답·solver_summary에 objective/bound 추가. 재현성 회귀 테스트 신설(`test_solver_determinism.py`).
- **After**: 학기 **`OPTIMAL`(격차 2% 이내) 7.15~7.49s** objective 2188 / bound 2146 — 30.02s 대비 4배 단축, 600초 최선(2166) 대비 1.0% 이내. 배정 58건 269시간·부족 0(#141과 동일 품질). 방학 `OPTIMAL` 4.13s → **2.66s**(bound=objective, 진짜 최적). **동일 입력 2회 hash 완전 일치**(학기·방학 각 2회 실측). 간헐 실패하던 free-grid 동일성 테스트 10회 연속 통과. 전체 회귀 333 passed·22 skipped.
- **비고**: 격차 한계로 멈춘 `OPTIMAL`은 "증명된 하한 대비 2% 이내 최적"을 뜻한다 — solver_summary의 objective/bound로 사후 검증 가능. interleave 없는 자유 병렬은 2.60s로 더 빠르지만 비결정적이라, 재현성이 필요한 기본 경로에는 interleave를 쓴다.

- **문제/가설**: develop에서 백엔드 테스트를 돌리자 `test_substitute_ai_check_api.py` 5건이 전부 404로 실패했다. 응답 본문이 `{"error": "Not Found"}`(라우트 미매칭)이라 권한·상태 문제가 아니라 **엔드포인트 자체가 없는 것**으로 봤다.
- **테스트 조건**: `origin/develop`(ee23213) 기준, `GEMINI_API_KEY=` 를 비운 채 `pytest -q` 전체 실행. 라우트 등록은 `grep '@router\.' backend/app/routers/substitutes.py`로 대조.
- **Before**: `substitutes.py`에 `@router.get("/{request_id}/ai-check")` 함수가 없고 파일 상단 독스트링의 API 목록 줄만 남아 있었다. 라우트는 8개(post 1·get 3·patch 3 + department). 테스트 **5 failed, 273 passed, 18 skipped**. 41c3aaa(PR #128 병합분)에는 361~390줄에 함수가 그대로 있었고, `4213cb5`(develop → feat-substitute-partial-api 병합)에서 `substitutes.py` 충돌을 브랜치 쪽으로만 정리하면서 유실됐다.
- **수정 내용**: 41c3aaa의 함수 30줄을 그대로 되돌려 `approve` 앞에 다시 넣었다. 의존 심볼(`substitute_check.get_ai_check`·`_get_request_or_404`·`require_own_department`·`_STATUS_APPROVED/REJECTED`)은 develop에도 모두 남아 있어 다른 수정은 필요 없었다. 부분 대타(#123)로 바뀐 승인 로직과는 겹치지 않는다 — ai-check는 조회 전용이다.
- **After**: 라우트 9개로 복구(`GET /{request_id}/ai-check` 재등록), 같은 조건에서 **278 passed, 18 skipped, 0 failed**. 되살린 테스트 5건이 404 권한(403)·미수락 상태(409)·정상 응답(200)·`is_stale` 플래그를 다시 검증한다.

## 2026-08-28 — '매주 반복' 격자가 오늘 기준 개관 시간을 그려, 방학에 낸 다음 학기 시간표가 저장에서 깎이던 문제

- **문제/가설**: 학생 화면(가능 시간 제출 → 매주 반복)에서 학기를 **가을학기로 바꿔도** 격자가 방학 모양(09:00~20:00)으로 그려졌다. `AvailabilityPanel`이 `periodOfDate(policy, new Date())`로 **오늘이 속한 기간**을 쓰기 때문이며, 그렇다면 격자 밖으로 밀린 학기 중 시간(08~09시·18~22시)은 저장 시 payload에서 통째로 빠질 것으로 봤다.
- **테스트 조건**: 시드 재주입 후 조수현(20220912)으로 로그인, `/schedule` → 가능 시간 제출 → 학기 선택기를 2026 가을학기로. 오늘은 2026-08-28(여름방학). 정책은 `GET /api/schedule/policy/me` 실제 응답(로욜라도서관 정보서비스팀: 학기 평일 08:00~22:00 / 방학 평일 09:00~20:00)을 그대로 사용.
- **Before**: 가을학기를 골라도 월요일 격자가 **09:00~20:00**(방학 블록)로 그려졌다. DB에는 가을학기 가능시간이 정상적으로 있었고(`GET /api/availability/me?term=2026-2` 30분 슬롯 85개), 그중 08:00~09:00·18:00~22:00 구간이 격자에 없어 화면에서 사라졌다. 그 상태로 저장하면 보이는 슬롯만 남기고 교체된다(`PUT /api/availability/me`는 그 학기를 통째 교체).
- **수정 내용**: `weeklyPeriod`를 오늘이 아니라 **보고 있는 학기의 시작일** 기준으로 판정하도록 바꾸고(`termStartDate`를 `utils/terms.js`에 추가), 학기를 못 찾으면 기존처럼 오늘로 떨어지게 뒀다. 학기는 통째로 학기 중이거나 통째로 방학이라 시작일 하나로 기간이 정해진다.
- **After**: 실제 정책 응답과 실제 유틸 모듈로 검증 — `2026-1 → semester(월 08:00~22:00)`, `2026-summer → vacation(09:00~20:00)`, `2026-2 → semester(08:00~22:00)`, `2026-winter → vacation(09:00~20:00)`. 학기 목록이 비면 `termStartDate`가 null을 반환해 오늘 기준으로 폴백한다. `npm run build` 통과(1.67s).

## 2026-08-28 — 시드 가능시간을 학기·방학 두 벌로 늘리자 학기 2주 생성이 OPTIMAL(0.20s) → FEASIBLE(30s 타임아웃)

- **문제/가설**: 시드 가능시간(`available_times.csv` 37행)이 학기 키 없이(NULL) 한 벌뿐이라, 학생 화면이 학기를 바꿔도 같은 값만 보이고 수업 시간표(`class_time`)는 아예 비어 있었다. 학기 중·방학을 각각 채우면 화면·근무표 생성이 실제 상황에 가깝게 돌아갈 것으로 봤다. 동시에 가능시간을 크게 늘리면 solver 탐색 공간이 넓어져 solve time이 늘어날 것으로 가정했다.
- **테스트 조건**: 임시 DB 2개(`stream_seedcheck_old` = develop 시드, `stream_seedcheck` = 새 시드)에 각각 `scripts/seed_mock_data.py` 주입 후, 같은 요청으로 `POST /api/schedule/generate` (department_id=2 정보서비스팀, num_days=14, time_limit 30s). 학기 중은 2026-09-07(2026-2), 방학은 2026-07-06(2026-summer) 시작. 근로 학생 9명 동일.
- **Before**: 가능시간 37행·학생당 평균 약 20시간(학기 구분 없음), 수업 시간표 0건.
  - 학기 중 2주: `OPTIMAL` / solve **0.20s** / 배정 70건 257시간 / 커버 30분 슬롯 312 / 미충원 0 / 학생별 26~39h
  - 방학 2주: `OPTIMAL` / solve **0.18s** / 배정 76건 242시간 / 커버 252 / 미충원 0 / 학생별 22~35h
- **수정 내용**: `available_times.csv`에 `term` 열을 추가하고 학기(`2026-2`)·방학(`2026-summer`) 두 벌로 재작성(180행), 학기별 수강 시간표 `class_times.csv`(39행, 방학분은 계절수업)를 새로 만들어 `seed_mock_data.py`가 `ClassTime`까지 넣도록 했다. 가능시간은 부서 정책의 근무 블록 경계에 맞추고(학생 화면이 블록 단위 체크), 같은 학생의 수업 시간과 겹치지 않게 생성했다.
- **After**: 가능시간 180행·학기 489시간/방학 439시간(학생당 각 48.9h·43.9h), 수업 38건.
  - 학기 중 2주: **`FEASIBLE` / solve 30.02s(시간 제한 도달)** / 배정 57건 269시간 / 커버 312 / 미충원 0 / 학생별 26~40h
  - 방학 2주: `OPTIMAL` / solve **4.13s** / 배정 54건 252시간 / 커버 252 / 미충원 0 / 학생별 21~46h
  - 개관 시간 커버리지(312·252 슬롯)와 미충원 0건은 그대로였고, 가정대로 solve time만 늘었다. 학기 중은 30초 제한에 걸려 최적성 증명을 못 하고 FEASIBLE로 끝난다 — 데모 체감 대기가 0.3초에서 30초로 바뀌므로, 학기 근무표 생성은 시간 제한을 올리거나 제약을 조이는 후속 검토가 필요하다.
  - 대타 후보 탐색(REQ-SUB-002) 실측: 시드 요청 4건의 후보가 각각 6·6·4·6명으로 잡혔다(블록별 가능 인원 최소 4명). 백엔드 테스트 254 passed / 13 skipped.
- **비고**: 학생 계정으로 화면을 눈으로 확인하다가 처음 만든 시드에서 **조수현의 계절수업(월수금 09-11)이 본인 시드 근무(월 09-12)·대타 수락 근무(화 09-12)와 겹치는 것**을 발견했다. 근무표 화면에서는 근무 칸이 수업 칸을 덮어 가려져 표만 봐서는 드러나지 않았다. 계절수업을 수·금으로 옮기고, "시드 근무와 그 학생의 수업이 겹치면 실패"하는 검증을 학기·방학 양쪽으로 추가했다.

## 2026-08-27 — 검토 프롬프트에 일별·주별 집계 추가: 오탐은 고쳤으나 약한 모델은 검증을 멈춤 (#114)

- **문제/가설**: 직전 라운드에서 측정한 9개 모델 중 7개가 `scale-clean`에서 오탐을 냈고, 원인이 대부분 하나로 수렴했다 — `per_student`(배치 기간 2주 총합, 26~32시간)를 "주당 20시간" 상한과 그대로 비교. 기간 합계와 주간 합계를 가르는 산술은 서버가 확정적으로 계산할 수 있으므로, LLM에 맡기지 말고 미리 계산해 넘기면 오탐이 줄어들 것으로 가정했다.
- **테스트 조건**: 14케이스 고정, 모델 9종 × 2라운드(repeat=1씩 별도 실행) = 252회 호출을 프롬프트 수정 전후로 동일하게 반복. 변경점은 `_build_prompt` 하나뿐이고 케이스·판정 기준·모델·호출 방식은 전부 동일.
- **Before**: 전체 221/252(88%). `scale-clean` 8/18 — 통과한 건 gemini-3.5-flash·3.7-flash 둘뿐.
- **수정 내용**: `app/scheduler/review.py`
  - `_student_hours_section()` 신규 — 배정 결과에서 학생별 **일자별·주(월~일)별 근무시간**을 서버가 계산해 프롬프트에 넣는다.
  - `per_student` 헤더에 기간을 명시(`per_student — 근무표 기간 2026-08-03(월)~2026-08-15(토) 전체 합계`)하고, "하루/주당 규칙은 per_student가 아니라 이 값으로 판단하라"는 지시를 새 섹션에 붙였다.
  - 프롬프트 길이: `scale-clean` 기준 3,319자 → 5,570자(+68%), 입력 토큰 1,816 → 2,421/호출.
- **After**: 전체 **212/252(84%)** — 겨냥한 케이스는 고쳤지만 총 검출률은 오히려 4%p 내려갔다. 호출 실패 0건.

  | 케이스 | Before → After (9모델 × 2라운드 = 18) |
  |---|---|
  | `scale-clean` | **8/18 → 14/18 (+6)** |
  | `tenure-relative-comparison` | 17/18 → 18/18 (+1) |
  | `scale-needle-sunday` | 17/18 → 14/18 (−3) |
  | `scale-needle-daily` | 15/18 → 12/18 (−3) |
  | `boundary-just-over` | 18/18 → 15/18 (−3) |
  | `weekly-hours` / `multi-rule` | 각 −2 |
  | `morning-min-staff` / `closing-gap` / `daily-max` | 각 −1 |

  **역효과의 정체 — 집계를 주니 약한 모델이 검증을 멈추고 "준수"로 결론냈다.** 집계 섹션이 잘못 계산된 게 아닌지 먼저 확인했는데, 렌더링된 프롬프트에는 위반이 그대로 적혀 있다:
  - `scale-needle-daily`: `20211357 … 2026-08-03(월) 8시간` (규칙은 하루 6시간 초과 금지)
  - `boundary-just-over`: `20221234 … 합계 12.5시간` (규칙은 주당 12시간 초과 금지)

  답이 문자 그대로 주어졌는데도 claude-haiku-4.5는 "모든 학생의 일일·주간 근무시간이 규정 범위 내"라고 했고, gpt-4.1-nano는 "12시간을 초과하지 않아 위반 없음", gpt-5.4-nano는 "위반 정황이 확인되지 않습니다"라고 답했다. 즉 집계표의 **존재 자체를 '준수의 근거'로 받아들이고 값을 읽지 않는** 실패로, 오탐(false positive)을 미검출(false negative)로 바꾼 셈이다.

  **계열별로 방향이 갈린다:**

  | 계열 | Before → After | Δ |
  |---|---|---|
  | **gemini** (3.7-flash, 3.5-flash, 3.5-flash-lite, 3.1-flash-lite) | 108/112 → **111/112** | **+3** |
  | claude (sonnet-5, haiku-4.5) | 52/56 → 48/56 | −4 |
  | gpt (4.1-mini, 5.4-nano, 4.1-nano) | 61/84 → 53/84 | −8 |

  손실은 전부 claude-haiku-4.5(−4)와 gpt nano 2종(−9)에 몰려 있고, gemini 계열은 개선됐다. 프로덕션 모델 gemini-3.5-flash와 후보 3.7-flash는 **양쪽 다 28/28로 변화 없음**(이미 상한이라 오를 자리가 없었다). 오탐이 실제로 사라진 건 gemini-3.1-flash-lite·3.5-flash-lite·gpt-4.1-mini 세 모델로, `scale-clean`에서 XX → OO로 바뀌었다.

  **토큰**: 입력은 전 모델 공통으로 +약 600토큰/호출 늘었지만, gemini-3.5-flash는 **총 토큰이 4,150 → 3,783으로 9% 줄었다** — 사고(thinking) 토큰이 줄어든 것으로, 집계를 미리 주면 모델이 그만큼 덜 추론한다. 반대로 나머지 모델은 총 토큰이 늘었다(입력 증가분이 그대로 반영).

  **결론**: 변경은 유지한다. 프로덕션 계열(gemini)이 순증(+3)이고 프로덕션 모델 자체는 검출력 동일에 토큰 9% 감소이므로 손해가 없다. 다만 이번 라운드의 진짜 소득은 **"서버가 계산해 주면 항상 낫다"가 거짓이라는 것** — 집계 제공은 강한 모델에는 중립~이득이지만 약한 모델에서는 검증을 생략하게 만들어 미검출을 늘린다. 저가 모델로 내려갈 계획이 생기면 이 프롬프트를 그대로 쓰면 안 되고, 집계와 함께 "집계값을 규칙과 직접 대조해 명시하라"는 지시를 강제하는 쪽을 따로 실험해야 한다.

## 2026-08-27 — 저가 티어 5종 추가 비교: 오탐 원인이 한 곳으로 수렴 (#114)

- **문제/가설**: 확장한 14케이스가 상위 구간을 가르는 걸 확인했으니, 이제 "얼마나 싼 모델까지 내려가도 되는가"를 잴 차례. 저가 티어는 검출 누락으로 무너질 것으로 예상했다.
- **테스트 조건**: 14케이스 동일, `eval_review.py --model`로 모델만 교체, 모델당 2라운드(repeat=1씩 별도 실행), 딜레이 없음. 모델 ID는 추측하지 않고 각 provider의 목록 API(`models.list()`)로 실재 확인 후 선택 — `gemini-3.5-flash-lite`, `gemini-3.1-flash-lite`, `gpt-4.1-nano`, `gpt-5.4-nano`, `claude-haiku-4-5-20251001`.
- **Before**: 상위 4종만 측정된 상태 — gemini-3.5/3.7-flash 14/14, claude-sonnet-5 13/14, gpt-4.1-mini 11~12/14.
- **After**: 총 9개 모델 실측. 호출 실패 0건.

  | Model | 검출률(1차→2차) | 평균 응답시간(2차) | 평균 토큰/호출(2차) | 실패 케이스(2차) |
  |---|---|---|---|---|
  | gemini-3.7-flash | **14/14 → 14/14** | 5.1s | 3,092 | 없음 |
  | gemini-3.5-flash(프로덕션) | **14/14 → 14/14** | 9.8s | 4,150 | 없음 |
  | claude-haiku-4.5 | 14/14 → **12/14** | 3.7s | 3,833 | `daily-max`, `scale-clean` |
  | claude-sonnet-5 | 13/14 → 13/14 | 7.0s | 3,933 | `scale-clean` |
  | gemini-3.5-flash-lite | 13/14 → 13/14 | **1.6s** | **2,052** | `scale-clean` |
  | gemini-3.1-flash-lite | 13/14 → 13/14 | **1.6s** | 2,041 | `scale-clean` |
  | gpt-4.1-mini | 12/14 → 11/14 | 3.2s | 2,006 | `closing-gap`, `daily-max`, `scale-clean` |
  | gpt-5.4-nano | 11/14 → 11/14 | 3.1s | 2,129 | `daily-max`, `multi-rule`, `scale-needle-daily` |
  | gpt-4.1-nano | 8/14 → 8/14 | 3.0s | 2,023 | 6건(가장 쉬운 `weekly-hours`도 미검출) |

  가설은 빗나갔다. 저가 티어가 무너진 축은 검출 누락이 아니라 **오탐**이고, 9개 중 7개가 `scale-clean` 하나에서 걸렸다. 통과한 건 gemini-3.5-flash·3.7-flash 둘뿐이다.

  **오탐 원인이 거의 하나로 수렴한다 — `per_student`의 기간 총합을 주간 상한과 직접 비교.** 프롬프트의 `per_student`는 batch 기간(2주) 총합인데, 규칙은 "주당 20시간"이다. gemini-3.1-flash-lite("모든 배정 학생의 주당 근무 시간이 20시간을 초과", 근거로 든 26~32시간은 2주 총합), gemini-3.5-flash-lite, gpt-4.1-mini(2차), gpt-5.4-nano가 전부 같은 착오를 했다. 실제 주간 최대는 16.0h로 상한 미달이다.

  같은 케이스에서 나온 다른 유형의 오류 두 가지:
  - **행-학생 오귀속(claude-haiku-4.5)**: "20221234: 2026-08-03(월) 12:00-16:00 + 13:00-17:00 = 8시간"이라고 했는데, 8/3의 12:00-16:00은 20232222의 배정이다. 20221234는 그날 13:00-17:00 하나뿐. 집계 이전에 어느 행이 누구 것인지를 잘못 읽었다.
  - **심각도 반전(gpt-5.4-nano)**: "규칙 위반으로 특정되는 배정 사례는 데이터에서 확인되지 않음"을 `severity=critical`로 출력. 앞 라운드에서 gpt-4.1-mini가 보인 것과 같은 유형이라, 일회성이 아니라 이 계열의 반복되는 실패 양상으로 보인다.

  **claude-haiku-4.5만 라운드 간 편차가 컸다**(14/14 → 12/14). 호스팅 모델 중 처음으로 온프레미스식 비결정성을 보였다 — 1회 측정으로 판단하면 gemini-flash와 동급으로 오인할 뻔했다.

  **가성비 관찰**: gemini-3.5-flash-lite는 claude-sonnet-5와 **같은 점수(13/14, 실패 케이스도 `scale-clean`으로 동일)**를 응답시간 1/4(1.6s vs 7.0s), 토큰 절반(2,052 vs 3,933)에 냈다. 검출력 상한이 필요 없는 용도라면 lite 티어가 유효한 선택지다.

  **결론**: 프로덕션은 Gemini flash 계열 유지가 맞다(REQ-SCHED-016) — `scale-clean`을 통과한 유일한 두 모델이다. 3.5 → 3.7-flash 업그레이드 근거도 유지(14/14 동일, 9.8s → 5.1s, 4,150 → 3,092토큰).

  **다음 할 일(코드 변경 후보)**: 오탐 7건 중 대부분이 `per_student` 기간 총합/주간 집계 혼동이므로, 모델을 바꾸기 전에 **프롬프트를 고치는 게 비용 대비 효과가 클 가능성이 높다** — `per_student`에 주차별 집계를 같이 넣거나 필드명을 "기간(2주) 총합"으로 명시하는 방향. 같은 14케이스로 프롬프트 수정 전후 검출률을 비교하면 효과를 바로 잴 수 있다(이 하네스의 원래 용도).

## 2026-08-27 — 평가 케이스 세트 확장: 계측기 포화 해소 (9 → 14케이스) (#114)

- **문제/가설**: 같은 날 앞선 라운드에서 claude-sonnet-5·gemini-3.5-flash·gemini-3.7-flash가 모두 9/9를 찍었다. 모델을 더 붙여도 9/9 행만 늘 뿐 순위 정보가 안 나오는 상태 — 케이스 세트가 위쪽에서 포화됐다고 보고, 모델 추가 대신 케이스 확장이 먼저라고 가정했다.
- **테스트 조건**: 기록된 18개 실행 결과(`backend/output/eval_*.json`) 전수로 케이스별 통과/시도를 집계해 변별력을 확인. 이후 확장한 14케이스로 4개 모델 × 2라운드 재측정(`eval_review.py`, repeat=1씩 별도 실행, gemini-3.7만 `--delay 15`).
- **Before**: 케이스별 변별력 집계 결과 — `clean-no-violation`은 **16/16 전원 통과로 변별력 0**, 호스팅 플래그십(gemini-3.5/3.7-flash, claude-sonnet-5)은 **9번의 실행에서 케이스 실패 0건**. 실패는 사실상 전부 온프레미스 3종에서만 발생했고 gpt-4.1-mini가 `daily-max`·`closing-gap` 두 개를 긁은 게 전부였다. 즉 상위 구간 해상도가 없었다.

  진단한 구멍 두 가지:
  1. **규모** — 기존 케이스는 배정행 1~5개·학생 1~2명. 샘플 데이터로 실제 2주 배정을 풀어 재보니 30분 슬롯 540개가 `merge_blocks`로 병합돼 **WorkSchedule 71행**(학생 9명, 13일)으로 저장된다. 검토 프롬프트가 실제로 받는 규모는 71행인데 15~70배 작은 입력으로만 재고 있었다.
  2. **오탐** — `forbid_critical` 케이스가 2개뿐이고 그중 하나는 전원 통과. 정밀도(위반 없는데 critical을 내는가)가 사실상 미측정.
- **수정 내용**: 케이스 5개 추가(9 → 14). 생성기가 규칙 위반을 전수 계산해 불변식을 검증한 뒤에만 JSON에 넣는다 — 검증 없이 만든 "위반 없음" 케이스는 오탐 측정 자체를 무효로 만들기 때문.
  - `scale-clean` — 실운영 규모(68행·학생 9명·2주·최대동시 2명) 위반 0건, `forbid_critical`. 생성기가 일간·주간·일요일·개관시간·자기중복 5개 축 전부 0건임을 검증.
  - `scale-needle-sunday` / `scale-needle-daily` — 같은 68행 기반에 위반 **1건만** 심음(요일 스캔 축 / 하루 누적 산술 축). 후자는 같은 날 연속 두 교대를 한 학생에게 몰아 하루 8시간을 만드는 방식이라 행 수·동시 인원이 불변이고, 주간 상한까지 같이 깨지지 않는 (날짜, 학생) 조합만 탐색해 needle을 단일 축으로 격리했다.
  - `boundary-exact-limit` / `boundary-just-over` — 주당 상한 12시간에 정확히 12.0h(위반 아님) vs 12.5h(위반). 0.5시간 차이로 경계 판단만 가름.
- **After**: 4개 모델 × 2라운드 실측. 호출 실패 0건.

  | Model | 검출률(1차→2차) | 평균 응답시간(2차) | 평균 토큰/호출(2차) | 실패 케이스 |
  |---|---|---|---|---|
  | gemini-3.7-flash | **14/14 → 14/14** | 5.1s | 3,092 | 없음 |
  | gemini-3.5-flash(프로덕션) | **14/14 → 14/14** | 9.8s | 4,150 | 없음 |
  | claude-sonnet-5 | 13/14 → 13/14 | 7.0s | 3,933 | `scale-clean`(양 라운드) |
  | gpt-4.1-mini | 12/14 → 11/14 | 3.2s | 2,006 | `scale-clean`(양 라운드), `closing-gap`(양 라운드), `daily-max`(2차) |

  **`scale-clean`이 새 변별점이다** — Gemini 2종은 두 라운드 다 통과, claude-sonnet-5와 gpt-4.1-mini는 두 라운드 다 오탐. 기존 9케이스로는 세 모델이 전부 동률이던 구간이 갈렸다.

  오탐의 내용이 각각 다르고, 셋 다 실제 운영에서 문제가 될 실패 유형이다:
  - **gpt-4.1-mini(1차)**: "일요일 근무가 배정되지 않아 규칙 준수됨", "모든 배정이 개관 시간 내에 있어 규칙 위반 사항 없음"을 **severity=critical로 출력**. 내용은 맞는데 심각도를 반대로 붙였다 — 깨끗한 근무표에 빨간 critical 2건이 뜨는 셈. 같은 응답에서 "근속 정보가 없어 일일 근무시간 확인 불가"라며 68행이 그대로 주어진 산술을 포기하기도 했다(근속 정보와 근무시간 기록을 혼동).
  - **gpt-4.1-mini(2차)**: `per_student`의 **2주 기간 총합(28~30시간)을 주간 상한 20시간과 직접 비교**해 위반이라고 판정. 프롬프트에 기간이 2026-08-03~08-15로 명시돼 있는데도 기간 총합/주간 집계를 구분하지 못했다.
  - **claude-sonnet-5**: 두 라운드 모두 오탐이지만 근거가 매번 달랐다(1차 주간 합계 오산, 2차 일간 합계 오산). 1차에서 지목한 20211357의 1주차를 독립 검산하니 **14.0h**(전체 최대 주간도 16.0h)로 20h 상한을 넘지 않는다 — 케이스 결함이 아니라 모델의 집계 오류로 확인.
- **부수 발견(미수정)**: `Case` 데이터클래스가 파싱하는 `expect_clarification_target_types`·`forbid_clarifications`·`department_id`를 `check_result`가 전혀 읽지 않는다. 이 필드를 넣은 케이스를 추가해도 기대치가 조용히 무시된다. `clarification_requests` 자체가 이 브랜치·develop·main 어디에도 없고 `feat-student-availability-blocks`에만 있어서, 되묻기 평가는 그 브랜치 머지 이후에 붙이는 게 맞다. 지금 상태로 되묻기 케이스를 추가하면 항상 통과하는 빈 검사가 된다.

  **결론**: 케이스 확장이 모델 추가보다 먼저였던 게 맞았다 — 5개를 늘렸을 뿐인데 기존 세트에서 동률이던 상위 3개 모델이 갈렸고, 갈린 지점이 검출 누락이 아니라 **오탐**이라는 게 이번 라운드의 핵심 소득이다. 프로덕션 provider는 Gemini 유지가 맞고(REQ-SCHED-016), 3.5 → 3.7-flash 업그레이드 근거는 이번에도 유지된다(동일 14/14에 응답시간 9.8s → 5.1s, 토큰 4,150 → 3,092). 다음 라운드에서는 저가 티어(haiku·flash-lite·nano) 하한 탐색과, 규모 케이스의 needle 축(주간 누적·최소 인원)을 더 늘리는 게 남았다.

## 2026-08-27 — AI 검토 모델 실험 2라운드: GPT·Claude 실측 + gemini-3.7-flash 클린 재측정 (#114)

- **문제/가설**: 8/24 라운드는 OPENAI_API_KEY·ANTHROPIC_API_KEY 미발급으로 GPT·Claude를 아예 못 돌렸고, gemini-3.7-flash는 무료 티어 일일 할당량(20/일, 프로젝트 단위) 소진으로 9케이스 완주를 못 했다. 세 키를 모두 유료로 발급해 #114 완료 기준(gpt/gemini/claude 실행 결과 저장)을 채우고, "호스팅 모델도 온프레미스처럼 재실행 시 검출률이 흔들리는가"를 같이 확인하려 함.
- **테스트 조건**: 8/24와 동일한 검출력 케이스 9개(`scripts/eval_review_cases.json`) · 동일 프롬프트(`review_system_prompt.md`) · 동일 스키마(`ReviewResult`), `eval_review.py --provider {openai,claude,gemini}`, repeat=1을 provider마다 2회 별도 실행(1차/2차). gemini-3.7-flash만 `--delay 15`(RPM 회피), 나머지는 딜레이 없음. 모델은 `.env` 기본값 — `gpt-4.1-mini`, `claude-sonnet-5`, `gemini-3.7-flash`, 프로덕션 대조군 `gemini-3.5-flash`(같은 날 1회 재측정 — 8/24 수치와 날짜가 달라 동일 조건 비교가 안 되므로 오늘 다시 잼).
- **Before**: 8/24 기준 — GPT·Claude는 `not_configured` 스킵으로 **측정치 없음**, gemini-3.7-flash는 4번의 조각난 시도 합산으로 9케이스 중 7개만 1회 이상 성공(429로 나머지 실패)해 **비교표에 못 올림**. 온프레미스 3종은 최고가 gemma3:12b 7/9(78%)→5/9(56%).
- **수정 내용**: 코드 변경 없음 — `.env`에 세 키 채우고 기존 하네스(PR #117)를 그대로 실행만 함. 원본 결과 JSON은 `backend/output/eval_2026-08-27_*.json`(gitignore, 로컬 보관).
- **After**: 호출 실패(429·quota 등) **0건**, 4개 모델 × 2라운드(3.5-flash만 1라운드) 전부 완주.

  | Provider | Model | 검출률(1차→2차) | 평균 응답시간(최소~최대, 2차) | 평균 토큰/호출(2차) |
  |---|---|---|---|---|
  | Claude | claude-sonnet-5 | **9/9 (100%) → 9/9 (100%)** | 5.3s (2.6~10.9s) | 3,461 (입력 3,068/출력 393) |
  | Gemini | gemini-3.7-flash | **9/9 (100%) → 9/9 (100%)** | 4.6s (2.4~8.7s) | 2,000 (입력 1,324/출력 249) |
  | Gemini(프로덕션) | gemini-3.5-flash | 9/9 (100%) — 1라운드 | 5.9s (2.9~9.8s) | 2,492 (입력 1,324/출력 239) |
  | OpenAI | gpt-4.1-mini | 7/9 (78%) → 8/9 (89%) | 2.8s (1.4~4.2s) | 1,619 (입력 1,457/출력 161) |

  관찰 3가지:

  1. **gemini-3.7-flash 클린 측정 완료** — 할당량 리셋 후 두 라운드 다 9/9, 429 0건. 프로덕션 3.5-flash 대비 응답시간(4.6s vs 5.9s)·토큰(2,000 vs 2,492) 모두 낮으면서 검출률은 동일 → 프로덕션 모델 업그레이드 후보로 볼 근거는 생겼다(단 3.7은 2라운드, 3.5는 1라운드라 표본이 비대칭).
  2. **gpt-4.1-mini만 유일하게 미검출 발생** — `daily-max`(하루 연속 근무 상한)는 **두 라운드 모두** 미검출로 재현됐고, `closing-gap`(마감 시간대 공백)은 1차만 미검출. 응답은 가장 빠르고(2.8s) 토큰도 가장 적지만(1,619) 이 케이스 세트에선 검출력이 한 단계 아래.
  3. **호스팅 모델은 온프레미스와 달리 재실행에 안정적** — 8/24에 온프레미스 3종이 재실행 시 22~23%p씩 떨어진 것과 대조적으로, claude-sonnet-5·gemini-3.7-flash는 두 라운드 다 9/9로 동일했고 gpt-4.1-mini만 ±1건 흔들렸다. 8/24의 "1회 측정으로 우열 판단 위험" 결론은 온프레미스 한정이고 호스팅 모델엔 그대로 적용되지 않는다.

  **토큰 수치 비교 시 주의(측정 방식 차이)**: 세 provider의 "총 토큰"은 정의가 다르다. OpenAI·Claude는 총계 = 입력+출력이 정확히 맞지만, Gemini의 `total_token_count`는 사고(thinking) 토큰을 따로 포함해서 입력+출력보다 크다(3.5-flash 22,427 vs 입력+출력 14,066 → 사고 8,361 / 3.7-flash 18,003 vs 14,155 → 사고 3,848). 또 Claude의 입력 토큰이 같은 프롬프트인데도 3,068/호출로 Gemini(1,324)의 2.3배인데, 이는 Claude에 구조화 출력이 없어 `ReviewResult`의 JSON 스키마 전체를 tool `input_schema`로 매 호출 넘기기 때문이다(하네스 구현상 오버헤드지 모델 특성이 아님). 따라서 위 표의 토큰 열은 provider 간 절대 비교가 아니라 같은 provider의 라운드 간 비교로만 쓴다.

  **결론**: 검출력만 보면 claude-sonnet-5 = gemini-3.7-flash = gemini-3.5-flash(9/9) > gpt-4.1-mini(7~8/9) > 온프레미스 최고 gemma3:12b(5~7/9). 프로덕션 provider를 Gemini에서 바꿀 근거는 여전히 없고(REQ-SCHED-016 유지), 다만 **같은 Gemini 안에서 3.5-flash → 3.7-flash 업그레이드는 검토할 만하다** — 다음 라운드에서 3.5/3.7을 같은 횟수로 맞춰 재측정 필요. claude-sonnet-5는 검출력 동률의 유효한 대안이지만 위 tool-schema 오버헤드를 걷어낸 뒤에 비용 비교를 해야 의미가 있다.

## 2026-08-27 — 좌측 바 DS 전환 · 본문 글자 크기 상향 (#115)

- **문제/가설**: "글씨가 전반적으로 작다"는 사용 피드백. 감각이 아니라 수치로 확인하려고 `frontend/src/**/*.jsx`의 `fontSize` 원시 숫자 분포를 집계했다. 좌측 바는 uiux 디자인과 다른 형태라는 지적도 함께 받았다.
- **테스트 조건**: 원시 `fontSize` 값 분포 집계 + `tokens.css`의 body 기본값 확인. 렌더 검증은 dev 서버 1440×900에서 학생 6탭·관리자 6탭을 순회하며 ① 가로 넘침(`document`/`main`의 `scrollWidth > clientWidth`) ② 글자 잘림(자식 없는 요소의 `scrollWidth/Height > clientWidth/Height`)을 계측.
- **Before**: 원시 `fontSize` 471곳 중 **12px 132곳·13px 189곳 — 68%가 12~13px**였고, `body` 기본값도 12px였다. DS 본문 규격은 `--fs-body` 14px이라 실제로 한 단계 작게 쓰이고 있었다. 좌측 바는 Shell·AdminShell에 **70줄씩 중복**된 채 12px 메뉴·`■` 문자 불릿으로 DS SidebarNav와 달랐다.
- **수정 내용**: ① 원시 값을 DS 타입 스케일 토큰으로 매핑하며 작은 쪽을 한 단계 올렸다(8~10→micro 11, 11→caption 12, 12→sm 13, 13~14→body 14, 15~16→title 16, 17~18→h3 18, 19~22→h2 21, 26→h1 26). `body` 기본값도 12px→`--fs-body`. ② DS 규격 공용 `SidebarNav`를 만들어 Shell·AdminShell 중복을 제거(232px 폭, `--surface-page` 레일 + 흰 패널, 7×7 사각 불릿, 14px 항목, `--fs-h3` 제목).
- **After**: 원시 `fontSize` **471 → 1곳**(밀도 제약이 있는 TimeGrid 범례 8px만 유지), DS 스케일 토큰 496곳 사용. 분포가 12~13px 중심에서 **13~14px 중심(sm 153·body 239)**으로 이동. 학생 6탭·관리자 6탭 전수 순회에서 **가로 넘침 0건, 글자 잘림 0건** — 30분 그리드(행 높이 18px)와 SAINT 상단 탭도 깨지지 않았다. 좌측 바 렌더 실측: 폭 232px, 배경 `rgb(245,246,248)`(`--surface-page`), 항목 14px, 활성 `--fw-bold`/`--sogang-red`/배경 `rgb(251,234,234)`(`--sogang-red-50`). `npm run build` 통과.
- **비고**: DS 원본 `SidebarNav`는 항목에 `background: "transparent"`를 인라인으로 박아 두는데, 그러면 `.stream-navitem[data-active]`의 배경 규칙이 인라인에 밀려 **활성 메뉴 하이라이트가 아예 표시되지 않는다.** 실측에서 활성 항목 배경이 `rgba(0,0,0,0)`으로 나와 발견했고, 인라인을 걷어내고 기본 배경까지 CSS 쪽으로 옮겨 해결했다.

## 2026-08-27 — 근무 시간표 달력 팝오버가 잘리던 문제 — 카드의 overflow:hidden 탈출

- **문제/가설**: 학생 근무 시간표에서 "확정된 근무가 없는 주"에 달력 버튼을 누르면 달력 대부분이 안 보인다는 제보. 근무가 있는 주에서는 멀쩡했다. 근무 유무에 따라 달라지는 건 **카드 높이**뿐이므로, 팝오버가 카드에 걸린 `overflow: hidden`에 잘리는 것으로 가정했다.
- **테스트 조건**: dev 서버, 근로 학생(20220042 김현서)으로 `/schedule` 진입 → 근무 없는 주까지 "다음 주" 이동 → 달력 열고, 팝오버와 `overflow: hidden` 조상의 `getBoundingClientRect()`를 비교. 뷰포트 1280×800·1280×480 두 조건.
- **Before**: 팝오버 높이 409px, 클리핑 카드 높이 183px(bottom 410) → **280px(약 68%)이 잘림**. 가정대로 `SchedulePage.jsx`의 카드(`overflow: hidden`, 둥근 모서리용)가 원인. 근무가 있는 주는 카드가 길어서 우연히 안 잘렸을 뿐이었다.
- **수정 내용**: `WeekCalendarButton`의 팝오버를 `createPortal`로 `document.body`에 띄우고 버튼 좌표 기준 `position: fixed`로 배치. 아래 공간이 부족하면 위로 뒤집고, 양쪽 다 부족하면 그 쪽 여유만큼만 잘라 내부 스크롤로 넘긴다. top을 뷰포트 안으로 최종 클램프. 포털은 카드와 같이 움직이지 않으므로 scroll·resize에 재배치를 걸고, 바깥 클릭 판정에 팝오버 ref를 추가했다(포털이 버튼 DOM 밖에 있어 기존 판정으로는 열자마자 닫힌다). Escape 닫기도 추가.
- **After**: 1280×800에서 근무 있는 주·없는 주 모두 팝오버 자연높이(459/407px)가 **잘림 없이 전부 표시**, 뷰포트 안, 중앙 지점 클릭 가능. 1280×480에서는 위로 뒤집혀(top 8, bottom 237) 뷰포트 안에 들어가고 내부 스크롤로 나머지 접근. 주 선택(9/07 → 9/21), 바깥 클릭·Escape 닫기 정상. 관리자 근로 시간표의 같은 버튼도 동일 확인. `npm run build` 통과.
- **비고**: 첫 수정에서는 첫 배치 때 팝오버 높이를 못 재 뒤집기 판정이 항상 실패했고, `maxHeight`에 걸어 둔 최소값 160px이 뷰포트를 넘길 수 있었다. 실측에서 관리자 화면이 뷰포트를 벗어나는 걸 보고 "가용 공간 기준 배치 + top 클램프 + 다음 프레임 재배치"로 다시 잡았다.

## 2026-08-27 — 디자인 시스템 컴포넌트 적용 2차 — 모달·빈 상태·오류 배너까지 전수 검증 (#115)

- **문제/가설**: 1차 감사는 "화면에 렌더된 요소"만 계측해서 **평상시 안 보이는 상태를 놓쳤다**. 모달·툴팁·빈 목록·오류 배너는 조건이 맞아야 나타나므로, 순회 계측만으로는 검증됐다고 말할 수 없다고 판단했다.
- **테스트 조건**: dev 서버에서 브라우저 스크립트로 숨은 상태를 **강제로 띄워** 계측했다. ① 매칭 없는 검색어를 입력해 빈 상태 ② `window.fetch`를 가로채 공고 API만 실패시켜 오류 배너 ③ 모달(신규 공고 등록·이전 공고 불러오기·대타 상세보기·검토/승인)과 DatePicker 팝오버를 클릭으로 열고 ④ 부서 설정의 ⓘ 툴팁 3개를 hover. 각 상태에서 미해석 `var(--*)`·잔존 hex·DS 미적용 필드를 셌다.
- **Before**: 오류 배너가 `danger-50 + danger-100` 조합으로 4개 화면에 복붙돼 있었고(공고 목록·지원 내역·관심 공고·지원 상세), 빈 상태 카드도 화면마다 폰트 크기가 15/16px로 제각각이었다. 탭 바는 `role="tablist"`·`aria-selected` 없이 `<button>` 나열이라 스크린리더가 탭으로 인식하지 못했다.
- **수정 내용**: DS 규격대로 `Alert`·`EmptyState`·`Card`·`Tabs`·`Checkbox`·`Radio` 6종을 추가하고 위 중복을 교체했다. `Tabs`에는 `role="tablist"`/`role="tab"`/`aria-selected`를, `Checkbox`/`Radio`에는 `useId` 기반 `label` 연결을 넣었다. `.stream-tab`·`.stream-control` 상태 규칙을 `tokens.css`에 추가.
- **After**: 숨은 상태 포함 전수 검증에서 **미해석 토큰 0건, 잔존 hex 0건, DS 미적용 필드 0건**. 렌더 실측 — Alert 배경 `rgb(251,234,234)`(`--danger-50`)·좌측 획 `3px rgb(176,17,22)`(`--danger`)·radius 3px(`--radius-sm`), EmptyState 제목 16px/700/`rgb(22,24,28)`(`--fs-title`·`--fw-bold`·`--text-strong`), Tabs 활성 600/`rgb(176,17,22)` vs 비활성 500/`rgb(118,124,137)`(`--text-muted`). `npm run build` 통과.
- **비고**: inline style 총량은 1,055 → 1,056으로 사실상 변화 없다. DS 컴포넌트 자체가 inline style 방식이라 페이지에서 줄어든 만큼 컴포넌트로 옮겨간 것이라, 이 수치는 이번 작업의 성과 지표가 아니다. 실제 성과는 중복 정의 제거와 규격 일치 쪽이다.

## 2026-08-27 — UI/UX 디자인 시스템 반영 감사 — 하드코딩 색상 244개 제거 (#115)

- **문제/가설**: `uiux/` 디자인 시스템이 "모든 탭에 반영"됐는지 확인하려 했다. 가설은 "토큰이 안 맞을 것"이었으나, 실제 원인은 다른 데 있을 것으로도 의심했다.
- **테스트 조건**: `uiux/tokens/*.css`와 `frontend/src/styles/tokens.css`의 `--토큰: 값` 쌍을 파싱해 대조. 화면 쪽은 `frontend/src/**/*.jsx`에서 하드코딩 hex·raw px·inline style 수를 계수. 렌더 검증은 dev 서버(5173)에서 학생 6탭·관리자 7탭을 순회하며 `[style]` 속성의 미해석 `var(--*)`와 잔존 hex를 브라우저에서 직접 계측.
- **Before**: 토큰 정의는 **125개 중 124개 일치**(`--shadow-focus` 1개만 누락, `--font-sans`/`--font-kr`은 한글 우선순위 조정으로 의도적 차이) — 즉 토큰 레이어는 이미 맞아 있었다. 문제는 화면이 그 토큰을 **안 쓰는 것**: 하드코딩 hex **244개**(Tailwind 계열 `#1F2937`·`#4B5563`·`#6B7280` 등 DS에 없는 회색이 다수), inline style 1,079개, raw px 791개. 필드 스타일 상수는 `inputStyle`/`selectStyle`/`cellInput`/`numberInputStyle` 등으로 **12곳에 중복 정의**(높이 30·32·34·36·38 제각각).
- **수정 내용**: ① 드리프트 hex를 DS 토큰으로 치환(속성명 기준으로 흰색을 `--surface-card`/`--text-on-brand`로 분기). ② SAINT 포털 크롬 재현값은 DS neutral로 바꾸지 않고 값 보존 토큰(`--saint-border` 등 9개) 신설. ③ 로그인·SAINT 홈 2개 화면은 외부 포털 픽셀 재현이라 예외 처리하고 파일 상단에 근거 주석. ④ DS 규격의 `Input`/`Select`/`Textarea` 공용 컴포넌트 신설 후 9개 파일의 중복 필드 스타일을 이것으로 교체(#75 범위 흡수).
- **After**: STREAM 화면 하드코딩 hex **244 → 0개**(SAINT 재현 2종 87개는 의도적 예외), inline style 1,079 → 1,055, raw px 791 → 743. 학생 6탭·관리자 6탭 전수 순회에서 **미해석 토큰 0건, 잔존 hex 0건, DS 미적용 필드 0건**. 렌더 값 실측: input 높이 38px, font-size 14px(`--fs-body`), border `rgb(203,206,213)`(`--border-default`), radius 3px(`--radius-sm`) — DS 규격과 일치. `npm run build` 통과, 콘솔 에러 0건.
- **비고**: 작업 중 `fix-frontend-review`(#103·#105)가 force-push로 리베이스돼(커밋 9개 SHA 변경 + 신규 4개) 기존 병합을 되돌리고 새 커밋 위에 재적용했다. 충돌 3건(`MonthCalendar`·`SchedulePage`·`AdminSchedulePage`)은 상대 커밋을 기준으로 두고 토큰화를 다시 입히는 방식으로 해소.

## 2026-08-24 — AI 검토 모델 실험: Gemini(프로덕션) vs 온프레미스 3종 (#114)

- **문제/가설**: 프로덕션 AI 검토(REQ-SCHED-016)는 Gemini 고정인데, 다른 provider(GPT·Claude)나 온프레미스 모델이 같은 규칙 위반 검출 과제에서 얼마나 쓸만한지 실측 데이터가 없었다. 온프레미스 7~12B급 모델이 무료 대안으로 쓸 만한 검출력이 나올지 확인하려 함.
- **테스트 조건**: 기존 검출력 케이스 9개(`scripts/eval_review_cases.json`, #80) 그대로, `eval_review.py --provider {gemini,local}`로 동일 프롬프트·스키마(`ReviewResult`) 통과, repeat=1. 온프레미스는 Mac M2/16GB + Ollama 0.32.15, 모델은 Ollama 라이브러리 기본 태그(q4 양자화) 그대로. GPT/Claude는 OPENAI_API_KEY·ANTHROPIC_API_KEY 미발급으로 이번 라운드 제외(`not_configured`로 스킵 확인만 함).
- **Before**: 비교 데이터 없음 — Gemini 외 provider는 검출력·응답속도 실측 이력 없었음.
- **수정 내용**: `eval_review.py`에 `--provider` 옵션 추가(gemini/openai/claude/local), 케이스별 호출 소요시간(elapsed_s)·토큰 사용량(input/output/total) 계측 추가. Gemini는 `review.py._call_gemini`가 `usage_metadata`를 `LAST_USAGE` 모듈 변수에 기록하는 side channel로, 기존 함수 시그니처·프로덕션 호출부는 변경 없음(Gemini 고정 유지, AI Layer 분리 원칙 그대로).
- **After**: 동일 조건으로 2회 반복 실행(1차 → 2차, repeat=1씩 별도 실행) — LLM 출력이 비결정적이라 재실행 시 검출률이 달라지는 것 자체도 기록.

  | Provider | Model | 검출률(1차→2차) | 평균 응답시간(최소~최대, 2차 기준) | 평균 토큰/호출(2차 기준) | 비고 |
  |---|---|---|---|---|---|
  | Gemini(프로덕션) | gemini-3.5-flash | 9/9 (100%) → 9/9 (100%) | 8.6s (2.7~44.3s) | 2,257 (입력 1,324/출력 234) | 두 라운드 다 429 1회(무료 티어 20회/일) 포함해도 안정적 |
  | 온프레미스 | gemma3:12b | 7/9 (78%) → 5/9 (56%) | 33.6s (11.8~50.6s) | 1,603 (입력 1,338/출력 265) | 1차 도중 Ollama Metal GPU OOM(`kIOGPUCommandBufferCallbackErrorOutOfMemory`)으로 전체 실패 → 서비스 재시작 후 정상화, 이후 수치는 재시작 후 값 |
  | 온프레미스 | deepseek-r1:7b | 6/9 (67%) → 4/9 (44%) | 36.6s (9.8~58.0s) | 2,017 (입력 1,456/출력 561) | "확인 불가 규칙" 케이스에서 `[time slot]`/`[adjective]` 같은 미채움 템플릿 placeholder를 그대로 출력하는 오류 관찰(2차) |
  | 온프레미스 | qwen2.5:7b | 2/9 (22%) → 2/9 (22%) | 7.6s (3.7~25.9s) | 1,558 (입력 1,466/출력 92) | 유일하게 재현됨(빈 findings로만 응답) — 검출력 부족이 부하·비결정성보다 모델 자체 한계로 보임 |

  호출 실패(quota 등) 0건(gemma3 1차의 GPU OOM 실패는 인프라 문제로 quota 실패와 별개, 재실행으로 회복). 원본 결과는 `backend/output/eval_2026-08-24_*.json`, `*-v2.json`(gitignore 대상, 로컬 보관). 결론: 이번 케이스 세트 기준으로는 Gemini가 검출력·응답속도·안정성 모두 압도적이라 프로덕션 provider 변경 근거는 없음. 온프레미스 3종 모두 재실행 시 검출률이 하락하는 경향(gemma3 -22%p, deepseek-r1 -23%p)을 보여 1회 측정만으로 우열을 단정하기 어렵고, qwen2.5:7b만 일관되게 낮음(이 과제엔 부적합). GPT/Claude는 키 발급 후 같은 케이스로 재측정 필요.

**추가: gemini-3.7-flash는 오늘 측정 불가 (할당량 소진)** — 같은 프로젝트의 다른 API 키로 바꿔도, 케이스 사이 15~40초 딜레이(`--delay` 옵션 신규 추가)를 줘도 9케이스 중 매번 대부분이 `429 RESOURCE_EXHAUSTED`로 실패했다. 에러 메시지가 명시한 quotaId는 `GenerateRequestsPerDayPerProjectPerModel-FreeTier`(한도 20/일) — RPM이 아니라 **일일 할당량이 Google Cloud 프로젝트 단위로 API 키와 무관하게 공유**되는 것으로 보이고, 여러 차례 재시도로 이미 소진됨. 4번의 조각난 시도를 합치면 9개 중 7개(weekly-hours·daily-max·multi-rule·closing-gap·clean-no-violation·unverifiable-rule·tenure-relative-comparison)는 최소 1회 성공(전부 PASS), sunday-forbidden·morning-min-staff 2개는 한 번도 성공 못함 — 표본이 동일 조건이 아니라 위 비교표에는 넣지 않는다. 할당량 리셋 후(`--model gemini-3.7-flash`, delay 불필요할 수도 있음) 깨끗한 9/9 재측정 필요.

## 2026-08-23 — 방학 기본 근무 슬롯 추가 — 방학 풀이가 OPTIMAL로 단축

- **문제/가설**: 방학 기간은 work_slots 미정의라 자유 30분 그리드로 배정 — 학기처럼 블록 단위 기본값이 필요. 블록 제약이 해공간을 좁혀 풀이에도 유리할 것으로 가정.
- **테스트 조건**: 샘플 데이터(`students_sample`, 학생 9명) + `library_info_service` 정책, 방학 기간 시작(2026-06-29 월) × 14일, `solve(time_limit_seconds=30.0)` 단독 실행. Before/After 차이는 정책 JSON의 `work_slots.default.vacation` 추가 여부뿐.
- **Before**: status=FEASIBLE, solve_time=30.02s(시간 제한 도달), objective=1994, 미충원 0, 배정 502슬롯.
- **수정 내용**: 방학 기본 블록 추가 — 평일 09-12·12-13·13-16·16-17·17-18·18-19·19-20(개관 09-20 정확 타일링), 토 09-12·12-13·13-16·16-17(09-17 타일링). 코드 변경 없음(정책 파일만).
- **After**: status=**OPTIMAL**, solve_time=**0.60s**(50배 단축 — 블록 등식으로 탐색 공간 축소), objective=1996(+0.1%), 미충원 0 동일, 배정 504슬롯, 블록 위반 0건(블록 있는 12일 전수). 전체 회귀 137건 통과.

## 2026-08-23 — 학기 고정 시간표: 서버 전개 + semester_pattern 국가 주간 상한 조임

- **문제/가설**: 기존 학기 고정은 프론트가 2주 결과를 그대로 복제해 확정 — 공휴일 단축·폐관·실제 학기 종료일을 무시하고, 국가근로 주 20h 패턴을 복제하면 월 46h 상한(HC-TIME-3) 위반. 복제를 서버로 옮겨 개관 시간 교집합을 취하고, 생성 시 국가 주간 상한을 9h로 조이면(9×5주=45≤46) 반복 후에도 규정이 구조적으로 지켜질 것으로 가정.
- **테스트 조건**: 샘플 데이터(`students_sample`, 학생 9명·2주) + `library_info_service` 정책, `solve(time_limit_seconds=30.0)` 단독 실행. Before/After 차이는 `_tighten_for_semester_pattern` 적용 여부뿐. 전개는 2026 실제 학사 캘린더로 브라우저 E2E 확인.
- **Before**: status=FEASIBLE, solve_time=30.04s, objective=13493, 국가 학생 주간 최대 20.0h (복제 시 월 80h+ 위반 가능).
- **수정 내용**: `expand_weekly_pattern`(confirm `repeat_until` 서버 전개 — 폐관 행 제거·단축 개관 클리핑·`adjusted_dates` 보고), `GenerateRequest.semester_pattern`(국가 주간 min(기존, 9h)), `AcademicCalendar.semester_containing`(응답 `semester_end`). 테스트 16건 추가(`test_semester_expand.py` 9, `test_schedule_confirm_repeat.py` 7).
- **After**: status=FEASIBLE, solve_time=30.04s, objective=13786(+2.2%), 국가 학생 주간 최대 **9.0h** — 월 상한 구조 보장. E2E(2026-09-07 시작, 12-21까지 반복 확정): 486건 저장, 조정 6일(추석 폐관 9/24-26 제외, 10/1·10/5·10/9 단축 클리핑) — 학사 캘린더와 일치. 전체 회귀 103건 통과.

## 2026-08-23 — 부서 정의 근무 슬롯(블록) all-or-none 제약 도입 (#89)

- **문제/가설**: 부서가 정의한 근무 슬롯(예: 학기 평일 09:00-10:30) 단위로 배정해야 하는데 솔버는 30분 슬롯을 자유롭게 조각 배정. 30분 그리드를 유지한 채 블록 단위 all-or-none Hard 제약만 추가하면 기존 시간 상한·인원 제약 무수정으로 동작할 것으로 가정.
- **테스트 조건**: 샘플 데이터(`students_sample`, 학생 9명·2주) + `library_info_service` 정책, `solve(time_limit_seconds=30.0)` 단독 실행. Before/After 차이는 정책 JSON의 `work_slots`(학기 평일 11블록·토 3블록) 추가 여부뿐.
- **Before**: status=FEASIBLE, solve_time=30.04s, objective=13470, 미충원 12슬롯(understaffing 12000), 배정 540슬롯.
- **수정 내용**: `WorkSlotBlockConstraint`(Hard, 블록 내 인접 변수 체인 등식) 추가, `DepartmentPolicy.work_slots` + `OpeningHoursResolver.resolve_work_blocks`(특별일 개관 구간과 교집합 클리핑), `department_policy.work_slots` DB 오버라이드, 정책 GET/PATCH API 확장 + 개관 시간 타일링 검증(400). 테스트 34건 추가(`test_work_slot_blocks.py` 18, `test_policy_work_slots_api.py` 16).
- **After**: status=FEASIBLE, solve_time=30.04s, objective=13892(+3.1%, 블록 등식으로 해공간 축소), 미충원 12슬롯 동일, 배정 540슬롯 동일, **블록 위반 0건**(블록 있는 12일 × 전 학생 전수 확인 — 모든 블록이 전부 배정 or 전무). 전체 회귀 121건 통과(라이브 LLM 8건 제외).

## 2026-08-23 — Solver status 기록 보강: solver_summary·로그에 status/solve_time 추가 (#84)

- **문제/가설**: DB `solver_summary`에 status·solve_time이 빠져 있어 확정된 시간표가 OPTIMAL이었는지 시간 제한 조기 종료(FEASIBLE)였는지 사후 추적 불가. 로그로도 남지 않아 이력 축적 안 됨.
- **테스트 조건**: 샘플 데이터(`students_sample`, 학생 9명·2주) + `library_info_service` 정책, `solve(time_limit_seconds=30.0)` 단독 실행.
- **Before**: `solver_summary` 키 = shortages·penalty_summary·per_student만 저장. Solver 실행 로그 없음.
- **수정 내용**: `routers/schedule.py` solver_summary에 `status`·`solve_time_seconds` 추가, `engine/solver.py` `_extract()`에 status·solve_time·objective INFO 로깅 추가. 테스트 2종(`test_solver_status_record.py`) 추가.
- **After**: 동일 조건 실행 시 로그 `Solver 종료: status=FEASIBLE solve_time=30.05s objective=13492` 출력 — 2주 샘플은 30초 제한에서 OPTIMAL이 아닌 FEASIBLE로 조기 종료됨이 실측으로 확인됨(= 동일 입력에도 결과가 달라질 수 있는 원인이 시간 제한임을 이제 기록으로 구분 가능). 신규 테스트 2건 통과.
## 2026-08-23 — Solver Edge Case 3종 검증 (인원 부족·학생 편중·조건 변경) (#83)

- **문제/가설**: `ScheduleSolver.solve()`/`solve_alternatives()` 직접 호출 테스트가 0건이라 극단 입력(인원 부족, 한 학생 편중, 재생성 안정성)에서의 동작이 미검증 상태였다.
- **테스트 조건**: `backend/tests/scheduler/test_solver_edge_cases.py` — 코드로 구성한 최소 정책(60분 슬롯, 평일 09-13시 개관 4슬롯, min 1/max 2, 가중치는 프로덕션과 동일)·전 기간 방학 캘린더·주 1회(7일) 시나리오. `pytest tests/scheduler/test_solver_edge_cases.py`로 재현.
- **Before**: Solver 직접 호출 테스트 0건 (검증 수치 없음).
- **수정 내용**: Edge Case 테스트 17건 추가 — 멘토 제안 3종(6건) + 추가 7종(11건: Hard 전수 검증·date_schedule 경로·시간 제한 status·부분 부족·빈 입력·아침 근무 불가 Hard·국가근로 특수 규칙). 코드 수정 없음 — 전부 기존 동작이 의도대로 확인됨.
- **After** (실측):
  - 인원 부족(완화 ON): `OPTIMAL` 0.023s — 가용 학생 없는 12슬롯이 shortage 리포트 12건 + understaffing 페널티 12,000으로 처리, 가용 슬롯 8건은 전부 배정. 부분 부족(min 2·1명 가용)도 required=2/assigned=1로 정확히 리포트.
  - 인원 부족(완화 OFF) 및 Hard 충돌(min 인원 20슬롯 vs 주간 상한 4시간): 둘 다 `INFEASIBLE` ≤0.002s.
  - 학생 편중: 전 시간대(20슬롯) 가용 학생이 주간 상한 14슬롯에서 멈추고, 가용 4슬롯 학생 2명은 각자 4슬롯 전부 배정 (`OPTIMAL` 0.010s, fair_hours shortfall 0). date_schedule 경로도 동일 결과.
  - 조건 변경 안정성: 학생 1명의 가용 슬롯 1개 제거 후 재생성 시 배정 34건 중 1건만 변경 (`OPTIMAL` 0.004s → 0.004s, diff=1).
  - Hard 전수 검증: 프로덕션 config + 샘플(9명·2주)로 풀어(`FEASIBLE` 10.04s, objective 13,501) 배정 540건을 전수 재검산 — max_per_slot·주간/월간/2주 상한·개관·can_work 위반 0건.
  - 시간 제한 조기 종료: 같은 샘플에 time_limit 0.001s → `UNKNOWN` 0.006s (INFEASIBLE과 구분됨을 검증, #84 status 기록과 연계).
