**Textarea** — multi-line text field for reasons, notes, and descriptions.

```jsx
<Textarea rows={4} placeholder="Reason for substitution request" />
```

`invalid` for error border; `rows` sets initial height; vertically resizable.
