**Select** — styled native dropdown. Pass `<option>` children.

```jsx
<Select defaultValue="all">
  <option value="all">All departments</option>
  <option value="lib">Library</option>
</Select>
```

Sizes `sm`/`md`/`lg`; `invalid` for error border.
